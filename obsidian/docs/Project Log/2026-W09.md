# Week of 24 Feb – 2 Mar 2026

Progress and contributions for this week. Add entries below; tag with **User** or **Hand** so we know who contributed.

---

- **Hand (2026-02-28):** Added frontend dual-UI convention to AGENT.md — both inline HTML in `app.py` and React in `frontend/` must be updated in sync when UI features change.
- **Hand (2026-02-28):** Implemented `goose` CLI backend with auto-derived `GOOSE_PROVIDER`/`GOOSE_MODEL` from `HELPING_HANDS_MODEL`, `--with-builtin developer` injection, dual `GH_TOKEN`/`GITHUB_TOKEN` mirroring, and Ollama host normalization.
- **Hand (2026-02-28):** Implemented `geminicli` CLI backend with `--approval-mode auto_edit` default for scripted runs, model unavailability auto-retry (strips `--model` flag), and `GEMINI_API_KEY` requirement enforcement.
- **Hand (2026-02-28):** Generalized CLI subprocess controls across all CLI backends: unified heartbeat/idle-timeout/IO-poll configuration via `HELPING_HANDS_CLI_*` env vars with 900s default idle timeout.
- **Hand (2026-02-28):** Added Celery Beat scheduling system with cron-based task scheduling, RedBeat integration, and full CRUD API endpoints (`/schedules`).
- **Hand (2026-02-28):** Added `ollama` AI provider wrapper, completing the provider abstraction layer (openai, anthropic, google, litellm, ollama).
