## v189 — Input validation for `generate_pr_description()` / `generate_commit_message()` and CLI `--max-iterations`

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Add input validation to `generate_pr_description()` and
`generate_commit_message()` in `pr_description.py` for `base_branch` and
`backend` parameters (empty/whitespace rejection). Add `--max-iterations`
positive integer validation in `cli/main.py` (matching the existing
`--pr-number` pattern). Write tests for all changes.

## Tasks

- [x] Add `base_branch` empty/whitespace validation to `generate_pr_description()`
- [x] Add `backend` empty/whitespace validation to `generate_pr_description()` and `generate_commit_message()`
- [x] Add `--max-iterations` positive integer validation to `cli/main.py`
- [x] Write tests for all changes
- [x] Run lint + format + tests
- [x] Update PLANS.md
- [x] Move plan to completed

## Completion criteria

- `generate_pr_description()` rejects empty/whitespace `base_branch` with `ValueError`
- `generate_pr_description()` and `generate_commit_message()` reject empty/whitespace `backend` with `ValueError`
- `--max-iterations <= 0` exits with a clear error message
- Tests verify all new behavior
- All existing tests pass
- `ruff check` and `ruff format` pass
