## v182 — Extract pre-commit error constant, DRY git error fallback, add task state set guards

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Extract duplicate pre-commit UV-missing error message to `_PRECOMMIT_UV_MISSING_MSG`
constant in `base.py` (DRY 2 identical occurrences). Extract `_DEFAULT_GIT_ERROR_MSG`
constant for the "unknown git error" magic string fallback. Add module-level
assertions in `server/app.py` verifying that `_TERMINAL_TASK_STATES` and
`_CURRENT_TASK_STATES` are disjoint, and that `_TASK_STATE_PRIORITY` keys are
a subset of `_CURRENT_TASK_STATES`. Write tests for all new constants and guards.

## Tasks

- [x] Extract `_PRECOMMIT_UV_MISSING_MSG` constant in base.py (DRY 2 occurrences)
- [x] Extract `_DEFAULT_GIT_ERROR_MSG` constant in base.py
- [x] Add disjointness assertion for `_TERMINAL_TASK_STATES` / `_CURRENT_TASK_STATES` in app.py
- [x] Add subset assertion for `_TASK_STATE_PRIORITY` keys ⊆ `_CURRENT_TASK_STATES` in app.py
- [x] Write tests for all new constants and assertions
- [x] Run lint + format + tests
- [x] Consolidate 2026-03-15.md into Week-12
- [x] Update PLANS.md, QUALITY_SCORE.md
- [x] Move plan to completed

## Completion criteria

- Duplicate pre-commit error message extracted to single constant
- Git error fallback string extracted to constant
- Task state sets have module-level disjointness and subset assertions
- Tests verify constant values, constant usage, assertion behavior
- All existing tests pass
- `ruff check` and `ruff format` pass
