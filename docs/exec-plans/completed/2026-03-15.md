## v184 — AI provider empty message validation, filesystem mkdir_path hardening, normalize_messages content type check

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Lift empty-message validation from Google provider to `AIProvider.complete()` base class
(consistent guard across all providers). Add OSError handling to `mkdir_path()` in `filesystem.py`
(matching `write_text_file()` pattern). Add content type validation to `normalize_messages()` for
non-string content values. Write tests for all changes.

## Tasks

- [x] Create active plan
- [x] Add empty message validation to `AIProvider.complete()` — check that at least one message has non-empty content before calling `_complete_impl()`
- [x] Remove redundant empty message check from `GoogleProvider._complete_impl()`
- [x] Add OSError handling to `mkdir_path()` — wrap `target.mkdir()` in try/except OSError, raise `RuntimeError` with context
- [x] Add content type validation to `normalize_messages()` — validate `content` field is str or None, reject other types with `TypeError`
- [x] Write tests for all changes
- [x] Run lint + format + tests
- [x] Consolidate daily plan into Week-12, remove redundant 2026-03-15.md
- [x] Update PLANS.md, QUALITY_SCORE.md
- [x] Move plan to completed

## Completion criteria

- All 5 AI providers consistently reject empty-content messages via base class
- Google provider no longer duplicates empty-message check
- `mkdir_path()` has consistent OSError→RuntimeError wrapping
- `normalize_messages()` rejects non-string content values
- Tests verify all new behavior
- All existing tests pass
- `ruff check` and `ruff format` pass
