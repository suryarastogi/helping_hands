## v188 — DRY `redact_credentials`, use constants in regex, add debug logging to catch-all handlers

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Fix `redact_credentials()` in `github_url.py` to use `GITHUB_TOKEN_USER`
and `GITHUB_HOSTNAME` constants instead of hardcoded strings in the regex pattern.
DRY `_redact_sensitive()` in `github.py` to delegate to `redact_credentials()` from
the shared module instead of duplicating the regex. Add `logger.debug(..., exc_info=True)`
to the 2 remaining catch-all exception handlers without traceback logging:
`_finalize_repo_pr()` in `base.py` and `_ci_fix_loop()` in `cli/base.py`.

## Tasks

- [x] Fix `redact_credentials()` to use `GITHUB_TOKEN_USER` and `GITHUB_HOSTNAME` constants
- [x] DRY `_redact_sensitive()` in `github.py` to delegate to `redact_credentials()`
- [x] Add `logger.debug(exc_info=True)` to `_finalize_repo_pr()` catch-all (base.py:1016)
- [x] Add `logger.debug(exc_info=True)` to `_ci_fix_loop()` catch-all (cli/base.py:1360)
- [x] Write tests for all changes
- [x] Run lint + format + tests
- [x] Consolidate daily plan file into Week-12
- [x] Update PLANS.md
- [x] Move plan to completed

## Completion criteria

- `redact_credentials()` regex uses module constants, not hardcoded strings
- `_redact_sensitive()` delegates to `redact_credentials()` (single implementation)
- All catch-all `except Exception` handlers in lib/ have `logger.debug(exc_info=True)`
- Tests verify all new behavior
- All existing tests pass
- `ruff check` and `ruff format` pass
