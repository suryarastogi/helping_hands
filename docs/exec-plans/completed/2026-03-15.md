## v167 — Add docstrings to goose.py/opencode.py/e2e.py, extract E2E and OpenCode constants

**Status:** Completed
**Created:** 2026-03-15

**Documentation and constant extraction:** Added Google-style docstrings with Args/Returns/Raises sections to 13 methods in `goose.py` (`_pr_description_cmd`, `_describe_auth`, `_normalize_base_command`, `_resolve_cli_model`, `_has_goose_builtin_flag`, `_apply_backend_defaults`, `_command_not_found_message`, `_normalize_goose_provider`, `_infer_goose_provider_from_model`, `_normalize_ollama_host`, `_resolve_ollama_host`, `_resolve_goose_provider_model_from_config`, `_build_subprocess_env`), 5 methods in `opencode.py` (`_build_opencode_failure_message`, `_build_failure_message`, `_command_not_found_message`, `_invoke_opencode`, `_invoke_backend`), and 8 methods in `e2e.py` (`__init__`, `_safe_repo_dir`, `_work_base`, `_configured_base_branch`, `_build_e2e_pr_comment`, `_build_e2e_pr_body`, `run`, `stream`). Extracted `_AUTH_ERROR_TOKENS` tuple constant in `opencode.py` (5 lowercase auth error substrings). Extracted 5 E2E constants: `_E2E_GIT_USER_NAME`, `_E2E_GIT_USER_EMAIL`, `_E2E_COMMIT_MESSAGE`, `_E2E_PR_TITLE`, `_E2E_STATUS_MARKER`. **4030 passing tests (+29 new), 112 skipped.**

## Tasks

- [x] Create active plan
- [x] Add Google-style docstrings to 13 goose.py methods
- [x] Add Google-style docstrings to 5 opencode.py methods
- [x] Extract `_AUTH_ERROR_TOKENS` constant tuple in opencode.py
- [x] Extract 5 E2E constants from e2e.py (`_E2E_GIT_USER_NAME`, `_E2E_GIT_USER_EMAIL`, `_E2E_COMMIT_MESSAGE`, `_E2E_PR_TITLE`, `_E2E_STATUS_MARKER`)
- [x] Add Google-style docstrings to 8 e2e.py methods
- [x] Write tests for all new constants and docstrings (29 tests)
- [x] Run lint + format + tests — 4030 passing, 112 skipped
- [x] Update Week-12 with v164-v167 summaries
- [x] Update PLANS.md, QUALITY_SCORE.md
- [x] Move plan to completed

## Completion criteria

- All target methods have Google-style docstrings with Args/Returns sections
- All hardcoded strings extracted to module-level constants
- Tests verify constant values, docstring presence
- All existing tests pass
- `ruff check` and `ruff format` pass

## v168 — Add docstrings to langgraph.py, app.py validators, cli/base.py

**Status:** Completed
**Created:** 2026-03-15

**Documentation:** Added Google-style docstrings with Args/Returns/Raises sections to 3 methods in `langgraph.py` (`__init__`, `_build_agent`, `run`), 4 methods in `server/app.py` (`_coerce_tools`, `_validate_tools`, `_coerce_skills`, `_validate_skills`), and 22 methods in `cli/base.py` (`__init__`, `_truncate_summary`, `_normalize_base_command`, `_base_command`, `_resolve_cli_model`, `_apply_backend_defaults`, `_render_command`, `_container_enabled`, `_container_image`, `_container_env_names`, `_use_native_cli_auth`, `_native_cli_auth_env_names`, `_effective_container_env_names`, `_execution_mode`, `_float_env`, `_io_poll_seconds`, `_heartbeat_seconds`, `_idle_timeout_seconds`, `_build_subprocess_env`, `_build_failure_message`, `_build_init_prompt`, `_build_task_prompt`, `_repo_has_changes`, `_looks_like_edit_request`, `_should_retry_without_changes`, `_build_apply_changes_prompt`, `_interrupted_pr_metadata`, `_finalize_after_run`, `_format_pr_status_message`, `_format_ci_fix_message`). **4113 passing tests (+83 new), 126 skipped.**

## v169 — Add docstrings to iterative.py helper methods

**Status:** Completed
**Created:** 2026-03-15

**Documentation:** Added Google-style docstrings with Args/Returns/Raises sections to 21 methods across `iterative.py`: 18 in `_BasicIterativeHand` (`__init__`, `_build_iteration_prompt`, `_is_satisfied`, `_extract_inline_edits`, `_extract_read_requests`, `_extract_tool_requests`, `_merge_iteration_summary`, `_execute_read_requests`, `_parse_str_list`, `_parse_positive_int`, `_parse_optional_str`, `_truncate_tool_output`, `_format_command_result`, `_format_web_search_result`, `_format_web_browse_result`, `_run_tool_request`, `_execute_tool_requests`, `_apply_inline_edits`), 1 in `BasicLangGraphHand` (`_result_content`), and 2 in `BasicAtomicHand` (`_make_input`, `_extract_message`). **4173 passing tests (+60 new), 126 skipped.**

## Tasks

- [x] Create active plan
- [x] Add Google-style docstrings to 18 _BasicIterativeHand methods
- [x] Add docstrings to 1 BasicLangGraphHand method (`_result_content`)
- [x] Add docstrings to 2 BasicAtomicHand methods (`_make_input`, `_extract_message`)
- [x] Write tests for docstring presence (60 tests)
- [x] Run lint + format + tests — 4173 passing, 126 skipped
- [x] Update PLANS.md, QUALITY_SCORE.md
- [x] Move plan to completed

## Completion criteria

- All 21 target methods have Google-style docstrings with Args/Returns sections
- Tests verify docstring presence and sections
- All existing tests pass
- `ruff check` and `ruff format` pass

## v170 — Add Attributes sections to all public dataclass docstrings

**Status:** Completed
**Created:** 2026-03-15

**Documentation:** Expanded one-line class docstrings on 12 public dataclasses to include Google-style `Attributes:` sections documenting each field. Covers `ToolSpec`/`ToolCategory` (registry.py), `WebSearchItem`/`WebSearchResult`/`WebBrowseResult` (web.py), `CommandResult` (command.py), `HandResponse` (base.py), `PRResult` (github.py), `PRDescription` (pr_description.py), `HandModel` (model_provider.py), `SkillSpec` (skills/__init__.py), `RepoIndex` (repo.py), `ScheduledTask` (schedules.py). **4197 passing tests (+24 new), 128 skipped.**

## Tasks

- [x] Create active plan
- [x] Add Attributes to ToolSpec, ToolCategory in registry.py
- [x] Add Attributes to WebSearchItem, WebSearchResult, WebBrowseResult in web.py
- [x] Add Attributes to CommandResult in command.py
- [x] Add Attributes to HandResponse in hand/base.py
- [x] Add Attributes to PRResult in github.py
- [x] Add Attributes to PRDescription in pr_description.py
- [x] Add Attributes to HandModel in model_provider.py
- [x] Add Attributes to SkillSpec in skills/__init__.py
- [x] Add Attributes to RepoIndex in repo.py
- [x] Add Attributes to ScheduledTask in schedules.py
- [x] Write tests for Attributes section presence (24 tests passed, 2 skipped)
- [x] Run lint + format + tests — 4197 passing, 128 skipped
- [x] Move plan to completed

## Completion criteria

- All 12 public dataclasses have Attributes sections in their docstrings
- Tests verify Attributes section presence for each class
- All existing tests pass
- `ruff check` and `ruff format` pass

## v171 — Add Attributes to Config dataclass, add docstrings to web.py and registry.py helpers

**Status:** Completed
**Created:** 2026-03-15

**Documentation:** Added Google-style `Attributes:` section to `Config` dataclass in `config.py` documenting all 11 fields (missed in v170's dataclass sweep). Added Google-style docstrings with Args/Returns/Raises sections to 4 private helpers in `web.py` (`_require_http_url`, `_strip_html`, `_as_string_keyed_dict`, `_extract_related_topics`) and 8 private helpers in `registry.py` (`_parse_str_list`, `_parse_positive_int`, `_parse_optional_str`, `_run_python_code`, `_run_python_script`, `_run_bash_script`, `_run_web_search`, `_run_web_browse`). **4233 passing tests (+36 new), 128 skipped.**

## Tasks

- [x] Add Attributes section to Config dataclass in config.py (11 fields)
- [x] Add docstrings to 4 web.py private helpers
- [x] Add docstrings to 8 registry.py private helpers
- [x] Write tests for Attributes/docstring presence (36 tests)
- [x] Run lint + format + tests — 4233 passing, 128 skipped

## Completion criteria

- Config dataclass has Attributes section documenting all fields
- All 12 target functions have Google-style docstrings
- Tests verify docstring/Attributes presence
- All existing tests pass
- `ruff check` and `ruff format` pass

## v172 — Add docstrings to docker_sandbox_claude.py, pr_description.py, config.py, skills/__init__.py

**Status:** Completed
**Created:** 2026-03-15

**Documentation:** Added Google-style docstrings with Args/Returns sections to 7 methods in `docker_sandbox_claude.py` (`__init__`, `_should_cleanup`, `_wrap_sandbox_exec`, `_execution_mode`, `_build_failure_message`, `_command_not_found_message`, `_fallback_command_when_not_found`), expanded one-liner docstrings to full Google-style with Args/Returns in 2 functions in `pr_description.py` (`_build_prompt`, `_build_commit_message_prompt`), 1 function in `config.py` (`_load_env_files`), and 1 function in `skills/__init__.py` (`_discover_catalog`). **4263 passing tests (+30 new), 128 skipped.**

## Tasks

- [x] Create active plan
- [x] Add Google-style docstrings to 7 docker_sandbox_claude.py methods
- [x] Expand docstrings in _build_prompt and _build_commit_message_prompt (pr_description.py)
- [x] Expand docstring for _load_env_files (config.py)
- [x] Expand docstring for _discover_catalog (skills/__init__.py)
- [x] Write docstring presence tests (30 tests)
- [x] Run lint + format + tests — 4263 passing, 128 skipped
- [x] Update PLANS.md, QUALITY_SCORE.md
- [x] Move plan to completed

## Completion criteria

- All 11 target functions have Google-style docstrings with Args/Returns sections
- Tests verify docstring presence and section content
- All existing tests pass
- `ruff check` and `ruff format` pass
