## v187 — Server endpoint path parameter validation

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Add empty/whitespace validation to 8 FastAPI endpoint path parameters
(`task_id` in `monitor`/`get_task`, `schedule_id` in 6 schedule endpoints).
Extract a shared `_validate_path_param()` helper following the existing
`_cancel_task()` validation pattern. Add Google-style docstrings to
`_build_task_status()` and `_schedule_to_response()`. Refactor `_cancel_task()`
to use `_validate_path_param()` (DRY).

## Tasks

- [x] Extract `_validate_path_param()` helper in app.py
- [x] Add validation to `monitor()` and `get_task()` for `task_id`
- [x] Add validation to 6 schedule endpoints for `schedule_id`
- [x] Add Google-style docstring to `_build_task_status()`
- [x] Add Google-style docstring to `_schedule_to_response()`
- [x] Refactor `_cancel_task()` to use `_validate_path_param()`
- [x] Write tests for all changes (38 new tests)
- [x] Run lint + format + tests
- [x] Update PLANS.md
- [x] Move plan to completed

## Completion criteria

- All 8 endpoints validate path params (empty/whitespace → ValueError)
- `_validate_path_param()` DRYs the validation pattern
- `_cancel_task()` refactored to use shared helper
- `_build_task_status()` and `_schedule_to_response()` have Google-style docstrings
- Tests verify all new behavior (38 new tests)
- All existing tests pass
- `ruff check` and `ruff format` pass

---

## v186 — Claude CLI docstrings, MCP tool input validation

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Add Google-style docstrings to 5 undocumented methods in `claude.py`
(`_StreamJsonEmitter.__call__`, `_process_line`, `result_text`,
`ClaudeCodeHand._resolve_cli_model`, `_skip_permissions_enabled`). Add input
validation to MCP tools `path_exists` (empty path guard) and `run_bash_script`
(mutual exclusivity at MCP layer).

## Tasks

- [x] Create active plan
- [x] Add Google-style docstrings to 5 claude.py methods
- [x] Add empty-path validation to MCP `path_exists` tool
- [x] Add mutual-exclusivity validation to MCP `run_bash_script` tool
- [x] Write tests for all changes (30 new tests)
- [x] Run lint + format + tests
- [x] Update PLANS.md
- [x] Move plan to completed

## Completion criteria

- All 5 claude.py methods have Google-style docstrings
- MCP `path_exists` rejects empty/whitespace path with ValueError
- MCP `run_bash_script` rejects both-None and both-provided at MCP layer
- Tests verify all new behavior
- All existing tests pass
- `ruff check` and `ruff format` pass
