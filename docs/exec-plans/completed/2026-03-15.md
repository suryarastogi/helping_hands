## v186 — Claude CLI docstrings, MCP tool input validation

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Add Google-style docstrings to 5 undocumented methods in `claude.py`
(`_StreamJsonEmitter.__call__`, `_process_line`, `result_text`,
`ClaudeCodeHand._resolve_cli_model`, `_skip_permissions_enabled`). Add input
validation to MCP tools `path_exists` (empty path guard) and `run_bash_script`
(mutual exclusivity at MCP layer).

## Tasks

- [x] Create active plan
- [x] Add Google-style docstrings to 5 claude.py methods
- [x] Add empty-path validation to MCP `path_exists` tool
- [x] Add mutual-exclusivity validation to MCP `run_bash_script` tool
- [x] Write tests for all changes (30 new tests)
- [x] Run lint + format + tests
- [x] Update PLANS.md
- [x] Move plan to completed

## Completion criteria

- All 5 claude.py methods have Google-style docstrings
- MCP `path_exists` rejects empty/whitespace path with ValueError
- MCP `run_bash_script` rejects both-None and both-provided at MCP layer
- Tests verify all new behavior
- All existing tests pass
- `ruff check` and `ruff format` pass
