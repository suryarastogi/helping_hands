## v136 — Gitignore E2E/Playwright artifacts and commit message quality hardening

**Status:** Completed
**Created:** 2026-03-12

## Goal

Two self-contained improvements from TODO.md:

1. **Gitignore E2E/Playwright test artifacts** — `frontend/test-results/`, `frontend/playwright-report/`, and `frontend/blob-report/` directories are generated by Playwright but not gitignored, causing accidental commits of large test artifacts.
2. **Commit message quality hardening** — `_parse_commit_message()` accepts trivially short/meaningless messages like `feat: -` or `feat: ...`. Add minimum-length validation and reject messages that are just punctuation/ellipsis. Also harden `_commit_message_from_prompt()` to reject trivially short extracted text (single char, punctuation-only).

## Tasks

- [x] Add `frontend/test-results/`, `frontend/playwright-report/`, `frontend/blob-report/` to `.gitignore`
- [x] Add `_is_trivial_message()` validator and `_MIN_COMMIT_MSG_LENGTH` constant to `pr_description.py`
- [x] Integrate `_is_trivial_message()` into `_parse_commit_message()` to reject trivial CLI output
- [x] Integrate `_is_trivial_message()` into `_commit_message_from_prompt()` to reject trivial heuristic text
- [x] Add tests for new gitignore patterns (3 parametrized tests in `TestGitignorePlaywrightArtifacts`)
- [x] Add tests for `_is_trivial_message` (15 tests in `TestIsTrivialMessage`)
- [x] Add tests for `_parse_commit_message` trivial rejection (5 tests in `TestParseCommitMessageTrivialRejection`)
- [x] Add tests for `_commit_message_from_prompt` trivial rejection (4 tests in `TestCommitMessageFromPromptTrivialRejection`)
- [x] Update TODO.md with completion notes
- [x] Run lint and tests — 3376 passing, 36 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md)

## Completion criteria

- All new tests pass (27 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v136 notes

---

## v137 — Extract health check timeout constants and DRY Anthropic API constants

**Status:** Completed
**Created:** 2026-03-12

## Goal

Two self-contained improvements:

1. **Extract health check timeout constants** in `server/app.py` — six hardcoded timeout values scattered across health check functions (`_check_redis_health`, `_check_db_health`, `_check_workers_health`, `_get_claude_oauth_token`, `_fetch_claude_usage`, `_collect_celery_current_tasks`, `_resolve_worker_capacity`) are replaced with module-level constants for maintainability and discoverability.

2. **DRY Anthropic usage API constants** in both `server/app.py` and `server/celery_app.py` — the usage API URL (`https://api.anthropic.com/api/oauth/usage`), beta header (`oauth-2025-04-20`), and user-agent string (`claude-code/2.0.32`) are duplicated identically in both modules. Extract to module-level constants in each file (modules don't import each other's internals per architecture rules).

## Tasks

- [x] Extract `_KEYCHAIN_TIMEOUT_S`, `_USAGE_API_TIMEOUT_S`, `_REDIS_HEALTH_TIMEOUT_S`, `_DB_HEALTH_TIMEOUT_S`, `_CELERY_HEALTH_TIMEOUT_S`, `_CELERY_INSPECT_TIMEOUT_S` in `server/app.py`
- [x] Extract `_ANTHROPIC_USAGE_URL`, `_ANTHROPIC_BETA_HEADER`, `_USAGE_USER_AGENT` in `server/app.py`
- [x] Extract `_ANTHROPIC_USAGE_URL`, `_ANTHROPIC_BETA_HEADER`, `_USAGE_USER_AGENT`, `_USAGE_API_TIMEOUT_S` in `server/celery_app.py`
- [x] Add tests for constant values and usage in `server/app.py` (value, type, sign invariants)
- [x] Add tests for constant values and usage in `server/celery_app.py`
- [x] Run lint and tests — 3835 passing, 2 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-11)

## Completion criteria

- All new tests pass (17 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v137 notes

---

## v138 — Extract hardcoded magic numbers to module-level constants (Hand base, CLI base, CLI main)

**Status:** Completed
**Created:** 2026-03-12

## Goal

Three self-contained improvements continuing the v135-v137 constant extraction pattern:

1. **Hand base class constants** (`base.py`) — Extract hardcoded git user defaults, CI configuration defaults, branch naming constants, output truncation limit, and file list preview limit to module-level constants.

2. **CLI hand base constants** (`cli/base.py`) — Extract hardcoded process termination timeout, CI check poll interval, and subprocess execution timeout to module-level constants.

3. **CLI main constants** (`cli/main.py`) — Extract hardcoded shallow clone depth and temp directory prefix to module-level constants.

## Tasks

- [x] Extract `_DEFAULT_BASE_BRANCH`, `_DEFAULT_GIT_USER_NAME`, `_DEFAULT_GIT_USER_EMAIL`, `_DEFAULT_CI_WAIT_MINUTES`, `_DEFAULT_CI_MAX_RETRIES`, `_BRANCH_PREFIX`, `_UUID_HEX_LENGTH`, `_MAX_OUTPUT_DISPLAY_LENGTH`, `_FILE_LIST_PREVIEW_LIMIT`, `_LOG_TRUNCATION_LENGTH` in `base.py`
- [x] Extract `_PROCESS_TERMINATE_TIMEOUT_S`, `_CI_POLL_INTERVAL_S`, `_PR_DESCRIPTION_TIMEOUT_S` in `cli/base.py`
- [x] Extract `_DEFAULT_CLONE_DEPTH`, `_TEMP_CLONE_PREFIX` in `cli/main.py`
- [x] Add tests for constant values and usage (31 tests: value, type, sign invariants)
- [x] Run lint and tests — 3436 passing, 37 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-11)

## Completion criteria

- All new tests pass (31 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v138 notes

---

## v139 — Extract hardcoded magic numbers in claude.py, pr_description.py, and CLI failure output

**Status:** Completed
**Created:** 2026-03-12

## Goal

Four self-contained improvements continuing the v135-v138 constant extraction pattern:

1. **Claude CLI preview truncation constants** (`cli/claude.py`) — Extract hardcoded text preview (200), tool result preview (150), and command/prompt preview (80) truncation limits to module-level constants.

2. **PR description truncation constants** (`pr_description.py`) — Extract hardcoded summary truncation (2000/1000), user prompt context (500), error output tail (300/500), and commit message length (72) limits to module-level constants.

3. **DRY failure output tail constant** (`cli/claude.py`, `cli/codex.py`, `cli/gemini.py`, `cli/opencode.py`) — The `output.strip()[-2000:]` failure message tail truncation is duplicated identically in 4 CLI hands. Extract to a module-level constant in each file (modules don't import each other's internals per architecture rules).

4. **Fix hardcoded file list limit** (`cli/base.py`) — Line 353 uses hardcoded `[:200]` instead of the existing `_FILE_LIST_PREVIEW_LIMIT` constant from `base.py`.

## Tasks

- [x] Extract `_TEXT_PREVIEW_MAX_LENGTH`, `_TOOL_RESULT_PREVIEW_MAX_LENGTH`, `_COMMAND_PREVIEW_MAX_LENGTH` in `cli/claude.py`
- [x] Extract `_FAILURE_OUTPUT_TAIL_LENGTH` in `cli/claude.py`, `cli/codex.py`, `cli/gemini.py`, `cli/opencode.py`
- [x] Extract `_PR_SUMMARY_TRUNCATION_LENGTH`, `_COMMIT_SUMMARY_TRUNCATION_LENGTH`, `_PROMPT_CONTEXT_LENGTH`, `_PR_ERROR_TAIL_LENGTH`, `_COMMIT_ERROR_TAIL_LENGTH`, `_COMMIT_MSG_MAX_LENGTH` in `pr_description.py`
- [x] Import and use `_FILE_LIST_PREVIEW_LIMIT` from `base.py` in `cli/base.py`
- [x] Add tests for constant values and usage (29 tests: value, type, sign, cross-module sync invariants)
- [x] Run lint and tests — 3436 passing, 37 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-11)

## Completion criteria

- All new tests pass
- `ruff check` and `ruff format` pass
- Docs updated with v139 notes

---

## v140 — Extract remaining magic numbers, GitHub PR number validation, truncation limit safety

**Status:** Completed
**Created:** 2026-03-12

## Goal

Three self-contained improvements continuing the hardening pattern:

1. **Extract remaining magic numbers** — `limit=2000` in `cli/base.py` `_build_apply_changes_prompt()`, `1024` byte read buffer in `cli/base.py` and `docker_sandbox_claude.py`, and `max_tokens=1024` default in `anthropic.py`. Extract to module-level constants `_APPLY_CHANGES_TRUNCATION_LIMIT`, `_STREAM_READ_BUFFER_SIZE`, and `_DEFAULT_MAX_TOKENS`.

2. **GitHub PR number/limit validation** — `get_pr(number)`, `update_pr_body(number)`, and `list_prs(limit)` in `github.py` don't validate their numeric parameters. Negative or zero values produce confusing PyGithub errors. Add explicit `ValueError` guards.

3. **`_truncate_diff` limit validation** — `_truncate_diff(limit)` in `pr_description.py` accepts zero/negative limits without error. Add `limit > 0` guard with `ValueError`.

## Tasks

- [x] Extract `_APPLY_CHANGES_TRUNCATION_LIMIT = 2000` in `cli/base.py`
- [x] Extract `_STREAM_READ_BUFFER_SIZE = 1024` in `cli/base.py` and `docker_sandbox_claude.py`
- [x] Extract `_DEFAULT_MAX_TOKENS = 1024` in `anthropic.py`
- [x] Add `number > 0` validation in `get_pr()` and `update_pr_body()` in `github.py`
- [x] Add `limit > 0` validation in `list_prs()` in `github.py`
- [x] Add `limit > 0` validation in `_truncate_diff()` in `pr_description.py`
- [x] Add tests for all improvements
- [x] Run lint and tests
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-11)

## Completion criteria

- All new tests pass
- `ruff check` and `ruff format` pass
- Docs updated with v140 notes
