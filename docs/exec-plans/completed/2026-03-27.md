# 2026-03-27 Daily Consolidation

## Multiplayer Performance & Backend Awareness Fix (v313)
- Fixed backend awareness state extraction — `get_connected_players()` now correctly extracts the `player` sub-dict from nested Yjs awareness state
- Frontend position broadcast throttling (60ms) reduces network traffic

## Multiplayer Leave Names & Chat Dedup Fix (v314)
- Leave messages show actual player names via `playerNamesRef` cache
- Per-player sequence counter fixes chat dedup over-filtering

## Multiplayer Cursor Sharing (v315)
- Remote mouse cursors visible as colored SVG arrow pointers with name labels
- `RemoteCursor` component, 100ms throttled broadcast, 80ms CSS interpolation

## Cursor Broadcast Throttle Coverage (v316)
- 3 tests for `updateCursor()` throttle behavior

## useRecentRepos Hook Test Coverage (v317)
- 16 tests — last custom hook without co-located tests

## RepoChipInput & RepoSuggestInput Test Coverage (v318)
- 45 tests for the two remaining untested frontend components

## App.tsx & useTaskManager Branch Coverage (v319)
- App.tsx: 69.23% → 81.25% branch, useTaskManager: 72.03% → 82.68% branch
- Overall branch coverage: 88.55% → 90.23%

## Multiplayer Hardening & Edge Case Coverage (v320)
- _clamp_float NaN/Infinity fix, client-side cursor clamping
- localStorage error handling tests, backend partial failure tests

## Design Doc Refresh & Multiplayer Resilience (v321)
- Timer cleanup (5 new timeout refs), accessibility improvements
- Refreshed multiplayer design doc to match Yjs architecture

## Decoration Placement Cooldown & Decoration Query Endpoint (v322)
- `DECO_COOLDOWN_MS` (1500ms) prevents decoration spam
- Backend `GET /health/multiplayer/decorations` endpoint

## Extract Inline HTML Template from app.py (v323)
- Extracted 2293-line `_UI_HTML` to `server/templates/ui.html`
- `_load_ui_template()` helper loads template at module import time
- app.py: 4363 → 2085 lines (-52%)
- 5 new backend tests (247 server app tests total)

---

**Totals for 2026-03-27:** v313–v323 (11 plans), 698 frontend tests, 247+ server app tests, 90 backend multiplayer tests
