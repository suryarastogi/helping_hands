## v185 — API key whitespace stripping, _build_generic_pr_body validation, filesystem error messages

**Status:** Completed
**Created:** 2026-03-15

**Scope:** Strip whitespace from API key environment variables across all 5 AI providers
(prevents silent auth failures from copy-paste whitespace). Add `commit_sha` and `stamp_utc`
validation to `_build_generic_pr_body()`. Include path context in `read_text_file()` error
messages for better debugging.

## Tasks

- [x] Create active plan
- [x] Add `.strip()` to API key env var reads in OpenAI, Anthropic, Google, LiteLLM providers
- [x] Add `.strip()` with fallback defaults to Ollama provider (api_key and base_url)
- [x] Add `commit_sha` and `stamp_utc` validation to `_build_generic_pr_body()`
- [x] Include display path in `read_text_file()` FileNotFoundError and IsADirectoryError messages
- [x] Write tests for all changes (20 new tests)
- [x] Run lint + format + tests
- [x] Update PLANS.md
- [x] Move plan to completed

## Completion criteria

- All 5 providers strip whitespace from API key env vars before truthy check
- Ollama falls back to defaults when whitespace-only key/base_url provided
- `_build_generic_pr_body()` rejects empty/whitespace/non-string commit_sha and stamp_utc
- `read_text_file()` error messages include the file path for debugging
- Tests verify all new behavior
- All existing tests pass
- `ruff check` and `ruff format` pass
