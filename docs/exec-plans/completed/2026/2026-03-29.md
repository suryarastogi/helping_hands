# 2026-03-29 — Coverage Hardening Day

## GooseCLIHand & CLIHandBase Coverage Hardening (v334)

**GooseCLIHand coverage (88% → 99%):** 10 new tests covering `_read_goose_config`
(YAML ImportError, parse exception, non-dict data, successful provider+model,
model-only, empty keys, file not found) and `_resolve_goose_provider_model_from_config`
(config fallback, empty provider inference, unknown model fallback).

**CLIHandBase coverage (98% → 99%):** 7 new tests covering `_repo_has_changes`
HEAD advance detection, `_has_pending_changes` delegation, and
`_fetch_failed_check_logs` (subprocess timeout, FileNotFoundError, max_lines
truncation, missing run_id).

**17 new tests. 6510 backend tests, 75.97% coverage.**

---

## Stream Emitter & Multiplayer Coverage + Test Fix (v335)

**Fixed `test_env_var_forwarding`:** Cleared `HELPING_HANDS_CLAUDE_USE_NATIVE_CLI_AUTH`
and `HELPING_HANDS_USE_NATIVE_CLI_AUTH` env vars so native CLI auth doesn't block
ANTHROPIC_API_KEY forwarding into the Docker sandbox.

**`_StreamJsonEmitter` coverage (98% → 100%):** 4 new tests covering JSON primitive
string/number passthrough and non-dict block skip in assistant/user events.

**`multiplayer_yjs.py` coverage (95% → 99%):** 5 new tests covering awareness=None,
unparseable state, ydoc=None, ydoc.get exception, and deco_map=None edge cases.

**9 new tests. 6519 backend tests, 75.84% coverage.**

---

## Hand Base & CLI Hand Coverage Hardening (v336)

**Hand base.py coverage (99% → 100%):** 4 new `_working_tree_is_clean` tests
(TimeoutExpired, OSError, clean tree, dirty tree) and 1 `_push_to_existing_pr`
clean working tree branch test.

**cli/base.py coverage (99%, 4 miss → 1 branch):** 1 new `_ci_fix_loop`
loop_deadline timeout test and 1 `_poll_ci_checks` early break test when
remaining poll time ≤ 0.

**7 new tests. 6526 backend tests, 75.95% coverage.**
