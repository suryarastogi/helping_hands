# 2026-03-29 Daily Consolidation (v334–v337)

Four execution plans completed on 2026-03-29: GooseCLIHand & CLIHandBase
coverage hardening, ModelProvider coverage hardening, OAuth token test
fix with credentials file coverage, and multiplayer YJS & Claude stream
emitter coverage.

## Summary

| Plan | Theme | New Tests | Total Passed |
|---|---|---|---|
| v337 | Multiplayer YJS & Claude stream emitter coverage | 9 | 6533 backend |
| v336 | OAuth token test fix & credentials coverage | 6 | 7631 backend |
| v335 | ModelProvider coverage hardening | 15 | 6524 backend |
| v334 | GooseCLIHand & CLIHandBase coverage hardening | 17 | 6510 backend |

## v334 — GooseCLIHand & CLIHandBase Coverage Hardening

**GooseCLIHand coverage (88% → 99%):** 10 new tests covering `_read_goose_config`
(yaml ImportError, parse exception, non-dict, success, model-only, empty keys,
not found) and `_resolve_goose_provider_model_from_config` (config file fallback,
empty provider inference, unknown model). **CLIHandBase coverage (98% → 99%):**
7 new tests covering `_repo_has_changes` HEAD advance, `_has_pending_changes`
delegation, `_fetch_failed_check_logs` edge cases.

**17 new tests. 6510 backend tests, 75.97% coverage.**

## v335 — ModelProvider Coverage Hardening

**`_require_langchain_class` direct unit tests:** 3 tests covering success path,
failure with derived install string, and failure with custom install string.
**`resolve_hand_model` provider-name branches:** 4 tests for direct provider names,
5 tests for `provider/` trailing-slash with empty model. **Empty model validation:**
2 tests for `build_langchain_chat_model` and `build_atomic_client` with empty model.
**Bug fix:** `test_env_var_forwarding` — cleared env var leaking into test.

**15 new tests (51 model_provider total). 6524 backend tests, 75.64% coverage.**

## v336 — OAuth Token Test Fix & Credentials Coverage

**Test regression fix:** 16 broken `_get_claude_oauth_token` tests across 3 test
files — `_read_claude_credentials_file()` (added as first-try path before Keychain
fallback) was finding a real credentials file before the `subprocess.run` mock was
reached. Fixed by mocking `_read_claude_credentials_file` to return `None` in all
Keychain-path tests.

**New coverage:** 5 `_read_claude_credentials_file` unit tests (valid file, missing
file, invalid JSON, missing key, OSError). 1 credentials-file-first-path test
verifying `_get_claude_oauth_token` returns the file token without hitting Keychain.

**6 new tests. 7631 backend tests, 96.09% coverage.**

## v337 — Multiplayer YJS & Claude Stream Emitter Coverage

**multiplayer_yjs.py (95% → 99%):** 5 new tests — `get_player_activity_summary`
awareness-None room skip and unparseable raw state skip; `get_decoration_state`
ydoc-None room skip, deco_map exception skip, deco_map-None skip.

**cli/claude.py (98% → 99%):** 4 new `_StreamJsonEmitter` tests — JSON primitive
string/number pass-through, non-dict block in assistant message skipped, non-dict
block in user message skipped.

**9 new tests. 6533 backend tests, 75.84% coverage.**
