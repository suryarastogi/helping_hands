# 2026-04-04 Daily Consolidation

Seventeen execution plans completed covering doctor enhancements, interactive CLI
mode, grill module testability, core utility coverage, CLI hand coverage,
server module coverage, edge case coverage, troubleshooting guide, server
endpoint coverage hardening, shared git clone extraction, token helper
extraction, E2E coverage, dead code cleanup, test consolidation,
model inference prefix expansion, and server pure helper coverage.

---

## v347 — Doctor & RepoIndex Enhancements

- Docker and Node.js availability checks for `helping-hands doctor`
- `file_count` property and `has_file()` binary search on `RepoIndex`
- 16 new tests

## v348 — Doctor Server-Mode Prerequisite Checks

- Redis CLI and Docker Compose checks for server-mode users
- Docs index and README section fixes
- 8 new tests

## v349 — Interactive CLI Mode & AI Provider Types Coverage

- Interactive CLI mode: `read_prompt_from_stdin()` for TTY and piped input
- AI provider `types.py` full branch coverage (23 tests)
- 29 new tests total

## v350 — Grill Module Testability & Coverage

- Deferred celery imports so pure helpers testable without server extra
- grill.py helper coverage 4% → 99%
- 37 new tests, 13 broken tests fixed

## v351 — Core Utility Module Test Coverage

- `validation.py`, `github_url.py`, `factory.py` all 0% → 100%
- 49 new tests

## v352 — CLI Hand Test Coverage: OpenCode + Devin

- `opencode.py` (`_describe_auth`, `_pr_description_cmd`) full coverage
- `devin.py` (`_pr_description_cmd`, `_resolve_cli_model`) full coverage
- 17 new tests

## v353 — Server Module Coverage Gaps

- `server/app.py` 77% → 90%+, `server/schedules.py` 77% → 95%
- TestClient and mocked Redis tests
- ~55 new tests

## v354 — Remaining Edge Case Coverage

- Last <2% gaps in `github.py`, `cli/base.py`, `cli/claude.py`, `cli/goose.py`
- 17 new tests

## v355 — Troubleshooting Guide & Docs Refresh

- User-facing `docs/TROUBLESHOOTING.md`
- AGENTS.md metadata refresh
- 12 doc structure tests

## v356 — Server App Endpoint Coverage Hardening

- `server/app.py` 90% → 95%+ via edge-case tests
- Task diff, file tree, file content, helper function branches
- 7971 passed, 98.70% coverage (with server extras)

## v357 — Extract Shared Git Clone Utility

- Extracted `run_git_clone()` into `lib/github_url.py`
- CLI and server both delegate to shared function
- 13 new tests

## v357 — Extract Token Helpers & Add Tests

- Extracted `redact_token`, `read_claude_credentials_file`, `get_claude_oauth_token`
  into `server/token_helpers.py`
- 22 new tests

## v358 — E2E Coverage & Exports Cleanup

- `_draft_pr_enabled` 8 env-var-gating tests, `stream()` 2 async wrapper tests
- Added `__all__` to `server/multiplayer_yjs.py`
- 10 new tests

## v359 — Dead Code Cleanup & Tech Debt Resolution

- Removed unreachable `if not candidate:` guard in `_commit_message_from_prompt`
- Resolved tech debt item (dead code since v173)
- 0 new tests (40+ existing tests already cover the simplified logic)

## v360 — Test Consolidation & Cleanup Candidate Resolution

- Resolved all 6 `# TODO: CLEANUP CANDIDATE` markers in test files
- Deleted `test_provider_build_inner.py` (9 duplicate tests)
- Removed 33 redundant isinstance/positive/type-only assertions from 5 constant test files
- Removed 6 docstring-policy tests from `test_iterative_constants.py`
- 42 tests removed total, 0 behavioral coverage lost
- 6779 tests pass, 76.03% coverage

## v362 — Expand Model Inference Prefixes

- Added 10 common OSS model family prefixes (mistral, mixtral, phi, codellama,
  deepseek, qwen, starcoder, vicuna, yi) to `_infer_provider_name` for Ollama routing
- Added explicit OpenAI reasoning model prefixes (gpt, o1, o3, o4)
- Refactored to `_OLLAMA_MODEL_PREFIXES` / `_OPENAI_MODEL_PREFIXES` tuple constants
- Updated `model-resolution.md` design doc
- 21 new tests

## v363 — Server Pure Helper Coverage

- Unit-tested pure helper functions across `server/app.py`, `server/celery_app.py`,
  and `server/schedules.py`
- Covers task extraction, status normalization, source-tag merging, Flower env config,
  kwargs parsing, usage-level extraction, backend validation, runtime formatting,
  repo-path resolution, cron/interval scheduling
- 111 new tests across 3 files
- With server extras: app.py 1%→94%, celery_app.py 3%→99%, schedules.py 2%→95%
- Total coverage with server extras: 98.71% (8100 tests pass)

---

**Total: ~427 new tests across 17 plans, 42 redundant tests cleaned up in v360.**
