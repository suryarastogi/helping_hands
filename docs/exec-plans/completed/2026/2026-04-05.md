# 2026-04-05 — Server Coverage, Registry API, Validation, Factory, Type Guards, List Backends, Enabled Status, List Tools, CLI Error Exit Consistency, Backend Descriptions, Coverage Accuracy, ARCHITECTURE.md Refresh, Registry Coverage Completion, Boundary Input Validation, CLI Introspection Design Doc, Provider API Key Map Consistency & Frontend Component Test Coverage

Seventeen execution plans completed.

---

## Remaining Server Coverage Gaps (v366)

Closed last coverage gaps in server modules by testing previously-uncovered
enabled paths: grill enabled-path endpoints (POST /grill, POST /grill/{id}/message,
GET /grill/{id}), `_launch_interval_chain` Celery chain dispatch,
`_get_redis_client` Redis URL resolution, and app lifespan async context manager.

**10 new tests. 8139 total tests (with server extras). 99.44% coverage.**

---

## Registry Public API Test Coverage (v367)

Added 52 dedicated tests for the registry module's public API functions:
tool category enumeration, selection normalization, validation, resolution,
legacy flag merging, runner map building, category-for-tool lookup, and
prompt instruction formatting (iterative `@@TOOL` blocks and CLI
natural-language guidance).

**52 new tests. 97 total registry tests. Registry coverage: 66%.**

---

## Validation & Factory Hardening (v368)

Added type guard to `validate_repo_value()` — non-string input now raises
`TypeError` instead of `AttributeError`. Added module-level consistency
assertion in `factory.py` verifying `SUPPORTED_BACKENDS` and
`_BACKEND_ENABLED_ENV_VARS` stay in sync.

**7 new tests.**

---

## Filesystem & Web Type Guards (v369)

Added `isinstance` type guards to two security-boundary functions:
`resolve_repo_target()` in `filesystem.py` (rejects non-`Path` `repo_root`)
and `_decode_bytes()` in `web.py` (rejects non-`bytes` `payload`). Both now
raise clear `TypeError` instead of confusing `AttributeError`.

**6 new tests.**

---

## `--list-backends` CLI Flag (v370)

Added `--list-backends` flag to the CLI that prints a table of all supported
backends with their availability status. CLI-backed hands show whether the
binary is on PATH; library hands show whether the Python extra is installed.
Intercepted before argparse like `--version` so it works without a positional
`repo` argument.

**12 new tests. 6936 total tests. 76.21% coverage.**

---

## Enrich `--list-backends` with Enabled Status (v371)

Enriched `--list-backends` output to show both availability (binary found /
extra installed) AND enabled/disabled status per backend via `*_ENABLED` env
vars. Added `is_backend_enabled()` public API to `factory.py`. Disabled
backends now show `[-]` with the env var name. Unmapped backends in
`_check_backend_available()` trigger a logger warning instead of silently
returning "available".

**10 new tests. 6946 total tests. 76.32% coverage.**

---

## `--list-tools` CLI Flag & Registry Coverage (v372)

Added `--list-tools` CLI flag for tool category discoverability, complementing
`--list-backends`. `list_tools()` formats a table of all tool categories with
their tool spec names. Intercepted before argparse so it works without a
positional `repo` argument. Also closed the last registry coverage gap
(`_run_bash_script` both/neither error branch, line 255).

**9 new tests (7 list-tools + 2 registry). 6955 total tests. 76.37% coverage. Registry: 100%.**

---

## CLI Error Exit Backends Consistency (v374)

Fixed `_CLI_ERROR_EXIT_BACKENDS` missing `opencodecli` and `devincli`. When
these CLI backends failed with `RuntimeError`, `ValueError`, or `OSError`, the
exception re-raised with a full stack trace instead of a clean error message.
Added structural consistency test verifying all CLI-tool-backed backends (those
in `_BACKEND_CLI_TOOL`) are present in `_CLI_ERROR_EXIT_BACKENDS`.

**9 new tests (5 consistency + 4 error-exit paths). 6969 total tests. 76.49% coverage.**

---

## Backend Descriptions & `--list-backends` Enrichment (v375)

Added `BACKEND_DESCRIPTIONS` dict to `factory.py` with a human-readable
description for each backend. Added `_validate_backend_descriptions_consistency()`
module-level check ensuring descriptions stay in sync with `SUPPORTED_BACKENDS`.
Exported `get_backend_description()` public API. Enriched `list_backends()` in
`cli/main.py` to display descriptions alongside availability and enabled status.

**10 new tests (8 factory + 2 CLI). 6979 total tests. 76.55% coverage.**

---

## Coverage Accuracy: Dynamic Server Omit & Pragma Cleanup (v376)

Default `uv run pytest` now reports **99.93% coverage** (was 76.55%). Three
heavy server modules (`app.py`, `celery_app.py`, `schedules.py`) that require
the `[server]` extra are omitted from coverage when that extra is absent, via
`.coveragerc-no-server` config. CI overrides with `--cov-config=pyproject.toml`
to cover everything. Marked 6 unreachable / optional-dep lines with
`pragma: no cover`. Added `exclude_lines` patterns. Updated fixture-usage
test to skip pytest hooks.

**18 new tests. 6997 total tests. 99.93% coverage.**

---

## ARCHITECTURE.md Refresh & Exec-Plan Index (v379)

Updated stale ARCHITECTURE.md (last updated 2026-03-07) to reflect ~30 days
of new modules and features. Added 7 structural accuracy tests verifying all
listed file paths exist on disk and all source modules are mentioned. Created
`docs/exec-plans/index.md` for plan navigation.

**7 new tests.**

---

## Registry Coverage Completion & Runner Test Hardening (v380)

Closed the last registry branch partial (line 560→558 in
`format_tool_instructions_for_cli` — tool with no `_CLI_TOOL_GUIDANCE` entry).
Added dedicated `TestParseRequiredStr` class with 9 tests covering valid input,
missing key, None, empty string, whitespace, int, bool, and list rejection.
Added custom-params forwarding tests for `_run_python_script` (1 test) and
`_run_bash_script` (2 tests: script_path and inline_script variants).

**13 new tests. 7038 total tests. 99.93% coverage. Registry: 100%.**

---

## Boundary Input Validation Hardening (v381)

Hardened input validation at three module boundaries:
1. `resolve_hand_model()` — replaced bare `PROVIDERS[key]` dict access with
   `.get()` + `RuntimeError` at both the default-provider (line 122) and
   inferred-provider (line 146) paths.
2. `run_git_clone()` — added `require_positive_int(depth, "depth")` to reject
   zero, negative, and boolean depth values before spawning `git clone`.
3. `RepoIndex.from_path()` — added `isinstance(path, Path)` type guard to
   raise `TypeError` on non-Path input instead of `AttributeError`.

**10 new tests. 7277+ total tests.**

---

## Docstring Completion & CLI Introspection Design Doc (v383)

Added Google-style docstrings to 4 public items: `BasicLangGraphHand.__init__()`,
`BasicAtomicHand.__init__()`, `ArcadeScoreEntry`, `ArcadeScoreSubmit`. Created
`docs/design-docs/cli-introspection.md` consolidating design decisions for
`--version`, `--list-backends`, `--list-tools`, `doctor`, interactive mode,
and first-run banner. Updated design-docs index and docs/index.md parenthetical.

**17 new tests. 7066 total tests. 99.94% coverage.**

---

## Provider API Key Map Consistency (v384)

Added missing `litellm` entry to `PROVIDER_API_KEY_ENV` in `model_provider.py`.
CLI hands (goose, opencode) that check auth status via
`PROVIDER_API_KEY_ENV.get(provider)` previously returned `None` for litellm
models. Updated docstring to clarify Ollama maps to `OLLAMA_HOST` (host
indicator, not API key). Added structural consistency tests ensuring
`PROVIDER_API_KEY_ENV` keys stay in sync with the `PROVIDERS` registry.

**4 new tests. 7069 total tests. 99.94% coverage.**

---

## Frontend Component Test Coverage & Backend Docstring Polish (v385)

Added test suites for three previously-untested frontend components:
`DiffView.tsx` (0.56% → 100%), `FileExplorer.tsx` (18.5% → 98.45%),
and `useOnboarding.ts` (73.82% → 97.31%). Expanded `Config.from_env()`
docstring with Args/Returns/Raises sections.

**66 new frontend tests. 753 total frontend tests.**
