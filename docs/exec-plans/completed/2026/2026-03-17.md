# 2026-03-17 Daily Consolidation (v247–v270)

Twenty-three execution plans completed on 2026-03-17, continuing the DRY refactoring,
constant extraction, import consolidation, and exception narrowing campaign.

## Summary

| Plan | Theme | New Tests | Total Passed |
|---|---|---|---|
| v270 | `has_cli_flag()`, `install_hint()`, `_check_optional_dep()` DRY helpers | 20 | 6254 |
| v269 | `_EMPTY_MODEL_MARKERS` constant + `parse_comma_list()` helper | 17 | 6254 |
| v268 | DRY label prefix in emitter/sandbox, `_error_exit()`, `_make_temp_clone_dir()` | 17 | 6237 |
| v267 | `_label_msg()` helper to DRY 32 CLI label prefix patterns | 7 | 6220 |
| v266 | `_require_langchain_class()` helper in model_provider.py | 11 | 6217 |
| v247 | Provider name constants in model_provider.py and goose.py | 24 | 5855 |
| v248 | Git-not-found constants, narrow run_async exceptions | 22 | 5877 |
| v249 | Deduplicate env var constants between github_url.py and base.py | 16 | 5893 |
| v250 | Hand factory function + backend name constants | 22 | 5912 |
| v251 | `_GITHUB_ERRORS` constant + Celery inspect exception narrowing | 18 | 5930 |
| v252 | Backend lookup constants + factory coverage | 12 | 5940 |
| v253 | Simplify schedule `getattr()` to direct access | 14 | 5951 |
| v254 | Simplify remaining `getattr()` to direct access | 10 | 5960 |
| v255 | Consolidate imports, extract magic numbers | 12 | 5972 |
| v256 | DRY `_env_var_status()` for CLI auth descriptions | 16 | 5988 |
| v257 | Centralize `DEFAULT_MAX_ITERATIONS` constant | 14 | 6002 |
| v258 | DRY env var parsing in `pr_description.py` | 21 | 6023 |
| v259 | DRY `_repo_tmp_dir`, `resolve_github_token`, truthy values | 32 | 6055 |
| v260 | Unify `_TRUTHY_VALUES` with `"on"`, env var helpers | 28 | 6083 |
| v261 | `REPO_SPEC_PATTERN`, `invalid_repo_msg`, `format_type_error` | 29 | 6110 |
| v262 | `_OUTPUT_FORMAT_STREAM_JSON`, `_normalize_preview`, `_extract_message_blocks` | 23 | 6133 |
| v263 | Extract `_run_git_diff()` helper in `pr_description.py` | 17 | 6151 |
| v264 | Shared validation, exception tuple constants, env var constants | 33 | 6184 |
| v265 | `_require_sdk()` import guard, `PROVIDER_API_KEY_ENV` constant | 22 | 6206 |

**Total new tests across v247–v270:** ~446
**Final test count:** 6254 passed, 273 skipped

## v247–v270 Themes

- **Provider name constants** — 20 bare provider name strings → 5 `_PROVIDER_*` constants
  in `model_provider.py` and `goose.py` (v247)
- **Git-not-found constants** — 6 bare debug strings → 3 named constants in
  `pr_description.py` (v248)
- **Env var deduplication** — `_ENV_GIT_TERMINAL_PROMPT`/`_ENV_GCM_INTERACTIVE`
  duplicated across `github_url.py` and `base.py` → canonical public exports (v249)
- **Hand factory extraction** — `_hand_from_backend()` factory function + 10 backend
  name constants in `factory.py` (v250)
- **GitHub error constants** — `_GITHUB_ERRORS` tuple + Celery `inspect` exception
  narrowing (v251)
- **Backend lookup constants** — `_BACKEND_LOOKUP` dict + factory coverage (v252)
- **Direct attribute access** — Replaced 14 defensive `getattr()` calls with direct
  attribute access across `celery_app.py`, `app.py`, `web.py`, `base.py` (v253–v254)
- **Import consolidation** — `combine-as-imports = true` in ruff isort, reducing
  24 → 3 statements in `celery_app.py` and 25 → 1 in `app.py` (v255)
- **DRY env var status** — `_env_var_status()` static method on `_TwoPhaseCLIHand`,
  replacing 5 inline `os.environ.get()` patterns (v256)
- **Centralize max iterations** — `DEFAULT_MAX_ITERATIONS` constant defined once in
  `iterative.py`, re-exported from `server/constants.py` (v257)
- **DRY env var parsing** — `_parse_positive_env_var()` helper in `pr_description.py`
  replacing duplicated parsing logic (v258)
- **DRY repo tmp + token resolution** — `repo_tmp_dir()` and `resolve_github_token()`
  shared helpers in `github_url.py`, unified `_PR_TRUTHY_VALUES` in
  `pr_description.py` (v259)
- **Unified truthy values** — Added `"on"` to base `_TRUTHY_VALUES`, removed
  `_PR_TRUTHY_VALUES` and `_CLI_TRUTHY_VALUES` duplicates, updated
  `_is_truthy_env()` to strip whitespace, added `_get_env_stripped()` helper,
  simplified 3 consumers to use `_is_truthy_env()` (v260)
- **Repo spec pattern + validation helpers** — `REPO_SPEC_PATTERN` constant
  (3 duplicates → 1), `invalid_repo_msg()` (2 duplicates → 1), `format_type_error()`
  (3 similar patterns → 1 helper) (v261)
- **Stream parser DRY** — `_OUTPUT_FORMAT_STREAM_JSON` constant (2 bare strings → 1),
  `_normalize_preview()` helper (2 duplicate `strip().replace()` → 1),
  `_extract_message_blocks()` helper (2 duplicate message extraction → 1) (v262)
- **DRY git diff helper** — `_run_git_diff()` extracted to deduplicate 3
  `subprocess.run(["git", "diff", ...])` + exception handling blocks in
  `pr_description.py` (v263)
- **Shared validation + exception constants + env var constants** —
  `_validate_full_name` delegates to `validate_repo_spec` (dedup owner/repo
  validation), `_TOOL_EXECUTION_ERRORS` (7-type tuple) and `_RUN_ASYNC_ERRORS`
  (5-type tuple shared by `iterative.py` and `atomic.py`), 9 `_ENV_*` constants
  in `config.py` replacing bare strings (v264)
- **Provider import guard + API key env map** — `_require_sdk()` method on
  `AIProvider` base class (dedup 5 identical ImportError blocks across all
  providers), `PROVIDER_API_KEY_ENV` constant in `model_provider.py` (dedup 2
  identical provider-to-env-var dicts in `goose.py` and `opencode.py`) (v265)
- **Langchain import helper** — `_require_langchain_class()` helper extracts
  3 identical `try/except ModuleNotFoundError` blocks in
  `build_langchain_chat_model()` into a single reusable function (v266)
- **DRY CLI label prefix** — `_label_msg()` helper on `_TwoPhaseCLIHand` replaces
  32 inline `f"[{self._CLI_LABEL}] ..."` patterns with a single method call (v267)
- **DRY label/error/clone helpers** — `_label_msg()` added to `_StreamJsonEmitter`
  (4 inline patterns), 5 Docker sandbox label patterns migrated to `_label_msg()`,
  `_error_exit()` replaces 5 inline error+exit blocks in `cli/main.py`,
  `_make_temp_clone_dir()` replaces 2 inline mkdtemp+atexit blocks (v268)
- **Empty model markers + comma list parser** — `_EMPTY_MODEL_MARKERS` constant
  (2 bare `("default", "None")` tuples → 1 constant in `cli/base.py`, imported by
  `opencode.py`), `parse_comma_list()` helper in `validation.py` (2 inline
  `r.strip() for r in s.split(",")` patterns → 1 reusable function) (v269)
- **CLI flag + install hint + dep check DRY** — `has_cli_flag()` replaces 5 inline
  `token == "--flag" or token.startswith("--flag=")` patterns across all CLI hands,
  `install_hint()` replaces 5 inline `f"Install with: uv sync --extra {extra}"`
  strings, `_check_optional_dep()` collapses 2 near-identical dependency guard
  functions in `schedules.py` (v270)

## Individual plan files

- `v247-provider-name-constants.md`
- `v248-git-not-found-constants-narrow-exceptions.md`
- `v249-deduplicate-env-var-constants.md`
- `v250-hand-factory-backend-constants.md`
- `v251-github-errors-constant-celery-exception-narrowing.md`
- `v252-backend-lookup-constants-factory-coverage.md`
- `v253-simplify-schedule-getattr-to-direct-access.md`
- `v254-remaining-getattr-simplification.md`
- `v255-consolidate-imports-magic-numbers-factory-tests.md`
- `v256-dry-env-var-status-describe-auth.md`
- `v257-centralize-default-max-iterations.md`
- `v258-dry-env-var-parsing-pr-description.md`
- `v259-dry-repo-tmp-token-resolution-truthy.md`
- `v260-unify-truthy-values-env-helpers.md`
- `v261-repo-spec-pattern-validation-type-error-helper.md`
- `v262-stream-json-constant-normalize-preview-extract-blocks.md`
- `v263-dry-run-git-diff-helper.md`
- `v264-validation-dedup-exception-constants-env-vars.md`
- `v265-provider-import-guard-api-key-map.md`
- `v266-langchain-import-helper.md`
- `v267-dry-cli-label-prefix.md`
- `v268-dry-label-error-exit-temp-clone.md`
- `v269-empty-model-markers-parse-comma-list.md`
- `v270-has-cli-flag-install-hint-check-dep.md`
