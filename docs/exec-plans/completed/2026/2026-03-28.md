# 2026-03-28 Daily Consolidation (v325–v332)

Eight execution plans completed on 2026-03-28: GitHub issue linking, issue
creation from tasks, issue lifecycle sync (labels + status comments),
task status sync to GitHub issues, GitHub Projects board integration,
integration test coverage hardening, server module test coverage hardening,
and validation/task result coverage hardening.

## Summary

| Plan | Theme | New Tests | Total Passed |
|---|---|---|---|
| v332 | Validation & task result coverage hardening | 20 | 6459 backend |
| v331 | Server test coverage hardening | 10 | — |
| v325 | GitHub issue linking | 14 | 724 (frontend), 92 (backend github) |
| v326 | Create new issue from task | 13 | 728 (frontend), 97 (backend github) |
| v327 | Sync task status with GitHub issue | 15 | 732 (frontend), 104 (backend github) |
| v328 | Sync task status with issue | 5 | 7501 backend, 127 celery |
| v329 | GitHub Projects board integration | 15 | 7516 backend |
| v330 | GitHub integration test coverage | 5 | 6439 backend |

## v325 — GitHub Issue Linking

**Full-stack issue linking:** Added `issue_number` field through the entire stack
(frontend FormState → SubmissionForm → useTaskManager → BuildRequest → Celery task →
Hand base class). When a user provides an issue number:
- PR body includes "Closes #N" for auto-close on merge
- Comment posted on the issue linking to the created/updated PR
- Error handling: comment failures logged but don't block PR creation

**GitHubClient:** `get_issue()` and `create_issue_comment()` methods added.

**Frontend:** Issue number field in Advanced settings, URL query param support
(`?issue_number=42`).

**Tests:** 10 new backend tests (4 GitHubClient, 3 _post_issue_link_comment,
2 issue_number attribute, 1 create_issue_comment validation). 4 new frontend
tests (2 SubmissionForm, 2 useTaskManager). 724 frontend tests total, 92 backend
GitHub tests.

## v326 — Create New Issue from Task

**Auto-create issue from task:** Added `GitHubClient.create_issue()` method for
creating issues via PyGithub API. New `create_issue` boolean field through the
full stack (frontend checkbox → BuildRequest → Celery task). When enabled and
no `issue_number` is manually provided, `_try_create_issue()` helper auto-creates
a GitHub issue with `[helping-hands]` prefixed title (first 120 chars of first
prompt line) and full prompt as body, applying the `helping-hands` label.

The created issue number flows into the existing `issue_number` pipeline — PR
gets "Closes #N" and a comment is posted on the issue linking to the PR.

**Error handling:** Issue creation failures are logged and reported in task
updates but do not block the build from proceeding.

**Frontend:** "Create issue" checkbox in Advanced settings, URL query param
support (`?create_issue=true`).

**Tests:** 5 new GitHubClient tests (create with title/body, with labels, null
body, empty title, whitespace title). 4 new Celery `_try_create_issue` tests
(success, title truncation, multiline prompt, error swallowing). 4 new frontend
tests (2 SubmissionForm checkbox, 2 useTaskManager submit body). 728 frontend
tests total (up from 724), 97 backend GitHub tests (up from 92).

## v327 — Sync Task Status with GitHub Issue

**Issue lifecycle sync:** When a task has a linked `issue_number`, GitHub issue
labels and comments are updated to reflect task progress:
- `_sync_issue_started()` adds `helping-hands:in-progress` label when the hand begins
- `_sync_issue_completed()` posts completion comment with PR link + runtime,
  swaps label to `helping-hands:completed`, removes `in-progress`
- `_sync_issue_failed()` posts failure comment with error details, swaps label
  to `helping-hands:failed`, removes `in-progress`

**GitHubClient:** `add_issue_labels()` (creates missing labels on repo) and
`remove_issue_label()` (silently ignores absent labels) methods added.

**Progress metadata:** `issue_number` included in Celery `_update_progress()`
metadata so frontend can track it during polling.

**Frontend:** `linkedIssueNumber` computed in `useTaskManager` hook from payload.
MonitorCard shows blue `#N` issue badge in header when a linked issue is present.
`taskInputs` includes `Issue: #N` item.

**Tests:** 7 new backend GitHubClient tests (3 add_issue_labels, 4 remove_issue_label).
8 new celery sync helper tests (2 _sync_issue_started, 3 _sync_issue_completed,
3 _sync_issue_failed). 4 new frontend tests (2 MonitorCard badge, 2 useTaskManager
linkedIssueNumber). 732 frontend tests total (up from 728), 104 backend GitHub
tests (up from 97).

## v328 — Sync Task Status with GitHub Issue

**Issue status lifecycle sync:** When a task has a linked GitHub issue, the
`_sync_issue_status()` helper posts or updates a single marker-tagged comment
on the issue as the task progresses:
- 🔄 "running" — posted when hand starts execution
- ✅ "completed" — posted after success, includes PR URL
- ❌ "failed" — posted on exception, includes truncated error (≤200 chars)

Uses `upsert_pr_comment()` with `<!-- helping_hands:issue_status -->` marker
for idempotent updates. Best-effort: sync failures are logged but never block
the build.

**Tests:** 5 new tests in `TestSyncIssueStatus` (noop for None, running body,
completed with PR URL, failed with error, exception swallowing). 127 celery
tests total (up from 122), 7501 backend tests.

## v329 — GitHub Projects Board Integration

**Full-stack project integration:** Added `project_url` field through the entire
stack (frontend FormState → SubmissionForm → useTaskManager → BuildRequest →
Celery task). When a user provides a GitHub Project URL, the linked issue is
automatically added to the project after creation/linking.

**GitHubClient:** `add_to_project_v2()` method using GitHub GraphQL API
(`addProjectV2ItemById` mutation). `parse_project_url()` static method parses
org/user project URLs. `_graphql()` private helper for GraphQL calls via
`urllib.request`.

**Celery:** `_try_add_to_project()` best-effort helper — errors logged but never
block the build. Called after issue creation/linking, before hand execution.

**Frontend:** Project URL input in Advanced settings, URL query param support
(`?project_url=...`).

**Tests:** 11 new backend tests (6 TestParseProjectUrl, 5 TestAddToProjectV2,
4 TestTryAddToProject, 5 TestBuildForm updated). 4 new frontend tests
(2 SubmissionForm, 2 useTaskManager). 7516 backend tests total.

## v330 — GitHub Integration Test Coverage

**Integration test hardening:** Filled coverage gaps in the v325–v329 GitHub
integration feature stack at the form endpoint, PR body generation, and
Celery helper edge case layers.

**TestBuildForm:** 2 new tests — `test_enqueues_with_issue_number` verifies
`issue_number=42` is parsed as int, `test_enqueues_with_create_issue_and_project_url`
verifies `create_issue=on` and `project_url` are forwarded correctly.

**TestCreateNewPrIssueClose:** 2 new tests verifying `_create_new_pr()` appends
`\n\nCloses #42` to PR body when `issue_number` is set, and omits it when `None`.

**TestTryAddToProject:** 1 new test — `test_invalid_project_url_does_not_propagate`
verifies malformed project URLs are swallowed gracefully.

**Tests:** 5 new tests. 6439 backend tests total (up from 6433), 0 failures.

## v331 — Server Module Test Coverage Hardening

**Targeted untested branches** in `celery_app.py` and `schedules.py` server modules:

**`_ProgressEmitter` direct unit tests:** 4 tests verifying `emit()` delegates to
`_update_progress` with stored defaults, supports per-call overrides, preserves
non-overridden fields (including `fix_ci`, `reference_repos`, `started_at`), and
handles sequential multi-call usage.

**`_resolve_repo_path` TimeoutExpired:** 1 test mocking `subprocess.run` to raise
`TimeoutExpired`, verifying `ValueError` with timeout message and `shutil.rmtree`
cleanup of the temp directory.

**`_setup_periodic_tasks` signal handler:** 1 test verifying the `on_after_finalize`
signal handler delegates to `ensure_usage_schedule()`.

**`_load_meta` corrupted data:** 4 tests in `test_schedule_manager.py` verifying that
`_load_meta` returns `None` for invalid JSON, logs a warning, returns `None` for
missing required fields, and returns `None` for empty required fields.

**Tests:** 10 new tests (6 in `test_celery_app.py`, 4 in `test_schedule_manager.py`),
all passing.

## v332 — Validation & Task Result Coverage Hardening

**Type-validation test gaps:** Added 15 new validation tests covering
`require_non_empty_string` TypeError branches (int, None, bool, list),
`require_positive_int` TypeError branches (bool True/False, float, string, None),
and `format_type_error` direct unit tests (param name, expected type, actual type,
None, dict, custom class).

**Task result edge cases:** Added 5 `normalize_task_result` tests covering empty
status, whitespace status, None status, int status, and non-serializable object
fallback paths.

**Tests:** 20 new tests (15 validation, 5 task_result). 6459 backend tests passed,
0 failures, 78.28% coverage.
