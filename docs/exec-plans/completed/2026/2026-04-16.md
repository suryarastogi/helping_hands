# Execution Plan: Coverage Hardening — github.py, hand/base.py, cli/base.py

**Date:** 2026-04-16
**Goal:** Raise coverage of the three sub-95% modules to ≥95%.

## Results

Total backend coverage: 95.88% → **98.76%**. 46 new tests. 5354 total tests pass.

| File | Before | After |
|---|---|---|
| `lib/github.py` | 85% | 100% |
| `hands/v1/hand/base.py` | 92% | 96% |
| `hands/v1/hand/cli/base.py` | 88% | 99% |

## Tasks

### 1. github.py — `list_issues`, `list_issues_excluding_labels`, `list_prs_with_label`

- [x] `list_issues`: PR filtering, field mapping, user=None, body=None (4 tests)
- [x] `list_issues_excluding_labels`: filter_labels dedup, exclude_labels, no-filter, PR exclusion, user=None (5 tests)
- [x] `list_prs_with_label`: label-not-found, PR mapping, non-PR skip, limit, empty label (5 tests)

### 2. hand/base.py — rebase/push conflict resolution paths

- [x] `_default_base_branch`: successful ref parsing, master ref, empty fallback (3 tests)
- [x] `_try_rebase_for_push`: fetch failure, clean rebase, conflict detection, non-conflict abort, timeout (5 tests)

### 3. cli/base.py — AI conflict resolution helpers

- [x] `_build_conflict_fix_prompt`: files, empty files, git output, truncation, empty output (5 tests)
- [x] `_get_conflicted_files`: normal, timeout, OSError, empty (4 tests)
- [x] `_attempt_rebase_with_conflict_fix`: fetch fail, clean, non-conflict, AI success, AI error, remaining, continue fail (7 tests)
- [x] `_ai_resolve_push_conflicts`: not-needs-ai, no-branch, no-files, AI error, remaining, continue-fail, success, push-fail (8 tests)

### 4. Documentation updates

- [x] Update INTENT.md with completion summary
- [x] Move plan to completed/

## Status

**Completed** — 2026-04-16
