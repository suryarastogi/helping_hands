# 2026-04-07 — Codex CLI Grill Tests & Doc Hygiene

Cover the newly-added Codex CLI backend in `server/grill.py` (PR #185) with
dedicated unit tests. The pure helpers `_build_codex_full_prompt` and
`_invoke_codex_turn` plus the `_CODEX_TURN_TIMEOUT_S` constant have zero test
coverage today.

## Tasks

- [x] Write tests for `_build_codex_full_prompt` (empty history, with history)
- [x] Write tests for `_invoke_codex_turn` (success, model override, FileNotFoundError, timeout, non-zero exit, on_status callback, sandbox env override)
- [x] Write test for `_CODEX_TURN_TIMEOUT_S` constant
- [x] Verify all new tests pass (`uv run pytest tests/test_grill_codex.py -v`)
- [x] Update INTENT.md with completion entry
- [x] Update PLANS.md with plan reference
- [x] Update exec-plans index

## Rationale

PR #185 added the Codex CLI backend to Grill Me but shipped without tests for
the two new helper functions. These are pure/subprocess functions that are
straightforward to test in isolation, matching the existing pattern in
`test_grill.py`.
