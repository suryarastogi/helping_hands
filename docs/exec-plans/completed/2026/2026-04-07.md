# 2026-04-07 — Codex CLI Grill Tests & Conflict Resolution Coverage

Two execution plans completed.

---

## Codex CLI Grill Tests & Doc Hygiene

Cover the newly-added Codex CLI backend in `server/grill.py` (PR #185) with
dedicated unit tests. The pure helpers `_build_codex_full_prompt` and
`_invoke_codex_turn` plus the `_CODEX_TURN_TIMEOUT_S` constant had zero test
coverage.

**24 new tests.**

---

## Conflict Resolution & Rebase Coverage

Closed the largest remaining coverage gaps in `cli/base.py` (88% → 99%) and
`hand/base.py` (92% → 100%) by testing the conflict resolution, rebase, and
push-retry code paths:

- **`_build_conflict_fix_prompt`** (6 tests): single file, multiple files with
  output, empty file list, truncation of long output, whitespace-only output
  exclusion, conflict markers presence
- **`_get_conflicted_files`** (5 tests): success, empty output, timeout,
  OSError, blank line filtering
- **`_attempt_rebase_with_conflict_fix`** (8 tests): fetch failure, fetch
  timeout, rebase success (no conflicts), non-conflict abort, AI resolution
  error, remaining conflicts after AI, rebase --continue failure, full success
- **`_ai_resolve_push_conflicts`** (10 tests): not needs_ai passthrough, no
  branch passthrough, no conflicted files abort, AI error, remaining conflicts,
  rebase --continue failure, full success without master rebase, push failure
  after resolve, master rebase success, master rebase failure still pushes
- **`_try_rebase_for_push`** (5 tests): fetch failure, fetch timeout, rebase
  success, conflicts left for AI, non-conflict abort
- **`_default_base_branch`** (3 tests): returns master from symbolic-ref,
  returns main, empty ref falls back to default
- **`_push_to_existing_pr` master_rebase** (2 tests): rebase success updates
  SHA, rebase failure aborts and continues
- **`_push_to_existing_pr` fix_conflicts** (3 tests): rebase ok + push ok,
  rebase ok + push still fails (diverged branch), rebase fail → needs_ai

**42 new tests. 5374 total tests pass. Coverage: 97.18% → 99.68%.**
