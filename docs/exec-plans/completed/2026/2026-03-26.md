# 2026-03-26 Daily Consolidation (v303–v312)

Ten execution plans completed on 2026-03-26: multiplayer coverage hardening,
schedule PR auto-persist, shared world decorations, join/leave notifications
with spawn randomization, player color customization, multiplayer hardening,
FactoryFloorPanel extraction, useTaskManager hook extraction, useSceneWorkers
hook extraction, and useServiceHealth + useClaudeUsage hook extraction.

## Summary

| Plan | Theme | New Tests | Total Passed |
|---|---|---|---|
| v303 | useMultiplayer branch coverage hardening | 2 | 466 (frontend) |
| v304 | Schedule PR auto-persist on first creation | 18 | 6321 (backend) |
| v305 | Shared world decorations via Y.Map | 16 | 482 (frontend) |
| v306 | Join/leave notifications & spawn randomization | 5 | 487 (frontend) |
| v307 | Player avatar color customization | 6 | 493 (frontend) |
| v308 | Multiplayer hardening — awareness validation & reconnection | 37 | 500 (frontend) |
| v309 | Extract FactoryFloorPanel component | 36 | 535 (frontend) |
| v310 | Extract useTaskManager hook from App.tsx | 17 | 553 (frontend) |
| v311 | Extract useSceneWorkers hook from App.tsx | 16 | 569 (frontend) |
| v312 | Extract useServiceHealth + useClaudeUsage hooks | 10 | 579 (frontend) |

## v303: Multiplayer Coverage Hardening

**Coverage improvement:** Added 2 tests for `setTyping` callback and remote
typing state tracking in `useMultiplayer` hook. Branch coverage: 81% → 82.35%.
Overall: 466 frontend tests.

## v304: Schedule PR Auto-Persist

**Auto-persist newly created PRs:** When a scheduled task with no `pr_number`
creates a new PR, the PR number is automatically saved back to the schedule so
subsequent runs push to the same PR.

**Implementation:**
- `Hand.last_pr_metadata` attribute stores finalization metadata after PR
  creation/update.
- `ScheduleManager.update_pr_number()` writes just the PR number field.
- `_maybe_persist_pr_to_schedule()` helper in `celery_app.py` bridges the
  hand result to schedule storage with guard conditions.
- `build_feature` task gains optional `schedule_id` parameter, passed from
  both `scheduled_build` and `trigger_now`.
- Non-E2E hand results now include `**hand.last_pr_metadata` for consistent
  PR metadata in task results.

**18 new tests** (4 ScheduleManager, 7 celery helper, 7 hand instantiation).

## v305: Shared World Decorations via Y.Map

**Persistent shared state:** Players can place emoji decorations in the world
scene that all connected players see in real-time. Uses Y.Map document state
(not just awareness), exercising the persistent Y.Doc capability for the first
time.

**Implementation:**
- `WorldDecoration` type, `DECORATION_EMOJIS` palette (8 emojis),
  `MAX_DECORATIONS` cap (20)
- `useMultiplayer` hook observes Y.Map `"decorations"`, exposes
  `placeDecoration(emoji, x, y)` and `clearDecorations()`
- Decoration toolbar in Factory Floor panel with emoji palette, count display,
  and clear button
- Double-click scene to place selected emoji at click position
- Pop animation and crosshair cursor during placement mode

**16 new tests** (6 hook, 10 scene).

## v306: Join/Leave Notifications & Spawn Randomization

**Multiplayer UX improvements:** System messages in chat history for player
join/leave events, and randomized spawn positions to prevent avatar overlap.

**Implementation:**
- Yjs awareness `change` event `added`/`removed` arrays drive join/leave
  system messages (`ChatMessage.isSystem` flag)
- `.chat-history-system` CSS class: italic, muted colour, hidden player name
- `randomSpawnPosition()` utility generates positions within padded bounds
- `useMovement` initializes with `useRef`-stable random spawn position
- `SPAWN_PADDING` constant (10%) prevents edge spawns
- Mock awareness in both `useMultiplayer.test.tsx` and `App.test.tsx` updated
  to pass `{added, updated, removed}` changes object

**5 new tests** (3 hook join/leave, 1 spawn bounds, 1 scene system message).

## v307: Player Avatar Color Customization

**Player color picker:** Players can now choose their avatar color from the
10-color palette instead of being auto-assigned based on Yjs clientID. Color
persists in localStorage across sessions. Broadcast via Yjs awareness without
reconnecting.

**Implementation:**
- `loadPlayerColor()` / `savePlayerColor()` localStorage helpers
- `playerColor` prop on `UseMultiplayerOptions` with awareness broadcast effect
- `.color-picker-row` with `.color-swatch` buttons in Factory Floor panel
- CSS: hover scale, selected border glow

**6 new tests** (3 hook, 3 scene). 493 frontend tests total.

## v308: Multiplayer Hardening — Awareness Validation & Reconnection Resilience

**Defensive hardening:** Server-side awareness state validation and frontend
reconnection resilience.

**Backend:**
- `validate_awareness_state()` — clamps positions [0,100], coerces types,
  truncates names/chat, strips control chars, validates direction enum
- `get_player_activity_summary()` — active/idle breakdown with validated states
- `GET /health/multiplayer/activity` endpoint
- `get_connected_players()` now uses validation pipeline

**Frontend:**
- Reconnection tracking with `MAX_RECONNECT_ATTEMPTS` (10) threshold
- "failed" terminal `ConnectionStatus` state after max reconnect attempts
- "Connection failed" banner with red overlay
- `reconnectAttempts` exposed in hook return

**30 new backend tests, 7 new frontend tests.** 500 frontend tests total.

## v309: Extract FactoryFloorPanel Component

Extracted `FactoryFloorPanel` component (~180 lines) from HandWorldScene.
Contains player name/color customization, presence panel, connection status,
emote picker, chat input/history, and decoration toolbar.

- HandWorldScene.tsx: 618 → 437 lines (-181 lines)
- 36 new tests in `FactoryFloorPanel.test.tsx`
- 535 frontend tests total (up from ~500)

## v310: Extract useTaskManager Hook

**Major App.tsx decomposition:** Extracted task submission, polling, history
management, output tab, floating numbers, toasts, and all derived task state
into a dedicated `useTaskManager` hook.

**Implementation:**
- New `frontend/src/hooks/useTaskManager.ts` (~500 lines)
- App.tsx reduced from 1,374 to 538 lines (-836 lines, -61%)
- Moved: `taskId`, `status`, `payload`, `updates`, `isPolling`, `taskHistory`,
  `floatingNumbers`, `toasts`, `form`, `mainView`, `showSubmissionOverlay`,
  `outputTab`, `prefixFilters`, `accUsage`, `monitorHeight`, `fetchedCapacity`
- Moved callbacks: `submitRun`, `selectTask`, `openSubmissionView`, `updateField`,
  `removeToast`, `handleMonitorScroll`, `handleResizeStart`
- Moved derived state: `selectedTask`, `activeTasks`, `activeTaskIds`, `taskById`,
  `activeOutputText`, `detectedPrefixes`, `taskInputs`, `runtimeDisplay`
- Moved effects: primary task polling (3s), background tracked-tasks polling (10s),
  current-tasks discovery (10s), worker capacity polling (30s), query-string init,
  localStorage persistence, accumulated usage tracking, elapsed time timer

**17 new tests** in `useTaskManager.test.tsx`. 553 frontend tests total.

## v311: Extract useSceneWorkers Hook

**Final App.tsx decomposition:** Extracted scene worker lifecycle management
(state, refs, callbacks, memos, effects) into a dedicated `useSceneWorkers`
hook (~210 lines). Also removed 48 lines of dead re-exports that had no
consumers.

**Implementation:**
- New `frontend/src/hooks/useSceneWorkers.ts`
- `SceneWorkerEntry` type consolidated in hook; `HandWorldScene` re-exports it
- App.tsx reduced from 538 to 313 lines (-225 lines, -42%)

**16 new tests** in `useSceneWorkers.test.tsx`. 569 frontend tests total.

## v312: Extract useServiceHealth & useClaudeUsage Hooks

Extracted the two remaining polling effects from App.tsx into dedicated hooks:

- **`useServiceHealth`** — 15-second polling interval, returns `ServiceHealthState | null`
- **`useClaudeUsage`** — 1-hour polling interval + manual `refreshClaudeUsage()` callback

App.tsx reduced from 313 to 271 lines (-42 lines, -13%). Both hooks at 100%
statement and branch coverage.

**10 new tests** (4 useServiceHealth, 6 useClaudeUsage). 579 frontend tests total.
Overall branch coverage: 88.25%.

## Individual plan files

- `v303-multiplayer-coverage-hardening.md`
- `v304-schedule-pr-auto-persist.md`
- `v305-shared-world-decorations.md`
- `v306-join-leave-notifications-spawn-randomization.md`
- `v307-player-color-customization.md`
- `v308-multiplayer-hardening.md`
- `v309-factory-floor-panel-extraction.md`
- `v310-extract-use-task-manager.md`
- `v311-extract-use-scene-workers.md`
- `v312-extract-polling-hooks.md`
