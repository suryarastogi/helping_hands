# Consolidated Plans: 2026-03-06

Plans completed on 2026-03-06, consolidated from individual plan files (v32-v79).

---

## v32 - PR description edge cases
pr_description.py edge case tests (`_diff_char_limit` negative, `_get_diff` empty stdout, `_build_prompt`/`_build_commit_message_prompt` truncation, `_parse_output` whitespace body, `_commit_message_from_prompt` whitespace); DESIGN.md PR description pattern; 1263 tests.

## v33 - Schedule module edge cases
Schedule module tests (`trigger_now` happy/missing/param, `get_schedule_manager`, `create_schedule`/`update_schedule` enabled/disabled, `_create_redbeat_entry` invalid cron, `_delete_redbeat_entry` KeyError, `list_schedules` None filtering, `ScheduledTask` roundtrip); DESIGN.md scheduling pattern; schedules.py 80% -> 93%; 1473 tests.

## v34 - E2E hand run and placeholders
E2EHand.run() unit tests (dry-run/fresh-PR/resumed-PR/empty-repo/base-branch/auto-uuid), E2EHand.stream() yield, placeholders.py backward-compat shim tests; e2e.py 25% -> 98%; 1278 tests.

## v35 - Server health checks and celery auth
Server health check tests (`_check_redis_health`, `_check_db_health`, `_check_workers_health`, `_is_running_in_docker`, `_iter_worker_task_entries`, `_safe_inspect_call`); celery_app `_has_codex_auth` tests; DESIGN.md health check patterns; 1514 tests.

## v36 - GitHub client and hand finalization
GitHub client edge cases (`fetch_branch`, `pull`, `set_local_identity`, `get_check_runs` mixed conclusion, `upsert_pr_comment`); Hand base.py edge cases (`_push_to_existing_pr` whoami exception, `_finalize_repo_pr` precommit/default_branch); DESIGN.md GitHub client patterns; 1290 tests.

## v37 - Package re-export tests
Package re-export tests for `hands/v1/hand/__init__.py` and `ai_providers/__init__.py`; fix stale obsidian reference in ARCHITECTURE.md; 1319 tests.

## v38 - CLI/meta package re-exports
Package re-export tests for `cli/__init__.py`, `meta/tools/__init__.py`, `meta/__init__.py`, `hands/v1/__init__.py`; meta tools layer pattern in DESIGN.md; 1365 tests.

## v39 - CLI subprocess errors and atomic hand
CLI `_invoke_cli_with_cmd` subprocess error paths (FileNotFoundError, npx retry, stdout None, non-zero exit, idle timeout, verbose); `BasicAtomicHand.run()` interrupted/max_iterations; ARCHITECTURE.md task result/skill catalog sections; 1378 tests.

## v40 - Docker sandbox, two-phase lifecycle
DockerSandboxClaudeCodeHand `_invoke_claude`/`_run_two_phase` tests; CLI base `_invoke_backend`/`_run_two_phase` skill catalog lifecycle; BasicAtomicHand.stream() delta paths; DESIGN.md two-phase/IO loop patterns; 1391 tests.

## v41 - CLI main coverage gaps
`__version__` test; cli/main.py gaps (docker-sandbox-claude, `_stream_hand`, Python <3.12 atomic error, generic exception re-raise); SECURITY.md Docker sandbox section; 1397 tests.

## v42 - Iterative tree snapshot and LangGraph
`_build_tree_snapshot` edge cases; `BasicLangGraphHand.run()` max_iterations; LangGraph/Atomic `stream()` pr_url at max iterations; `_effective_container_env_names` empty-blocked; iterative.py 94% -> 96%; 1404 tests.

## v43 - Celery usage monitoring
celery_app.py `_get_db_url_writer`, `ensure_usage_schedule`, `log_claude_usage` (keychain/token/HTTP/DB/JWT/garbage); ARCHITECTURE.md usage monitoring; celery_app.py 73% -> 98%; 1653 tests.

## v44 - CLI stream and PR description
cli/base.py `stream()` CI fix/pr_status paths; base.py `_finalize_repo_pr` native git auth and rich PR description; cli/base.py `_run_two_phase_inner` verbose; base.py 98% -> 99%; cli/base.py 97% -> 98%; 1413 tests.

## v45 - LangGraph/Atomic stream branches
LangGraph/Atomic `stream()` pr_status elif branches; LangGraph interrupted inner loop, non-chat-model event skip, empty text skip; Atomic duplicate delta/empty delta skip, chat_message falsy branches; iterative.py 96% -> 97%; atomic.py 93% -> 97%; 1430 tests.

## v46 - CLI delegation and exception paths
Codex/OpenCode `_invoke_*`/`_invoke_backend` delegation; CLI base `_invoke_cli` delegation and `stream()` producer error re-raise; Atomic non-AssertionError exception re-raise; skills `_discover_catalog` missing dir; 1440 tests.

## v47 - Frontend localStorage and dead code docs
Fix frontend localStorage.clear jsdom failures; frontend tests for `apiUrl`, `isTerminalTaskStatus`, `parseError`; document dead code (iterative.py, codex.py) in tech-debt-tracker; 20 frontend tests.

## v48 - CLI backend delegation
Claude `_skip_permissions_enabled` geteuid exception, `_build_failure_message`, `_StreamJsonEmitter` empty-line; Gemini `_build_failure_message`/`_invoke_gemini` delegation; Goose `_resolve_goose_provider_model_from_config` (7 tests); 1456 tests.

## v49 - MCP main and CLI IO loop
MCP `main()` stdio/http transport tests (97% -> 98%); CLI base IO loop interrupt/process-exited breaks; `run()` CI-fix noop-emit; PRODUCT_SENSE.md updates; 1461 tests.

## v50 - Frontend utility and component tests
Export/test 11 pure utility functions (55 tests); component render tests (8 tests); install @testing-library/dom; 83 frontend tests; 1461 backend tests.

## v51 - Iterative edge cases and CLI stream
iterative.py `_build_tree_snapshot` slash/multi-slash edge cases; cli/base.py `stream()` producer_task cancellation; confirmed dead code in tech-debt-tracker; 1464 tests.

## v52 - Frontend interaction tests
Frontend utility edge cases (loadTaskHistory, upsertTaskHistory, statusTone, cronFrequency); component interaction tests (view toggles, navigation, form inputs); 110 frontend tests; 1464 backend tests.

## v53 - Branch coverage gap closure
base.py `_run_precommit_checks_and_fixes` stdout-only; `_finalize_repo_pr` pr_number/default_branch; registry.py validators; web.py RelatedTopics; goose.py dead code documented; registry.py 98% -> 99%; web.py 98% -> 99%; 1470 tests.

## v54 - Frontend component coverage
Form submission, monitor view, schedule view component tests; mockResponse helper with clone(); frontend 54% -> 71.5% statements; 134 frontend tests; 1470 backend tests.

## v55 - Frontend 80%+ coverage
Schedule CRUD, task discovery, polling, notification/toast UI, monitor resize/scroll; frontend 71.5% -> 82.3% statements, 80.2% branches; 153 frontend tests.

## v56 - Iterative _build_agent coverage
iterative.py `_build_agent` for BasicLangGraphHand/BasicAtomicHand via mocked imports; docs/index.md documentation map; TODO.md frontend target marked done; iterative.py 97% -> 98%+; 1472 tests.

## v57 - Atomic exception propagation
atomic.py `stream()` non-AssertionError exception propagation; DESIGN.md skill catalog pattern; RELIABILITY.md Docker sandbox/async compatibility; atomic.py 97% -> 98%+; 1473 tests.

## v58 - Claude emitter and skills
Claude `_StreamJsonEmitter` unknown content block/whitespace preview; `_skip_permissions_enabled` geteuid-not-callable; skills whitespace-only token test; fix stale server docstring; DESIGN.md error recovery patterns; claude.py 99%+.

## v59 - E2E current_branch detection
e2e.py `current_branch` falsy detection path; e2e.py `final_pr_number is None` dead code documented; DESIGN.md testing patterns; 1478 tests.

## v60 - Final branch gap closure
model_provider.py unrecognized provider/model (100%); docker_sandbox_claude.py verbose=False/sandbox-in-base (100%); skills no-heading discovery (100%); registry.py empty inner token (100%); cli/main.py and cli/base.py gaps documented; 1485 tests.

## v61 - Dead code docs and testing methodology
Document web.py latin-1 fallback and mcp_server.py entry guard dead code; testing methodology design doc; remaining coverage gaps table in QUALITY_SCORE.md; 1485 tests.

## v62 - Plan consolidation and shared fixtures
Consolidate 2026-03-04 (v1-v4) and 2026-03-05 (v5-v31) into date-based files; add tests/conftest.py with `repo_index` and `fake_config` fixtures; refactor test_hand.py and test_hand_base_statics.py; 1485 tests.

## v63 - CLI hand fixture refactor and consolidation
Consolidate v32-v62 into `2026-03-06.md` (31 files -> 1); add `make_cli_hand` factory fixture to conftest.py; refactor 5 CLI hand test files to use shared fixture; 1485 tests.

## v64 - Goose fixture refactor and error-handling doc
Refactor test_cli_hand_goose.py to shared `make_cli_hand` fixture; add conftest fixture self-tests (10 tests); update AGENTS.md with Docker Sandbox and scheduled agents; add error-handling design doc; 1495 tests.

## v65 - Web dataclass tests and MCP architecture doc
Web dataclass construction and `_decode_bytes` edge case tests (19 tests); MCP architecture design doc; updated testing methodology stats; 1514 tests.

## v66 - Config monkeypatch refactor
Refactor test_config.py to monkeypatch; add Config frozen/dotenv/verbose tests (5 new); add config-loading design doc; 1519 tests.

## v67 - Repo indexing doc and package init tests
Add repo-indexing design doc; shared `mock_github_client` conftest fixture; top-level package import/docstring tests (21 new); 1540 tests.

## v68 - Scheduling system doc and conftest self-tests
Add scheduling-system design doc; `make_fake_module` conftest fixture; conftest self-tests (5 new); 1545 tests.

## v69 - Deployment modes doc and Hand smoke tests
Consolidate v63-v68 into 2026-03-06.md; deployment-modes design doc; mock_github_client fixture self-tests (11 new); Hand instantiation smoke tests (27 new); 1583 tests.

## v70 - CI pipeline doc and docs validation
CI pipeline design doc; docs structure validation tests (index completeness, exec-plans tracking; 4 new); 1587 tests.

## v71 - Skills system doc and validation
Skills system design doc; extended validation tests (product-specs index, root-level docs, reference files, tech-debt-tracker module refs; 6 new); 1595 tests.

## v72 - GitHub client doc and API validation
GitHub client design doc; extended validation tests (API docs link validity, completed plan structure, design-docs index count; 7 new); 1602 tests.

## v73 - PR description doc and structural tests
PR description design doc; extended validation tests (ARCHITECTURE.md key paths, AGENTS.md sections, docs/index.md link resolution, CLAUDE.md sections; 17 new); 1619 tests.

## v74 - Default prompts doc and doc section validation
Default prompts design doc; extended validation tests (DESIGN.md sections, SECURITY.md sections, RELIABILITY.md sections, README.md sections, QUALITY_SCORE.md structure; 19 new); 1638 tests.

## v75 - Filesystem security doc and content checks
Filesystem security design doc; extended validation tests (FRONTEND.md sections, PRODUCT_SENSE.md sections, SECURITY.md sandboxing subsections, design doc content checks, ARCHITECTURE.md section count; 18 new); 1656 tests.

## v76 - Model resolution doc and plan link validation
Model resolution design doc; extended validation tests (PLANS.md link resolution, design-docs count sync, tech-debt-tracker structure, TODO.md structure, completed plan content; 10 new); 1666 tests.

## v77 - E2E hand workflow doc and plan consistency
E2E hand workflow design doc; extended validation tests (design doc source references, API docs completeness, PLANS.md structure, active plan consistency, key source files; 10 new); 1676 tests.

## v78 - Task lifecycle doc and extended validation
Task lifecycle design doc; extended validation tests (design-docs index descriptions, generated docs content, product-specs content, ARCHITECTURE.md module boundaries, QUALITY_SCORE.md areas for improvement; 12 new); 1688 tests.

## v79 - Web tools doc and deep content validation
Web tools design doc; extended docs structure validation tests (RELIABILITY.md subsections, DESIGN.md content validation, conftest fixture self-tests, design doc cross-references; 29 new); 1717 tests.
