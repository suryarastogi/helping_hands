## v125 — Type safety, timeout bounds, and _is_boilerplate_line test coverage

**Status:** Completed
**Created:** 2026-03-11

## Goal

Three self-contained improvements: add missing return type hint to `_get_schedule_manager`, add upper bound validation to `_git_timeout` (cap at 3600s to prevent misconfiguration), and add direct test coverage for `_is_boilerplate_line` in pr_description.py (currently zero direct tests).

## Tasks

- [x] Add `ScheduleManager` return type hint to `_get_schedule_manager()` in `server/app.py`
- [x] Add upper bound (3600s) to `_git_timeout()` in `lib/github.py` with warning log
- [x] Add direct tests for `_is_boilerplate_line()` in pr_description.py
- [x] Add tests for `_get_schedule_manager` return type and `_git_timeout` upper bound
- [x] Consolidate daily plan files (2026-03-10.md, 2026-03-11.md already in Week-11)
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, testing-methodology.md)

## Completion criteria

- All new tests pass
- `ruff check` and `ruff format` pass
- Docs updated with v125 notes

---

## v126 — Input validation hardening and test coverage for untested helpers

**Status:** Completed
**Created:** 2026-03-11

## Goal

Three self-contained improvements: add `min_length=1` validation to `BuildRequest`/`ScheduleRequest` `repo_path` and `prompt` fields (currently accept empty strings), add mutual exclusivity validation to `_run_bash_script` in registry.py (currently allows both or neither `script_path`/`inline_script`), and add direct test coverage for `_as_string_keyed_dict` and `_require_http_url` host validation in web.py.

## Tasks

- [x] Add `min_length=1` to `BuildRequest.repo_path` and `BuildRequest.prompt`
- [x] Add `min_length=1` to `ScheduleRequest.repo_path` and `ScheduleRequest.prompt`
- [x] Add tests for empty/whitespace `repo_path` and `prompt` rejection
- [x] Add mutual exclusivity validation to `_run_bash_script` (exactly one of script_path/inline_script required)
- [x] Add tests for `_run_bash_script` mutual exclusivity (neither, both, each alone)
- [x] Add direct tests for `_as_string_keyed_dict` (valid dict, non-dict, non-string keys, empty dict, mixed keys)
- [x] Add direct tests for `_require_http_url` host validation edge cases (no host, with port, ftp scheme)
- [x] Update docs (PLANS.md, QUALITY_SCORE.md)

## Completion criteria

- All new tests pass (22 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v126 notes

---

## v127 — Input validation hardening for AI providers and server helpers

**Status:** Completed
**Created:** 2026-03-11

## Goal

Three self-contained improvements: harden `normalize_messages()` to reject non-Mapping items in message sequences (previously crashed with AttributeError), add empty-model guard to `AIProvider.complete()` to prevent silent failures, and add max-size guard to `_parse_task_kwargs_str()` to prevent parsing extremely large payloads.

## Tasks

- [x] Harden `normalize_messages()` with `TypeError` for non-Mapping items in sequence
- [x] Add empty-model `ValueError` guard to `AIProvider.complete()`
- [x] Add `_MAX_TASK_KWARGS_LEN` (1MB) size guard to `_parse_task_kwargs_str()`
- [x] Add tests for `normalize_messages()` non-Mapping rejection (4 tests: str, int, None, later index)
- [x] Add tests for `AIProvider.complete()` empty-model guard (3 tests: empty default, whitespace default, explicit override)
- [x] Add tests for `_parse_task_kwargs_str()` size limit (3 tests: oversized, at-limit, constant value)
- [x] Update docs (PLANS.md, QUALITY_SCORE.md)

## Completion criteria

- All new tests pass (10 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v127 notes

---

## v128 — Robustness hardening: JSON error handling, iteration bounds, inline edit safety

**Status:** Completed
**Created:** 2026-03-11

## Goal

Three self-contained robustness improvements:

1. **`_load_meta()` JSON error handling** in `schedules.py` — corrupted Redis data crashed with unhandled `json.JSONDecodeError`; wrapped in try-except with warning logging and return `None`.
2. **`max_iterations` upper bound** in `iterative.py` — was unbounded after `max(1, ...)` floor; added `_MAX_ITERATIONS` cap (1000) with warning log to prevent accidental runaway loops.
3. **`_apply_inline_edits` OSError handling** in `iterative.py` — only caught `ValueError` (invalid paths); `OSError` (permission denied, disk full) now caught separately with warning-level logging.

## Tasks

- [x] Harden `_load_meta()` with try-except around `json.loads()` and `ScheduledTask.from_dict()`
- [x] Add `_MAX_ITERATIONS` constant (1000) to `_BasicIterativeHand` with warning on cap
- [x] Catch `OSError` separately in `_apply_inline_edits` with warning log
- [x] Add tests for all three improvements (12 tests)
- [x] Run lint and tests
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-11)

## Completion criteria

- All new tests pass
- `ruff check` and `ruff format` pass
- Docs updated with v128 notes

---

## v129 — Exception debug logging and Goose _build_subprocess_env test coverage

**Status:** Completed
**Created:** 2026-03-11

## Goal

Three self-contained improvements:

1. **Add debug logging to silent `except Exception: raise`** in `atomic.py` and `iterative.py` — the only bare re-raises without logging in the codebase, inconsistent with the project convention of always logging before re-raising or suppressing exceptions.
2. **Add test coverage for `GooseCLIHand._build_subprocess_env()`** — completely untested method containing token validation, provider/model resolution, and OLLAMA_HOST injection.
3. **Add tests for the new debug logging** to verify log messages fire before re-raise.

## Tasks

- [x] Add `logger.debug()` with `exc_info=True` before `raise` in `atomic.py` `stream()` method
- [x] Add `logger.debug()` with `exc_info=True` before `raise` in `iterative.py` `BasicAtomicHand.stream()` method
- [x] Add tests for `GooseCLIHand._build_subprocess_env()` (10 tests)
- [x] Add tests for debug logging before re-raise (2 tests)
- [x] Run lint and tests
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-11)

## Completion criteria

- All new tests pass (12 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v129 notes
