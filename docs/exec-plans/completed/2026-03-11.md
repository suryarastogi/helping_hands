# Consolidated Plans: 2026-03-11

Plans completed on 2026-03-11.

---

## v119 - DRY validator extraction, input validation hardening, defensive guards

Extracted duplicate `_coerce_tools`/`_validate_tools`/`_coerce_skills`/`_validate_skills` validators from `BuildRequest` and `ScheduleRequest` into a shared `_ToolSkillValidatorMixin` base class with `max_length=50` on tools/skills list fields. Replaced `assert self.pr_number is not None` with explicit `ValueError` guards in three `base.py` methods. Added NaN-safe numeric parsing for `pr_number` in both frontend submission forms.

**Source changes:**
- `server/app.py`: Extracted `_ToolSkillValidatorMixin(BaseModel)` with shared `tools`/`skills` fields (including `max_length=_MAX_TOOL_SKILL_ITEMS`) and 4 field validators; `BuildRequest` and `ScheduleRequest` now inherit from mixin instead of `BaseModel`; removed 16 lines of duplicate validator code; added `_MAX_TOOL_SKILL_ITEMS = 50` constant
- `lib/hands/v1/hand/base.py`: Replaced `assert self.pr_number is not None` with `if self.pr_number is None: raise ValueError(...)` in `_push_to_existing_pr`, `_update_pr_description`, and `_create_pr_for_diverged_branch`
- `frontend/src/App.tsx`: Added `Number.isNaN()` and `Number.isFinite()` guards around `Number(form.pr_number.trim())` in both build submission and schedule creation forms

**Doc changes:**
- Updated PLANS.md (active v119)
- Updated QUALITY_SCORE.md (base.py v119 guard notes, server/app.py 23 test classes with mixin notes)
- Updated testing-methodology.md coverage count (3543 -> 3560)

**Test changes:**
- Added `TestToolSkillValidatorMixin` (8 tests: BuildRequest/ScheduleRequest mixin inheritance, tools/skills string coercion for both models, None→empty list for both fields)
- Added `TestToolSkillMaxLength` (5 tests: constant value, oversized tools/skills rejected, valid entries accepted)
- Added `TestPrNumberExplicitGuards` (4 tests: ValueError from _push_to_existing_pr/update_pr_description/create_pr_for_diverged_branch without pr_number, successful flow with pr_number set)

3560 tests (was 3543).

---

## v120 - CLI base test isolation fix, hook fix fallback coverage

Fixed CLI base.py coverage gap caused by `TestBaseCommandShlexError` tests being gated behind `pytest.importorskip("fastapi")` in `test_v115_code_quality.py`. Moved these tests to `test_cli_hand_base_utils.py` where they run unconditionally. Added tests for `_try_fix_git_hook_errors` fallback command failure paths (FileNotFoundError and TimeoutExpired on the fallback invocation).

**Source changes:**
- None (test-only change)

**Test changes:**
- Moved `TestBaseCommandShlexError` (3 tests) from `test_v115_code_quality.py` to `test_cli_hand_base_utils.py` to avoid fastapi importorskip gate
- Added `test_handles_fallback_also_file_not_found` and `test_handles_fallback_timeout` to `TestTryFixGitHookErrorsCLI` in `test_cli_hand_base_hook_fix.py` (covers lines 1057-1062)
- Removed unused `_TwoPhaseCLIHand` import from `test_v115_code_quality.py`

**Coverage impact:**
- `cli/base.py`: 99% (0 missing stmts, 1 partial branch: heartbeat-without-timeout tech debt) — was 99% with 6 missing stmts

3186 tests (was 3183).

---

## v121 - Defensive hardening: silent exception logging, assertion cleanup, repo_root validation

Added debug logging to two silent `except Exception` blocks in `base.py` (whoami fallback and default_branch fetch). Replaced unsafe `assert stdout is not None` with explicit `RuntimeError` in `docker_sandbox_claude.py`. Added `repo_root` is-directory validation to `resolve_repo_target()` in `filesystem.py`.

**Source changes:**
- `lib/hands/v1/hand/base.py`: Added `logger.debug(..., exc_info=True)` to `except Exception` at whoami call (line 410) and `except Exception: pass` at default_branch fetch (line 670)
- `lib/hands/v1/hand/cli/docker_sandbox_claude.py`: Replaced `assert stdout is not None` with `if stdout is None: raise RuntimeError(...)` in `_ensure_sandbox`
- `lib/meta/tools/filesystem.py`: Added `if not root.is_dir(): raise ValueError(...)` check at start of `resolve_repo_target()`

**Doc changes:**
- Updated PLANS.md (active v121)
- Updated QUALITY_SCORE.md (base.py, filesystem.py, docker_sandbox_claude.py v121 notes)
- Updated testing-methodology.md coverage count (3186 -> 3192)

**Test changes:**
- Added `TestFinalizeDefaultBranchExceptionLogging` (1 test: verifies debug log emitted when get_repo raises)
- Added `TestPushToExistingPrWhoamiExceptionLogging` (1 test: verifies debug log emitted when whoami raises)
- Added `TestEnsureSandboxStdoutNone` (1 test: RuntimeError raised instead of AssertionError when stdout is None)
- Added `TestResolveRepoTargetRootValidation` (3 tests: nonexistent root, file-as-root, valid directory)

3192 tests (was 3186).

---

## v122 - Defensive hardening: assert cleanup, silent exception logging, MCP validation

Three self-contained hardening improvements: replaced `assert` statements with explicit `RuntimeError` guards in `command.py` and `e2e.py`, added `logger.debug` calls to 4 silent exception handlers across `claude.py`, `schedules.py`, `iterative.py`, and `e2e.py`, added non-empty input validation to 5 MCP server tools, and hardened `ScheduledTask.from_dict` with required-field validation.

**Source changes:**
- `lib/meta/tools/command.py`: Replaced `assert script_path is not None` and `assert inline_script is not None` with explicit `RuntimeError` guards in `run_bash_script`
- `lib/hands/v1/hand/e2e.py`: Replaced `assert final_pr_number is not None` with `RuntimeError`; added `logger.debug` with `exc_info=True` to `except Exception:` in `run()` default_branch fetch; added `import logging` and `logger`
- `lib/hands/v1/hand/cli/claude.py`: Added `logger.debug("geteuid() check failed", exc_info=True)` to `except Exception:` in `_skip_permissions_enabled`; added `import logging` and `logger`
- `server/schedules.py`: Added `logger.debug(...)` to `except KeyError:` in `_delete_redbeat_entry`; added `_REQUIRED_FIELDS` tuple and required-field validation to `ScheduledTask.from_dict`; added `import logging` and `logger`
- `lib/hands/v1/hand/iterative.py`: Added `logger.debug("Skipping inline edit for %r: invalid path", ...)` to `except ValueError:` in `_apply_inline_edits`; added `import logging` and `logger`
- `server/mcp_server.py`: Added non-empty string validation to `build_feature` (repo_path, prompt), `get_task_status` (task_id), `web_search` (query), `web_browse` (url)

**Doc changes:**
- Updated PLANS.md (active v122 → completed)
- Updated QUALITY_SCORE.md (command.py, e2e.py, iterative.py, claude.py, schedules.py, mcp_server.py v122 notes)
- Updated testing-methodology.md coverage count (3192 → 3208)

**Test changes:**
- Added `TestCommandAssertGuards` (2 tests: script_path and inline_script guards are RuntimeError)
- Added `TestE2EAssertGuard` (1 test: final_pr_number guard is RuntimeError)
- Added `TestClaudeGeteuidExceptionLogging` (1 test: debug log on geteuid failure)
- Added `TestDeleteRedbeatEntryLogging` (1 test: debug log on KeyError)
- Added `TestScheduledTaskFromDictValidation` (5 tests: missing single/multiple/all fields, valid data, missing prompt)
- Added `TestApplyInlineEditsLogging` (1 test: debug log on invalid path ValueError)
- Added `TestE2EDefaultBranchLogging` (1 test: debug log on default_branch exception)
- Added `TestMcpBuildFeatureValidation` (4 tests: empty/whitespace repo_path, empty/whitespace prompt)
- Added `TestMcpGetTaskStatusValidation` (2 tests: empty/whitespace task_id)
- Added `TestMcpWebSearchValidation` (2 tests: empty/whitespace query)
- Added `TestMcpWebBrowseValidation` (2 tests: empty/whitespace url)

3208 tests (was 3192).

---

## v123 - Tool summarization expansion, assert→RuntimeError guards, debug logging

Three self-contained hardening improvements: expanded `_summarize_tool` in `claude.py` with 6 new Claude Code tool types, replaced 4 `assert` statements in `schedules.py` with explicit `RuntimeError` guards, and added debug logging to `_repo_has_changes` in `cli/base.py`.

**Source changes:**
- `lib/hands/v1/hand/cli/claude.py`: Added `Skill`, `CronCreate`, `CronDelete`, `CronList`, `EnterWorktree`, `ExitWorktree` branches to `_summarize_tool` with parameter extraction (skill name, prompt truncation, id, worktree name, action)
- `server/schedules.py`: Replaced `assert croniter is not None` with `if croniter is None: raise RuntimeError(...)` in `validate_cron_expression` and `next_run_time`; replaced `assert RedBeatSchedulerEntry is not None` with `if RedBeatSchedulerEntry is None: raise RuntimeError(...)` in `_create_redbeat_entry` and `_delete_redbeat_entry`
- `lib/hands/v1/hand/cli/base.py`: Added `logger.debug("git status check failed (code=%d); assuming no changes", ...)` to `_repo_has_changes` when `git status --porcelain` returns non-zero

**Doc changes:**
- Updated PLANS.md (active v123 → completed)
- Updated QUALITY_SCORE.md (claude.py v123 tool summarization, schedules.py v123 assert guards, cli/base.py v123 logging)
- Updated testing-methodology.md coverage count (3208 → 3221)
- Consolidated Mar 10–11 daily plans into `docs/exec-plans/completed/2026/Week-11.md`

**Test changes:**
- Added 12 tests to `TestSummarizeTool` in `test_cli_hand_claude.py`: Skill (with/without name), CronCreate (with prompt/long prompt truncation/no prompt), CronDelete (with/without id), CronList, EnterWorktree (with/without name), ExitWorktree (with/without action)
- Added `TestCroniterRuntimeErrorGuards` in `test_v123_hardening.py` (2 tests: validate_cron_expression and next_run_time raise RuntimeError when croniter is None)
- Added `TestRedBeatRuntimeErrorGuards` in `test_v123_hardening.py` (2 tests: _create_redbeat_entry and _delete_redbeat_entry raise RuntimeError when RedBeatSchedulerEntry is None)
- Added `test_git_status_failure_logs_debug` to `TestRepoHasChanges` in `test_cli_hand_base_helpers.py` (1 test: debug log emitted on git status failure)

3221 tests (was 3208).

---

## v124 - Git operation hardening: timeout + full_name validation

Hardened `_run_git()` subprocess helper with a configurable timeout (default 300s via `HELPING_HANDS_GIT_TIMEOUT` env var) to prevent indefinite hangs during git clone/push/pull operations. Added `_validate_full_name()` to `GitHubClient.get_repo()` to reject malformed `owner/repo` strings with clear error messages before they reach PyGithub.

**Source changes:**
- `lib/github.py`: Added `_DEFAULT_GIT_TIMEOUT = 300`, `_git_timeout()` (env var parsing with validation), `_validate_full_name()` (owner/repo format check); updated `_run_git()` with `timeout=` parameter and `TimeoutExpired` → `RuntimeError` conversion; added `_validate_full_name()` call to `get_repo()`

**Doc changes:**
- Updated PLANS.md (active v124 → completed)
- Updated QUALITY_SCORE.md (github.py 21 test classes with v124 notes)
- Updated testing-methodology.md coverage count (3221 → 3241)

**Test changes:**
- Added `TestGitTimeout` (7 tests: default/env override/non-numeric/zero/negative/empty/whitespace)
- Added `TestRunGitTimeout` (3 tests: timeout raises RuntimeError/redacts token/passes timeout to subprocess)
- Added `TestValidateFullName` (10 tests: valid/valid-with-dashes/empty/whitespace/no-slash/multi-slash/trailing-slash/leading-slash/space/tab)
- Added `TestGetRepoValidation` (2 tests: rejects invalid/accepts valid full_name)

3243 tests (was 3221).
