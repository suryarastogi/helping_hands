## v161 — Add `__all__` exports to hand, CLI hand, server, and CLI entry modules

**Status:** Completed
**Created:** 2026-03-14

**API consistency:** Added `__all__` declarations to 13 remaining modules, completing project-wide explicit public API coverage. Hand modules: `base.py` (`["Hand", "HandResponse"]`), `e2e.py` (`["E2EHand"]`), `iterative.py` (`["BasicAtomicHand", "BasicLangGraphHand"]`). CLI hand modules: `claude.py` (`["ClaudeCodeHand"]`), `codex.py` (`["CodexCLIHand"]`), `gemini.py` (`["GeminiCLIHand"]`), `goose.py` (`["GooseCLIHand"]`), `opencode.py` (`["OpenCodeCLIHand"]`), `docker_sandbox_claude.py` (`["DockerSandboxClaudeCodeHand"]`). Server modules: `app.py` (17 public symbols: app instance + 16 Pydantic models), `celery_app.py` (`["build_feature", "celery_app"]`), `mcp_server.py` (`["main", "mcp"]`). CLI entry: `main.py` (`["build_parser", "main"]`). All entries isort-sorted per RUF022. **3858 passing tests (+48 new: 35 passed, 13 skipped without server extras), 112 skipped.**

## Context

Versions v156–v159 added `__all__` to 17 modules (config, types, github, filesystem, command, web, repo, default_prompts, task_result, 5 AI providers, pr_description, model_provider, schedules). The remaining modules without `__all__` are:

- Hand modules: `base.py`, `e2e.py`, `iterative.py`
- CLI hand modules: `claude.py`, `codex.py`, `gemini.py`, `goose.py`, `opencode.py`, `docker_sandbox_claude.py`
- Server modules: `app.py`, `celery_app.py`, `mcp_server.py`
- CLI entry point: `cli/main.py`

## Tasks

- [x] Add `__all__` to `hands/v1/hand/base.py` — `["Hand", "HandResponse"]`
- [x] Add `__all__` to `hands/v1/hand/e2e.py` — `["E2EHand"]`
- [x] Add `__all__` to `hands/v1/hand/iterative.py` — `["BasicAtomicHand", "BasicLangGraphHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/claude.py` — `["ClaudeCodeHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/codex.py` — `["CodexCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/gemini.py` — `["GeminiCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/goose.py` — `["GooseCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/opencode.py` — `["OpenCodeCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/docker_sandbox_claude.py` — `["DockerSandboxClaudeCodeHand"]`
- [x] Add `__all__` to `server/app.py` — public request/response models + `app` instance
- [x] Add `__all__` to `server/celery_app.py` — `celery_app` + public tasks
- [x] Add `__all__` to `server/mcp_server.py` — `mcp` + `main`
- [x] Add `__all__` to `cli/main.py` — `["build_parser", "main"]`
- [x] Write tests for all new `__all__` declarations
- [x] Run lint + tests

## Completion criteria

- All modules in `src/helping_hands/` have `__all__` declarations
- All `__all__` entries are isort-sorted per RUF022
- Tests verify: expected symbols present, no private names, all symbols importable, correct count
- All existing tests pass

---

## v162 — Extract bootstrap doc constants, DRY backend names, add docstrings to iterative hand

**Status:** Completed
**Created:** 2026-03-14

**Maintainability and documentation:** Extracted inline bootstrap document filename tuples to module-level constants (`_README_CANDIDATES`, `_AGENT_DOC_CANDIDATES`) in `iterative.py`. Extracted hardcoded backend name strings to class-level `_BACKEND_NAME` constants in `BasicLangGraphHand` (`"basic-langgraph"`) and `BasicAtomicHand` (`"basic-atomic"`), replacing 6 hardcoded occurrences across `run()` and `stream()` methods. Added Google-style docstrings with Args/Returns sections to 8 key protected methods: `_execution_tools_enabled()`, `_web_tools_enabled()`, `_tool_instructions()`, `_format_command()`, `_tool_disabled_error()`, `_read_bootstrap_doc()`, `_build_tree_snapshot()`, `_build_bootstrap_context()`. **3880 passing tests (+22 new), 112 skipped.**

## Tasks

- [x] Add `_README_CANDIDATES = ("README.md", "readme.md")` module-level constant
- [x] Add `_AGENT_DOC_CANDIDATES = ("AGENT.md", "agent.md")` module-level constant
- [x] Update `_build_bootstrap_context()` to use the new constants
- [x] Add `_BACKEND_NAME = "basic-langgraph"` class-level constant to `BasicLangGraphHand`
- [x] Add `_BACKEND_NAME = "basic-atomic"` class-level constant to `BasicAtomicHand`
- [x] Replace all hardcoded backend name strings with `self._BACKEND_NAME`
- [x] Add Google-style docstrings to 8 key methods
- [x] Write tests for all new constants and docstrings (22 tests)
- [x] Run lint + tests — 3880 passing, 112 skipped

## Completion criteria

- All bootstrap doc filenames use named constants
- Backend name strings are DRY via class-level constants
- All key protected methods have Google-style docstrings
- All new tests pass (22 new tests)
- `ruff check` and `ruff format` pass

---

## v163 — Add docstrings to Hand base.py methods, extract Claude CLI stream-json constants

**Status:** Completed
**Created:** 2026-03-14

**Documentation and maintainability:** Added Google-style docstrings with Args/Returns/Raises sections to 9 protected methods in `base.py`: `_is_interrupted()`, `_default_base_branch()`, `_run_git_read()`, `_github_repo_from_origin()`, `_build_generic_pr_body()`, `_configure_authenticated_push_remote()`, `_should_run_precommit_before_pr()`, `_run_precommit_checks_and_fixes()`, `_finalize_repo_pr()`. Extracted 6 hardcoded stream-json event/block type strings to module-level constants in `claude.py`: `_EVENT_TYPE_ASSISTANT`, `_EVENT_TYPE_USER`, `_EVENT_TYPE_RESULT`, `_BLOCK_TYPE_TOOL_USE`, `_BLOCK_TYPE_TOOL_RESULT`, `_BLOCK_TYPE_TEXT`. **3902 passing tests (+22 new), 112 skipped.**

## Tasks

- [x] Add Google-style docstrings to 9 `base.py` protected methods
- [x] Extract 6 stream-json event/block type constants in `claude.py`
- [x] Replace all inline string usages with constants
- [x] Write tests (22 new: 11 docstring presence, 11 constant value/type/distinctness)
- [x] Run lint + tests — 3902 passing, 112 skipped

## Completion criteria

- All 9 `base.py` methods have Google-style docstrings with Args/Returns sections
- All 6 stream-json strings are module-level constants in `claude.py`
- Tests verify constant values, docstring presence, and symbol importability
- All existing tests pass

---

## v164 — Close remaining coverage gaps in Hand base.py, pr_description.py, claude.py

**Status:** Completed
**Created:** 2026-03-14

**Test coverage:** Closed 10 previously uncovered lines across 3 modules with 8 new targeted tests. Hand base.py: tested `pr_number is None` ValueError guards on `_push_to_existing_pr`, `_update_pr_description`, `_create_pr_for_diverged_branch` (3 tests). pr_description.py: tested multi-word keyword substring matching in `_infer_commit_type` ("clean up" → refactor, "github action" → ci), second `FileNotFoundError` in `_get_uncommitted_diff` (git add succeeds but git diff --cached fails), and empty-line skip in `_commit_message_from_prompt` boilerplate extraction (4 tests). claude.py: tested `_resolve_cli_model()` empty-model early return by monkeypatching `_DEFAULT_MODEL` to "" (1 test). **3910 passing tests (+8 new), 112 skipped.**

## Tasks

- [x] Test `_push_to_existing_pr` raises ValueError when pr_number is None
- [x] Test `_update_pr_description` raises ValueError when pr_number is None
- [x] Test `_create_pr_for_diverged_branch` raises ValueError when pr_number is None
- [x] Test `_infer_commit_type` multi-word keyword "clean up" → refactor
- [x] Test `_infer_commit_type` multi-word keyword "github action" → ci
- [x] Test `_get_uncommitted_diff` second subprocess FileNotFoundError
- [x] Test `_commit_message_from_prompt` empty-line skip between boilerplate and content
- [x] Test `ClaudeCodeHand._resolve_cli_model()` returns "" with empty `_DEFAULT_MODEL`
- [x] Run lint + tests — 3910 passing, 112 skipped

## Completion criteria

- All 10 previously uncovered lines are now exercised
- All existing tests continue to pass
- No production code changes (tests only)

---

## v165 — DRY auth error detection across CLI hands, add docstrings to CLI base.py methods

**Status:** Completed
**Created:** 2026-03-14

**DRY and documentation:** Extracted duplicated authentication error detection strings from 4 CLI hands (claude.py, gemini.py, opencode.py, codex.py) into a shared `_AUTH_ERROR_TOKENS` module-level tuple and `_is_auth_error()` static method in `cli/base.py`. Each CLI hand now calls the shared helper with optional `extra_tokens` for backend-specific markers (e.g. `"anthropic_api_key"` for Claude, `"gemini_api_key"` for Gemini, `"missing bearer or basic authentication"` for Codex). Added Google-style docstrings with Args/Returns/Raises/Yields sections to 12 key `_TwoPhaseCLIHand` methods: `_base_command()`, `_resolve_cli_model()`, `_render_command()`, `_build_failure_message()`, `_command_not_found_message()`, `_fallback_command_when_not_found()`, `_retry_command_after_failure()`, `_build_init_prompt()`, `_build_task_prompt()`, `_invoke_backend()`, `run()`, `stream()`. **3962 passing tests (+52 new), 112 skipped.**

## Tasks

- [x] Add `_AUTH_ERROR_TOKENS` module-level tuple in `cli/base.py`
- [x] Add `_is_auth_error(output, *, extra_tokens=())` static method to `_TwoPhaseCLIHand`
- [x] Refactor `claude.py` `_build_claude_failure_message()` to use `_is_auth_error()`
- [x] Refactor `gemini.py` `_build_gemini_failure_message()` to use `_is_auth_error()`
- [x] Refactor `opencode.py` `_build_opencode_failure_message()` to use `_is_auth_error()`
- [x] Refactor `codex.py` `_build_codex_failure_message()` to use `_is_auth_error()`
- [x] Add Google-style docstrings to 12 key `_TwoPhaseCLIHand` methods
- [x] Write tests for `_AUTH_ERROR_TOKENS` constant and `_is_auth_error()` method (18 tests)
- [x] Write tests for CLI hand auth regression (12 tests)
- [x] Write tests for docstring presence (24 tests: 12 presence + 12 section checks)
- [x] Run lint + tests — 3962 passing, 112 skipped

## Completion criteria

- All 4 CLI hands use shared `_is_auth_error()` for auth detection
- Auth detection behavior is identical before and after refactor
- All key CLI base.py methods have Google-style docstrings
- All new tests pass (52 new tests)
- `ruff check` and `ruff format` pass
