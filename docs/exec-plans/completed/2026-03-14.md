## v161 — Add `__all__` exports to hand, CLI hand, server, and CLI entry modules

**Status:** Completed
**Created:** 2026-03-14

**API consistency:** Added `__all__` declarations to 13 remaining modules, completing project-wide explicit public API coverage. Hand modules: `base.py` (`["Hand", "HandResponse"]`), `e2e.py` (`["E2EHand"]`), `iterative.py` (`["BasicAtomicHand", "BasicLangGraphHand"]`). CLI hand modules: `claude.py` (`["ClaudeCodeHand"]`), `codex.py` (`["CodexCLIHand"]`), `gemini.py` (`["GeminiCLIHand"]`), `goose.py` (`["GooseCLIHand"]`), `opencode.py` (`["OpenCodeCLIHand"]`), `docker_sandbox_claude.py` (`["DockerSandboxClaudeCodeHand"]`). Server modules: `app.py` (17 public symbols: app instance + 16 Pydantic models), `celery_app.py` (`["build_feature", "celery_app"]`), `mcp_server.py` (`["main", "mcp"]`). CLI entry: `main.py` (`["build_parser", "main"]`). All entries isort-sorted per RUF022. **3858 passing tests (+48 new: 35 passed, 13 skipped without server extras), 112 skipped.**

## Context

Versions v156–v159 added `__all__` to 17 modules (config, types, github, filesystem, command, web, repo, default_prompts, task_result, 5 AI providers, pr_description, model_provider, schedules). The remaining modules without `__all__` are:

- Hand modules: `base.py`, `e2e.py`, `iterative.py`
- CLI hand modules: `claude.py`, `codex.py`, `gemini.py`, `goose.py`, `opencode.py`, `docker_sandbox_claude.py`
- Server modules: `app.py`, `celery_app.py`, `mcp_server.py`
- CLI entry point: `cli/main.py`

## Tasks

- [x] Add `__all__` to `hands/v1/hand/base.py` — `["Hand", "HandResponse"]`
- [x] Add `__all__` to `hands/v1/hand/e2e.py` — `["E2EHand"]`
- [x] Add `__all__` to `hands/v1/hand/iterative.py` — `["BasicAtomicHand", "BasicLangGraphHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/claude.py` — `["ClaudeCodeHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/codex.py` — `["CodexCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/gemini.py` — `["GeminiCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/goose.py` — `["GooseCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/opencode.py` — `["OpenCodeCLIHand"]`
- [x] Add `__all__` to `hands/v1/hand/cli/docker_sandbox_claude.py` — `["DockerSandboxClaudeCodeHand"]`
- [x] Add `__all__` to `server/app.py` — public request/response models + `app` instance
- [x] Add `__all__` to `server/celery_app.py` — `celery_app` + public tasks
- [x] Add `__all__` to `server/mcp_server.py` — `mcp` + `main`
- [x] Add `__all__` to `cli/main.py` — `["build_parser", "main"]`
- [x] Write tests for all new `__all__` declarations
- [x] Run lint + tests

## Completion criteria

- All modules in `src/helping_hands/` have `__all__` declarations
- All `__all__` entries are isort-sorted per RUF022
- Tests verify: expected symbols present, no private names, all symbols importable, correct count
- All existing tests pass
