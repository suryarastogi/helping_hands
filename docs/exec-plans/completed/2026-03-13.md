## v147 — Per-task GitHub token override, pr_description dead code cleanup, constant docstrings

**Status:** Completed
**Created:** 2026-03-13

## Goal

Three self-contained improvements:

1. **Per-task GitHub token override** — Addresses TODO.md item: "ability to add a github token for a specific task." Add `github_token` field to `Config`, wire through `Config.from_env()` via `HELPING_HANDS_GITHUB_TOKEN` env var, pass to `GitHubClient()` in all 3 call sites (`base.py`, `e2e.py`, `cli/base.py`), add `--github-token` CLI arg, add `github_token` field to server `BuildRequest`/`ScheduleRequest` models and Celery task signature.

2. **Remove dead `if cmd else "cli"` fallbacks** in `pr_description.py` — `generate_pr_description()` (line 333) and `generate_commit_message()` (line 635) both have `cli_label = cmd[0] if cmd else "cli"` where `cmd` cannot be None (guarded by early returns on lines 311 and 616). Simplify to `cmd[0]`.

3. **Add docstrings for `_COMMIT_MSG_DIFF_LIMIT` and `_COMMIT_MSG_TIMEOUT`** — These two constants in `pr_description.py` (lines 383-384) lack docstrings, unlike all other module-level constants in the same file which have docstring annotations.

## Tasks

- [x] Add `github_token: str = ""` field to `Config` dataclass
- [x] Load `HELPING_HANDS_GITHUB_TOKEN` env var in `Config.from_env()`
- [x] Pass `config.github_token` (when non-empty) to `GitHubClient()` in `base.py`, `e2e.py`, `cli/base.py`
- [x] Add `--github-token` CLI arg in `cli/main.py` and wire to Config overrides
- [x] Add `github_token` field to `BuildRequest` and `ScheduleRequest` in `server/app.py`
- [x] Add `github_token` parameter to `build_feature()` in `server/celery_app.py` and pass to Config
- [x] Wire `github_token` through server task dispatch (`schedules.py`, `app.py`)
- [x] Remove dead `if cmd else "cli"` in `generate_pr_description()` and `generate_commit_message()`
- [x] Add docstrings for `_COMMIT_MSG_DIFF_LIMIT` and `_COMMIT_MSG_TIMEOUT`
- [x] Add tests for Config `github_token` (7 tests: default, override, env var, override beats env, none preserves env, no env no override, frozen)
- [x] Add tests for `GitHubClient` token passthrough from Config (4 tests: field exists, hand base reads, empty falls back, default empty)
- [x] Add tests for CLI `--github-token` arg (4 tests: parser accepts, default None, wired to config e2e, wired to config backend)
- [x] Add tests for dead code removal and constant docstrings (8 tests: 2 cli_label, 6 constant tests)
- [x] Run lint and tests — 3563 passing, 80 skipped
- [x] Update docs (TODO.md, PLANS.md, QUALITY_SCORE.md, Week-12)

## Completion criteria

- All new tests pass (21 new tests)
- `ruff check` and `ruff format` pass
- Per-task GitHub token flows from CLI/server → Config → GitHubClient
- Docs updated with v147 notes

---

## v148 — Remove hardcoded DB credentials, GitHubClient input validation

**Status:** Completed
**Created:** 2026-03-13

## Goal

Two self-contained improvements:

1. **Remove hardcoded database credentials** — `_get_db_url_writer()` in `celery_app.py` contained a hardcoded PostgreSQL connection string with plaintext credentials as a fallback default. Replaced with a `RuntimeError` when `DATABASE_URL` is not set or empty, consistent with secure-by-default practices.

2. **Add empty-string validation to GitHubClient branch/commit methods** — `create_branch`, `switch_branch`, `fetch_branch`, `add_and_commit` (message param), and `set_local_identity` (name/email params) now validate inputs before passing to git subprocess, consistent with `_validate_full_name()` pattern.

## Tasks

- [x] Remove hardcoded DB credentials in `_get_db_url_writer()`, raise `RuntimeError` if `DATABASE_URL` not set
- [x] Add `_validate_branch_name()` helper to `github.py` (reject empty/whitespace)
- [x] Add validation to `create_branch`, `switch_branch`, `fetch_branch` for branch_name
- [x] Add validation to `add_and_commit` for empty message
- [x] Add validation to `set_local_identity` for empty name/email
- [x] Add tests for `_get_db_url_writer()` (5 tests: env set, no env raises, empty raises, whitespace raises, strips whitespace)
- [x] Add tests for `_validate_branch_name()` (5 tests: empty, whitespace, tab, valid branch, valid simple)
- [x] Add tests for GitHubClient method validation (11 tests: create_branch, switch_branch, fetch_branch, add_and_commit, set_local_identity)
- [x] Run lint and tests — 3582 passing, 80 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-12)

## Completion criteria

- Hardcoded credentials removed from source code
- All GitHubClient branch/commit methods validate string inputs
- All new tests pass (19 new tests)
- `ruff check` and `ruff format` pass

---

## v149 — Git subprocess timeouts, clone URL validation, error message redaction

**Status:** Completed
**Created:** 2026-03-13

## Goal

Four self-contained robustness and security improvements:

1. **Add timeout to `_run_git_read()`** (`base.py`) — Added `_GIT_READ_TIMEOUT_S = 30` constant and `timeout=` parameter to the static helper, catching `TimeoutExpired` and returning empty string.

2. **Add timeout to `_repo_has_changes()`** (`cli/base.py`) — Imported and used `_GIT_READ_TIMEOUT_S` for the `git status --porcelain` subprocess call, catching `TimeoutExpired` and returning `False`.

3. **Validate repo spec in `_github_clone_url()`** (`cli/main.py` and `celery_app.py`) — Added `_validate_repo_spec()` that rejects empty strings, whitespace-only, and strings not matching `owner/repo` format before they are embedded into URL strings.

4. **Redact env var value from `_base_command()` error message** (`cli/base.py`) — Removed the raw env var value `({raw!r})` from the `shlex.split()` error message to prevent potential token/secret exposure in logs.

## Tasks

- [x] Add `_GIT_READ_TIMEOUT_S = 30` constant in `base.py`
- [x] Add `timeout=_GIT_READ_TIMEOUT_S` to `_run_git_read()`, catch `TimeoutExpired`
- [x] Import and use `_GIT_READ_TIMEOUT_S` in `cli/base.py` `_repo_has_changes()` with `TimeoutExpired` catch
- [x] Add `_validate_repo_spec()` helper in `cli/main.py` and `celery_app.py`
- [x] Wire `_validate_repo_spec()` into `_github_clone_url()` in both modules
- [x] Redact raw env var value in `_base_command()` error message
- [x] Add tests (17 new: 3 constant, 3 _run_git_read timeout, 2 _repo_has_changes timeout, 7+5 _validate_repo_spec, 2+2 clone URL validation, 1 error redaction)
- [x] Run lint and tests — 3599 passing, 80 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-12)

## Completion criteria

- All git subprocess calls in `base.py` and `cli/base.py` have timeouts
- `_github_clone_url()` validates repo format before URL construction
- Sensitive env var values are not exposed in error messages
- All new tests pass (17 new: 10 passed, 7 skipped without celery)
- `ruff check` and `ruff format` pass

---

## v150 — GitHubClient method input validation hardening

**Status:** Completed
**Created:** 2026-03-13

## Goal

Four self-contained input validation improvements in `github.py`, completing the validation coverage for all public GitHubClient methods:

1. **`create_pr()` input validation** — `title`, `head`, and `base` are not validated for empty/whitespace. Empty strings would produce cryptic PyGithub errors. Added validation using `_validate_branch_name()` for head/base and inline check for title.

2. **`get_check_runs()` ref validation** — `ref` (commit SHA, branch name, or tag) is not validated for empty/whitespace. Added validation mirroring `_validate_branch_name()` pattern.

3. **`upsert_pr_comment()` input validation** — `body` is not validated for empty/whitespace (would create empty comments), and `number` is not validated for positive value (unlike `get_pr`/`update_pr_body`). Added both checks.

4. **`list_prs()` state validation** — `state` is not validated against the allowed values ("open", "closed", "all"). Invalid values would be passed to the GitHub API and rejected. Added `_VALID_PR_STATES` frozenset constant and enum validation.

## Tasks

- [x] Add title/head/base validation to `create_pr()` in `github.py`
- [x] Add ref validation to `get_check_runs()` in `github.py`
- [x] Add body/number validation to `upsert_pr_comment()` in `github.py`
- [x] Add `_VALID_PR_STATES` frozenset and state enum validation to `list_prs()` in `github.py`
- [x] Add tests for all validation (20 new tests: 6 create_pr, 7 list_prs state, 3 get_check_runs, 4 upsert_pr_comment)
- [x] Run lint and tests — 3619 passing, 80 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-12)

## Completion criteria

- All public GitHubClient methods validate their inputs consistently
- All new tests pass (20 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v150 notes

---

## v151 — Input type validation for filesystem, tool/skill selection, and summary truncation

**Status:** Completed
**Created:** 2026-03-13

## Goal

Four self-contained input validation improvements adding runtime type checks to public functions that accept typed parameters but don't validate at runtime:

1. **`normalize_relative_path()` non-string rejection** (`filesystem.py`) — Function declares `rel_path: str` but calling with a non-string (int, dict, list) produces an `AttributeError` at `.strip()` instead of a clear validation error. Added `isinstance` guard raising `TypeError`.

2. **`normalize_tool_selection()` input type validation** (`registry.py`) — Function accepts `str | list[str] | tuple[str, ...] | None` but passing a `dict` or `set` silently processes via `list(values)` (yielding dict keys or set items). Added explicit type validation before processing.

3. **`normalize_skill_selection()` input type validation** (`skills/__init__.py`) — Same pattern as `normalize_tool_selection()`. A `dict` or `set` input is silently converted via `list()` instead of raising a clear error.

4. **`_truncate_summary()` positive limit validation** (`cli/base.py`) — The `limit: int` parameter accepts any integer but negative or zero values produce unexpected truncation behavior. Added `limit > 0` guard with `ValueError`, matching the `_truncate_diff` pattern (v140).

## Tasks

- [x] Add `isinstance(rel_path, str)` guard to `normalize_relative_path()` in `filesystem.py`
- [x] Add input type validation to `normalize_tool_selection()` in `registry.py`
- [x] Add input type validation to `normalize_skill_selection()` in `skills/__init__.py`
- [x] Add `limit > 0` validation to `_truncate_summary()` in `cli/base.py`
- [x] Add tests for all improvements (28 new tests: 6 filesystem, 8 tool selection, 8 skill selection, 6 truncation)
- [x] Run lint and tests — 3644 passing, 80 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-12)

## Completion criteria

- All public functions validate input types before processing
- All new tests pass (28 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v151 notes

---

## v152 — Remove stale `ty: ignore` comments causing CI warnings

**Status:** Completed
**Created:** 2026-03-13

## Goal

Fix 7 `unused-ignore-comment` warnings from `ty check` across 3 files. These stale suppression comments were added when `ty` flagged issues that have since been resolved in newer `ty` versions, making the ignore directives themselves trigger warnings.

## Analysis

1. **`model_provider.py:86-87`** — 2 unused `ty: ignore[unknown-argument]` on `base_url` and `api_key` kwargs to `ChatOpenAI()`.
2. **`celery_app.py:977`** — 1 unused `ty: ignore[unresolved-attribute]` on `@celery_app.on_after_finalize.connect`.
3. **`schedules.py:27-29,34`** — 4 unused `ty: ignore[invalid-assignment]` on fallback `None` assignments when `redbeat`/`croniter` imports fail.

## Tasks

- [x] Remove `# ty: ignore[unknown-argument]` from `model_provider.py:86-87`
- [x] Remove `# ty: ignore[unresolved-attribute]` from `celery_app.py:977`
- [x] Remove `# ty: ignore[invalid-assignment]` from `schedules.py:27-29,34`
- [x] Add regression guard tests (5 tests: 2 model_provider, 1 celery_app, 1 schedules, 1 codebase-wide)
- [x] Run `ty check` — 0 warnings, all checks passed
- [x] Run `ruff check` and `ruff format` — clean
- [x] Run `pytest` — 3649 passing, 82 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md, Week-12)

## Completion criteria

- `ty check src --ignore unresolved-import --ignore invalid-method-override` produces 0 warnings
- All existing tests pass (3649 passing + 5 new = 3649 total, 82 skipped)
- `ruff check` and `ruff format` pass
- Docs updated with v152 notes

---

## v153 — Subprocess timeouts and input validation for Hand base + GitHubClient.clone()

**Status:** Completed
**Created:** 2026-03-13

## Goal

Four self-contained robustness improvements:

1. **Add timeout to `_configure_authenticated_push_remote()`** (`base.py`) — The `git remote set-url` subprocess call has no timeout, unlike the sibling `_run_git_read()` which uses `_GIT_READ_TIMEOUT_S`. Added timeout and `TimeoutExpired` handling.

2. **Add timeout constant for `_run_precommit_checks_and_fixes()`** (`base.py`) — The `uv run pre-commit run --all-files` subprocess call has no timeout. Pre-commit can hang indefinitely. Added `_PRECOMMIT_TIMEOUT_S = 300` constant and wired it in.

3. **Validate `repo`/`token` in `_configure_authenticated_push_remote()`** (`base.py`) — The `repo` and `token` parameters are embedded into a URL without validation. Added empty/whitespace rejection.

4. **Validate `full_name` in `GitHubClient.clone()`** (`github.py`) — The `full_name` parameter was not validated before being embedded in a clone URL, unlike `get_repo()` which calls `_validate_full_name()`. Added the same validation.

## Tasks

- [x] Add `timeout=_GIT_READ_TIMEOUT_S` and `TimeoutExpired` handling to `_configure_authenticated_push_remote()`
- [x] Add `_PRECOMMIT_TIMEOUT_S = 300` constant and wire to `_run_precommit_checks_and_fixes()`
- [x] Add `repo`/`token` validation to `_configure_authenticated_push_remote()`
- [x] Add `_validate_full_name(full_name)` call to `GitHubClient.clone()`
- [x] Add tests for all improvements (16 new tests)
- [x] Run lint, format, and tests — 3666 passing, 82 skipped
- [x] Update docs (PLANS.md, QUALITY_SCORE.md)

## Completion criteria

- All git subprocess calls in `base.py` have timeouts
- `_configure_authenticated_push_remote()` validates inputs before URL construction
- `GitHubClient.clone()` validates `full_name` before cloning
- All new tests pass (16 new tests)
- `ruff check` and `ruff format` pass
- Docs updated with v153 notes
