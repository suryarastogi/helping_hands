# Quality Score

Metrics and standards for code quality in helping_hands.

## Current quality gates

### CI pipeline (GitHub Actions)

| Check | Tool | Scope | Status |
|---|---|---|---|
| Lint | `ruff check` | All Python | Enforced |
| Format | `ruff format --check` | All Python | Enforced |
| Type check | `ty check` | `src/` | Enforced |
| Tests | `pytest -v` | `tests/` | Enforced |
| Coverage | `pytest-cov` + Codecov | Python 3.12 job | Reporting |
| Frontend lint | `eslint` | `frontend/src/` | Enforced |
| Frontend types | `tsc --noEmit` | `frontend/src/` | Enforced |
| Frontend tests | Vitest | `frontend/src/` | Enforced |
| Frontend e2e | Playwright | `frontend/e2e/` | Enforced |

### Local quality checks

```bash
uv run ruff check .               # lint
uv run ruff format --check .      # format
uv run pytest -v                  # tests + coverage
uv run pre-commit run --all-files # all hooks
npm --prefix frontend run lint    # frontend lint
npm --prefix frontend run typecheck  # frontend types
npm --prefix frontend run test    # frontend tests
npm --prefix frontend run test:e2e # playwright e2e tests
```

## Coverage targets

- **Backend:** Track via Codecov; aim for increasing coverage each PR
- **Frontend:** Track via Codecov (separate flag); aim for component coverage

## Testing conventions

- Tests live in `tests/` (flat structure, `test_*.py` naming)
- Use `pytest` fixtures (`tmp_path`, `monkeypatch`) over manual setup/teardown
- Mock external services (GitHub API, AI providers) in unit tests
- Integration tests are opt-in (`HELPING_HANDS_RUN_E2E_INTEGRATION=1`)

## Ruff configuration

Rules enabled: `E, W, F, I, N, UP, B, SIM, RUF`
Line length: 88
Target Python: 3.12+

## Per-module coverage targets

| Module | Current state | Target | Notes |
|---|---|---|---|
| Top-level packages | Excellent (100%) | Maintained | `helping_hands`, `lib`, `cli`, `server`, `hands` package import/docstring tests added in v67 |
| `lib/config.py` | Excellent (32 tests, all paths) | Maintained | Normalization edge cases added in v5; `_load_env_files` no-dotenv early return, bool tool/skill override normalization added in v30; refactored to monkeypatch, frozen immutability, repo dotenv loading, verbose truthy/falsy/override tests added in v66; `_load_env_files` non-dir repo path, config_path field, string/tuple tool/skill overrides, from_env precedence tests added in v101 |
| `lib/repo.py` | Excellent (12 tests, all paths) | Maintained | No gaps identified |
| `lib/meta/tools/filesystem.py` | Excellent (37+ tests, security) | Maintained | Edge cases added in v5; differentiated error messages (empty/absolute vs traversal) and 6 new `TestResolveRepoTargetErrorMessages` tests added in v112; `max_file_size` parameter with `stat()` pre-check (reject/accept/boundary/error-message/default-constant, 6 tests) added in v118 |
| `lib/meta/tools/command.py` | Good (execution paths) | 90%+ | `CommandResult`, `_normalize_args`, `_resolve_cwd` added in v5; `FileNotFoundError` (exit 127) and `OSError` (exit 126) handling in `_run_command`, 5 new `TestRunCommandExecErrors` tests added in v112 |
| `lib/hands/v1/hand/model_provider.py` | Excellent (100%) | Maintained | `_infer_provider_name`, `HandModel` added in v5; unrecognized provider/model slash fallthrough added in v60 |
| `lib/ai_providers/` | Good (init, normalize, complete, _build_inner) | 85%+ | `_build_inner` ImportError + env var paths added in v11; dedicated `test_litellm_provider.py` added in v98; dedicated `test_anthropic_provider.py` and enhanced `test_google_provider.py` (class attrs, `_build_inner` with/without API key, `_complete_impl` max_tokens default/custom, singleton identity) added in v99 |
| `lib/hands/v1/hand/base.py` | Excellent (99% coverage) | Maintained | `_default_base_branch`, `_build_generic_pr_body` added in v8; `_run_git_read` success/failure, `_finalize_repo_pr` early returns (no_repo/not_git_repo/no_changes/disabled/no_github_origin) added in v29; `_push_to_existing_pr` whoami exception, `_finalize_repo_pr` precommit-no-changes and get_repo default_branch exception added in v36; `_finalize_repo_pr` native git auth skip and rich PR description title/body paths added in v44; `_run_precommit_checks_and_fixes` stdout-only (no stderr) branch, `_finalize_repo_pr` pr_number delegation and empty default_branch fallback added in v53; `_update_pr_description` exception logging (replaced silent suppress) added in v115; `assert`→`ValueError` guards for `pr_number` in `_push_to_existing_pr`/`_update_pr_description`/`_create_pr_for_diverged_branch` (4 tests) added in v119 |
| `lib/hands/v1/hand/cli/` | Good (dedicated test files per backend) | 80%+ | Claude/Codex/Gemini/OpenCode/Goose helpers well-tested; subprocess paths harder |
| `lib/github.py` | Excellent (18 test classes) | Maintained | `fetch_branch`, `pull` with branch, `set_local_identity`, mixed conclusion, `upsert_pr_comment` body-already-has-marker and None-body-comment added in v36 |
| `server/app.py` | Good (23 test classes) | 85%+ | Health check and config helper edge cases added in v35; `/health`, `/health/services` (all-healthy/redis-error/db-na/workers-error/all-degraded), `_check_redis_health` (ok/error/import-fail), `_check_db_health` (no-url/empty/ok/error), `_check_workers_health` (ok/no-workers/empty/inspect-raises), `/health/claude-usage` (no-creds/cached/force-bypass) added in v107; direct `_get_claude_oauth_token` (JSON token/missing key/plain JWT/non-JWT/nonzero exit/empty stdout/timeout/exception/malformed JSON), `_fetch_claude_usage` (cache hit/expired/no token/HTTP error/generic exception/session+weekly/no data/force bypass/session only/cache update), `_schedule_to_response` (enabled/disabled/all fields/exception suppressed/missing attrs/run count) added in v108; `_build_form_redirect_query` (9 tests: required fields, all optional, false booleans, defaults, none values, empty/whitespace tools/skills), `_fetch_claude_usage` error body logging, `_schedule_to_response` next-run logging added in v115; `_check_redis_health`/`_check_db_health`/`_check_workers_health`/`_resolve_worker_capacity`/`_get_claude_oauth_token` debug logging on exceptions added in v116; `_ToolSkillValidatorMixin` extraction with `max_length` constraints on tools/skills (8+5 tests), mixin inheritance verification added in v119 |
| `lib/hands/v1/hand/iterative.py` | Excellent (97% -> 98%+) | Maintained | `_build_tree_snapshot`, `_read_bootstrap_doc`, `_build_bootstrap_context`, `_apply_inline_edits` added in v9; `_build_tree_snapshot` empty-normalized and slash-only edge cases added in v42; `_build_tree_snapshot` slash-only and multi-slash empty-parts tests added in v51; `BasicLangGraphHand._build_agent` and `BasicAtomicHand._build_agent` (mocked imports, agent construction verification) added in v56 |
| `lib/ai_providers/ollama.py` | Excellent (env vars, ImportError, kwargs, singleton) | Maintained | `_build_inner`, `_complete_impl` added in v10; `api_key_env_var`/`base_url_env_var`/`default_base_url` class attrs, `_complete_impl` delegation without kwargs, singleton identity tests added in v102 |
| `lib/hands/v1/hand/e2e.py` | Excellent (99% coverage) | Maintained | `_safe_repo_dir`, `_work_base`, `_configured_base_branch`, `_build_e2e_pr_comment`, `_build_e2e_pr_body` added in v10; `run()` (dry-run/fresh-PR/resumed-PR/empty-repo/configured-base-branch/default-branch-fallback/auto-uuid), `stream()` yield added in v34; `current_branch` falsy detection path (clone_branch=None, detected="") added in v59; `final_pr_number is None` dead code documented in tech-debt-tracker in v59; `_draft_pr_enabled` (default/env true/false/yes/no/0), `create_pr` draft=True/False integration added in v105 |
| `server/celery_app.py` | Excellent (99% coverage) | Maintained | `_github_clone_url`, `_redact_sensitive`, `_repo_tmp_dir`, `_trim_updates`, `_append_update`, `_UpdateCollector` added in v10; `_resolve_celery_urls` all-defaults, `_resolve_repo_path` (local dir/invalid format/clone failure/PR number), `_normalize_backend` (whitespace/case/extra backends), `_has_gemini_auth` (empty/whitespace), `_update_progress` (callable/non-callable/workspace), `_collect_stream` added in v28; `_has_codex_auth` (env var set/auth file exists/neither/empty key) added in v35; `_get_db_url_writer` (env override/default), `ensure_usage_schedule` (already-exists/create/import error), `log_claude_usage` (keychain failure/no token/API HTTP error/API generic error/DB write failure/success/raw JWT/non-JWT garbage) added in v43; `_format_runtime` extraction, `_UpdateCollector` edge cases (multi-newline/split-line/buffer-boundary/whitespace/truncation), `_collect_stream` edge cases (empty/workspace/periodic), `_has_gemini_auth` in helpers added in v106; `ensure_usage_schedule` debug logging on entry-not-found and outer failure added in v116 |
| `server/schedules.py` | Good (ScheduledTask, cron validation, ScheduleManager CRUD, trigger_now) | 93%+ | `_check_redbeat`, `_check_croniter`, ScheduleManager with mocked Redis added in v11; `create_schedule` enabled/disabled branches, `update_schedule` enabled/disabled branches, `_create_redbeat_entry` invalid cron validation, `_delete_redbeat_entry` KeyError handling, `list_schedules` None filtering, `trigger_now` (happy path/missing/param forwarding), `get_schedule_manager`, `ScheduledTask` fix_ci/ci_check_wait_minutes roundtrip added in v33 |
| `lib/meta/tools/registry.py` (runners) | Excellent (100%) | Maintained | `_run_python_code`, `_run_python_script`, `_run_bash_script`, `_run_web_search`, `_run_web_browse` added in v12; `normalize_tool_selection` non-string ValueError, `format_tool_instructions_for_cli` tool-without-guidance skip added in v53; `normalize_tool_selection` empty inner comma-split token skip added in v60 |
| `server/mcp_server.py` | Excellent (14 test classes, 98%) | Maintained | `_repo_root`, `_command_result_to_dict`, error paths (IsADirectory, Unicode, path traversal) added in v12; fixed UnicodeError handler ordering; `main()` stdio/http transport selection added in v49 |
| `server/app.py` (helpers) | Good (18 test classes) | 85%+ | `_parse_backend`, `_task_state_priority`, `_normalize_task_status`, `_extract_task_id/name/kwargs`, `_coerce_optional_str`, `_parse_task_kwargs_str`, `_is_helping_hands_task`, `_upsert_current_task`, `_flower_timeout_seconds`, `_flower_api_base_url` added in v13; `_check_redis_health` (ok/error), `_check_db_health` (na/ok/error), `_check_workers_health` (ok/none/empty/exception), `_is_running_in_docker` (dockerenv/env var/neither), `_iter_worker_task_entries` (valid/non-dict/non-list/non-dict entries), `_safe_inspect_call` (success/missing/exception) added in v35 |
| `lib/hands/v1/hand/cli/base.py` (CI/PR) | Good (6 test classes) | 80%+ | `_build_ci_fix_prompt`, `_format_ci_fix_message`, `_format_pr_status_message`, `_looks_like_edit_request` added in v13; `stream()` CI fix message yield, pr_status_message=None skip, CI fix exhausted message added in v44 |
| `cli/main.py` (helpers) | Good (8 test classes) | 95%+ | `_github_clone_url`, `_git_noninteractive_env`, `_redact_sensitive`, `_repo_tmp_dir`, `opencodecli` backend, `model_not_found` error, invalid `--tools` added in v14; `docker-sandbox-claude` backend branch, `_stream_hand` (chunk printing/trailing newline), Python <3.12 atomic error, generic exception re-raise for non-CLI backends, `__version__` accessibility added in v41 |
| `lib/hands/v1/hand/model_provider.py` (builders) | Excellent (99% coverage) | Maintained | `build_langchain_chat_model` (all 5 providers + ImportError paths + env vars), `build_atomic_client` (OpenAI, LiteLLM + missing attr, unsupported) added in v15 |
| `lib/ai_providers/openai.py` | Good (dedicated tests) | 85%+ | `_build_inner` (ImportError, with/without API key), `_complete_impl` (delegation, kwargs) added in v16 |
| `lib/ai_providers/google.py` | Good (build_inner + complete_impl) | 85%+ | `_complete_impl` (delegation, empty content filtering, kwargs) added in v16; `_build_inner` covered in v11 |
| `lib/hands/v1/hand/cli/claude.py` | Excellent (99% coverage) | Maintained | `_command_not_found_message`, `_native_cli_auth_env_names`, `_pr_description_cmd` added in v16; _StreamJsonEmitter edge cases (empty text/result/tool_result, non-dict items, unknown events, multi-line chunks, empty flush), `_invoke_claude`/`_invoke_backend` async tests added in v31; `_skip_permissions_enabled` geteuid exception path, `_build_failure_message` instance delegation, `_StreamJsonEmitter` empty-line-between-events added in v48; `_StreamJsonEmitter` unknown content block type skip, whitespace-only text preview skip, `_skip_permissions_enabled` geteuid-not-callable path added in v58; `_StreamJsonEmitter` non-dict message defense (assistant/user string/null/missing/list), `_summarize_tool` direct tests (all 7 tools + unknown + missing fields), `_invoke_claude` flush-on-error (`try/finally`), `_inject_output_format` empty cmd added in v110; `_summarize_tool` expanded with Agent/WebFetch/WebSearch/NotebookEdit/TodoWrite/MultiTool (11 new tests), result event token usage tracking (6 new tests) added in v113 |
| `lib/hands/v1/hand/cli/base.py` (helpers) | Good (74% coverage) | 80%+ | `_resolve_cli_model`, `_inject_prompt_argument`, `_normalize_base_command`, `_build_failure_message`, `_describe_auth`, `_effective_container_env_names`, `_build_subprocess_env`, `_interrupted_pr_metadata` added in v17; `_effective_container_env_names` empty-blocked early return added in v42; `_apply_verbose_flags` empty-cmd guard, `_build_failure_message` `_SUMMARY_CHAR_LIMIT` consistency (3+4 tests) added in v117 |
| `lib/ai_providers/anthropic.py` | Good (complete_impl kwargs) | 85%+ | Extra kwargs forwarding test added in v17 |
| `lib/ai_providers/litellm.py` | Good (complete_impl kwargs) | 85%+ | Extra kwargs forwarding test added in v17 |
| `lib/hands/v1/hand/base.py` (statics) | Excellent (83% -> 95%) | Maintained | `_github_repo_from_origin` edge cases, `_run_precommit_checks_and_fixes`, `_push_noninteractive`, `_push_to_existing_pr`, `_should_run_precommit_before_pr`, `_finalize_repo_pr` error paths added in v18; `_update_pr_description` (rich/fallback/exception-suppressed), `_create_pr_for_diverged_branch` (rich/fallback/follow-up link) added in v26; `_run_git_read` success/failure, `_finalize_repo_pr` early returns (no_repo/not_git_repo/no_changes/disabled/no_github_origin) added in v29 |
| `lib/meta/tools/command.py` | Good (73% -> 85%+) | 85%+ | `_resolve_python_command` (uv/direct/neither/empty), `_run_command` timeout (with/without output, zero/negative), `run_python_code` empty validation, `run_python_script` (missing/directory), `run_bash_script` (missing/directory/empty-both/inline-args) added in v18 |
| `lib/hands/v1/hand/cli/base.py` (prompts/container) | Good (78% -> 92%+) | 90%+ | `_execution_mode`, `_container_enabled` (env var truthy/falsy/missing/empty/no-config), `_container_image` (set/missing/empty/no-config/whitespace), `_apply_verbose_flags` (on/off/duplicates/no-flags), `_build_init_prompt` (repo root, file list, cap at 200, empty list), `_build_task_prompt` (prompt, summary, truncation, placeholder), `_build_apply_changes_prompt` (prompt, output, truncation, placeholder) added in v19; `_stage_skill_catalog` (stages to temp dir/no-op), `_cleanup_skill_catalog` (removes dir/no-op), `_wrap_container_if_enabled` (disabled/docker-not-found/env-forwarding/mount), `_build_task_prompt` tool/skill sections (include/omit branches) added in v29 |
| `lib/hands/v1/hand/cli/base.py` (render/finalize) | Good (80%+) | 85%+ | `_render_command` base-level tests (placeholder/model/prompt/verbose/repo/multi-placeholder), `_finalize_after_run` direct tests (interrupted/normal/truncated/auto-pr), `_collect_run_output` (collects/empty/newlines), `_truncate_summary` (stripped/exact/over/empty/whitespace) added in v114 |
| `lib/hands/v1/hand/cli/base.py` (command/timing) | Good (75% -> 76%+) | 80%+ | `_base_command` (default/env override/empty raises), `_io_poll_seconds`/`_heartbeat_seconds`/`_idle_timeout_seconds` (defaults/env overrides/verbose heartbeat), `_repo_has_changes` (with changes/clean/non-git) added in v20; `_base_command` shlex.split error wrapping (unclosed quote/raw value in message/valid passthrough) added in v115; `_float_env` warning logging (non-numeric/zero/negative/valid/unset, 5 tests) added in v118 |
| `lib/hands/v1/hand/cli/base.py` (retry/interrupt) | Good (76% -> 77%+) | 80%+ | `_should_retry_without_changes` (all 4 branches: feature off, interrupted, not edit, has changes), `_no_change_error_after_retries` (base returns None), `_build_apply_changes_prompt` (prompt+output, empty output, truncation), `_terminate_active_process` (None/exited/terminate/kill-on-timeout), `interrupt()` (active/None/exited) added in v22 |
| `lib/hands/v1/hand/iterative.py` (iteration helpers) | Excellent (79% -> 97%+) | Maintained | `_build_iteration_prompt`, `_execution_tools_enabled`/`_web_tools_enabled`, `_tool_instructions`, `BasicLangGraphHand._result_content`, `BasicAtomicHand._extract_message` added in v20; `_execute_read_requests` error paths, `_run_tool_request` dispatch, `_execute_tool_requests` error pass-through added in v21; `BasicLangGraphHand.stream()` (satisfied/max-iter/interrupt/file-changes/PR metadata) and `BasicAtomicHand.stream()` (satisfied/max-iter/interrupt/assertion-fallback/awaitable/PR metadata) added in v26; `BasicAtomicHand.run()` interrupted/max_iterations status paths added in v39; `BasicAtomicHand.stream()` delta-without-prefix (assertion-fallback/async-iter/awaitable paths), file-change yield, tool-result yield added in v40; `BasicLangGraphHand.run()` max_iterations status path, LangGraph/Atomic `stream()` pr_url yield at max iterations added in v42; LangGraph/Atomic `stream()` pr_status elif branches (entered/skipped for both satisfied and max-iterations paths), LangGraph interrupted inner loop, non-chat-model event skip, empty text skip, Atomic async-iter duplicate delta skip, awaitable empty delta skip added in v45; Atomic `stream()` `run_async` non-AssertionError exception re-raise (except Exception path) added in v46 |

| `lib/hands/v1/hand/atomic.py` | Excellent (78% -> 98%+) | Maintained | `_build_agent` (mocked atomic_agents imports), `run()` (HandResponse structure, PR metadata), `stream()` (AssertionError sync fallback, async iterator path, awaitable path, awaitable AssertionError fallback, PR URL yield), `_make_input` added in v23; `stream()` chat_message falsy branches (assertion fallback/async iter/awaitable paths) added in v45; `stream()` non-AssertionError exception propagation (`except Exception: raise` path) added in v57 |
| `lib/hands/v1/hand/langgraph.py` | Good (83% -> 95%+) | 90%+ | `_build_agent` (mocked create_react_agent), `run()` (HandResponse, str fallback, PR metadata), `stream()` (chunk extraction, non-chat event filtering, empty content skip, PR URL yield) added in v23; `run()` defensive message access (empty/missing/None messages, 5 tests) added in v117 |
| `lib/hands/v1/hand/cli/goose.py` | Excellent (91% -> 98%) | Maintained | `_describe_auth` (all providers, key set/not set), `_normalize_base_command` (bare goose/goose run/goose run --instructions/passthrough), `_pr_description_cmd` (anthropic with/without claude, non-anthropic), `_has_goose_builtin_flag`, `_apply_backend_defaults`, `_resolve_ollama_host` added in v23; `_resolve_goose_provider_model_from_config` (bare model/provider-slash/default/empty/gemini-normalized/gpt-inferred), `_invoke_backend` delegation added in v48; refactored to shared `make_cli_hand` fixture in v64 |
| `lib/hands/v1/hand/cli/base.py` (CI fix loop) | Good (78% -> 80%+) | 80%+ | `_ci_fix_loop` (all early-return paths, success/no_checks/pending/failure conclusions, fix-with-changes-then-success, no-changes-exhausted, interrupt before/after poll, exception error status), `_poll_ci_checks` (immediate return, poll-until-deadline), `run()` (collect+finalize, CI fix triggered, CI fix skipped when no PR, CI fix noop emit callable added in v49), `stream()` (chunk yielding, PR status message, CI fix integration) added in v24; `stream()` consumer-break producer_task cancellation path added in v51 |
| `lib/hands/v1/hand/cli/base.py` (subprocess invoke) | Good (92% -> 95%+) | 95%+ | `_invoke_cli_with_cmd` FileNotFoundError (no fallback/with fallback/npx fallback/same-as-original raises), stdout-None, non-zero exit (no retry/with retry/same-as-original raises), idle timeout termination, verbose mode (cmd+cwd+finished emitted) added in v39; `_invoke_backend` delegation, `_run_two_phase` skill catalog staging/cleanup (including cleanup on exception) added in v40; `_run_two_phase_inner` verbose mode (model/heartbeat/phase timings), auth display (empty/non-empty), default model fallback added in v44; `_invoke_cli` delegation to `_invoke_cli_with_cmd`, `stream()` producer error re-raise added in v46; IO loop interrupt break and process-exited-during-timeout break added in v49 |
| `lib/meta/tools/web.py` | Excellent (99% coverage) | Maintained | `_extract_related_topics` (text+url extraction, recursive Topics, non-dict/missing/empty skips), `_require_http_url` (whitespace-only, no-scheme), `_strip_html` (noscript, blank line collapsing), `search_web` (invalid params, unexpected format, dedup, empty URLs, max_results cap), `browse_url` (invalid params, non-HTML content, HTML-by-body-detection) added in v24; `search_web` RelatedTopics non-list skip added in v53; dataclass construction tests (WebSearchItem/WebSearchResult/WebBrowseResult frozen immutability, equality, hashability), `_decode_bytes` edge cases (empty bytes, UTF-8 BOM, multibyte, UTF-16 BOM, latin-1 high bytes) added in v65 |
| `lib/hands/v1/hand/cli/gemini.py` | Excellent (98% -> 99%) | Maintained | `_describe_auth` (key set/not set/empty), `_pr_description_cmd` (gemini found/not found), `_command_not_found_message` added in v25; `_build_failure_message` instance delegation, `_invoke_gemini`/`_invoke_backend` async delegation added in v48 |
| `lib/hands/v1/hand/cli/codex.py` | Excellent (92% -> 98%) | Maintained | `_command_not_found_message`, `_native_cli_auth_env_names`, `_apply_codex_exec_sandbox_defaults` (empty/whitespace env override fallback) added in v25; `_build_failure_message` delegation, `_invoke_codex`/`_invoke_backend` async delegation added in v46 |
| `lib/hands/v1/hand/cli/opencode.py` | Excellent (91% -> 100%) | Maintained | `_build_failure_message` delegation, `_build_opencode_failure_message` additional auth token variations (case-insensitive, non-auth distinction, exit code) added in v26; `_invoke_opencode`/`_invoke_backend` async delegation added in v46 |
| `lib/hands/v1/hand/cli/docker_sandbox_claude.py` | Excellent (100%) | Maintained | Class attrs, `_resolve_sandbox_name` (env override/auto-gen/cache/sanitize), `_should_cleanup` (truthy/falsy), `_wrap_sandbox_exec` (wrapping/env forwarding), `_execution_mode`, `_build_failure_message` (auth/generic/sandbox note), `_command_not_found_message`, `_fallback_command_when_not_found`, `_docker_sandbox_available` (success/fail/FileNotFoundError), `_ensure_sandbox` (skip/docker-not-found/plugin-unavailable/success/failure/template), `_remove_sandbox` (skip/stop+rm) added in v27; `_invoke_claude` (sandbox-wrapped cmd/result/raw fallback), `_run_two_phase` (ensure+cleanup/skip-cleanup/cleanup-on-exception) added in v40; `_ensure_sandbox` verbose=False branch, `_build_failure_message` sandbox-already-in-base skip added in v60 |
| `lib/meta/skills/__init__.py` | Excellent (100%) | Maintained | `normalize_skill_selection` non-string ValueError, `stage_skill_catalog` missing .md skip added in v30; `_discover_catalog` returns empty when catalog dir missing added in v46; `normalize_skill_selection` whitespace-only token skip added in v58; `_discover_catalog` no-heading title fallback (branches 42->47, 44->42) added in v60 |
| `lib/hands/v1/hand/pr_description.py` | Excellent (100+ tests, all paths) | Maintained | `_diff_char_limit` negative, `_get_diff` empty stdout on success, `_build_prompt`/`_build_commit_message_prompt` summary truncation, `_parse_output` whitespace-only body, `_commit_message_from_prompt` whitespace edge cases added in v32; `_commit_message_from_prompt` punctuation sentence boundaries (exclamation/question), prefix stripping (fix/refactor/docs/chore/test/ci/case-insensitive/parenthetical scope), single word, trailing period removal; `_parse_output` inline body marker, title whitespace, body whitespace/internal blanks, late title; `_build_prompt`/`_build_commit_message_prompt` whitespace-only summary, conventional commit/72-char/single-line guidance validation added in v100 |
| `lib/hands/v1/hand/placeholders.py` | Excellent (100%) | Maintained | Backward-compat shim re-export verification (class symbols, module aliases, `__all__`, identity checks) added in v34 |
| `lib/hands/v1/hand/__init__.py` | Excellent (100%) | Maintained | Package re-export tests (`__all__` completeness, symbol identity, subprocess alias) added in v37 |
| `lib/ai_providers/__init__.py` | Excellent (100%) | Maintained | PROVIDERS dict completeness, singleton identity, `__all__` verification added in v37 |
| `lib/default_prompts.py` | Excellent (100%) | Maintained | Constant content assertions (directives, guards, references) covered in test_default_prompts.py; structural validation (numbered steps, directive uniqueness, guard ordering, safety note, script references, python version, reasonable length) added in v100 |
| `lib/hands/v1/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness, symbol identity with hand package, all 10 re-exports verified added in v38 |
| `lib/hands/v1/hand/cli/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness (7 symbols), identity checks against source modules added in v38 |
| `lib/meta/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness (`skills`, `tools`), submodule identity added in v38 |
| `lib/meta/tools/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness (21 symbols), identity checks for all 4 submodules (filesystem, command, registry, web) added in v38 |
| `lib/ai_providers/types.py` | Excellent (100%) | Maintained | `normalize_messages` (string/sequence/empty/missing keys), `AIProvider` base (lazy init, complete, acomplete, model override) covered in test_ai_providers.py |
| `server/task_result.py` | Excellent (100%) | Maintained | All branches of `normalize_task_result` (None/dict/exception/other/bool/list/custom) covered in test_task_result.py |
| `frontend/src/App.tsx` (utils) | Excellent (93 tests) | 80%+ | `shortTaskId`, `statusTone`, `parseBool`, `extractUpdates`, `parseOptimisticUpdates`, `parseError` (detail/no-detail/non-JSON/empty body), `isTerminalTaskStatus` (terminal/non-terminal/whitespace), `apiUrl`, `loadTaskHistory`, `upsertTaskHistory` added in v47; `backendDisplayName`, `providerFromBackend` (all 10 backends), `formatProviderName` (openai/opencode/e2e/generic), `repoName` (slash/trailing-slash/no-slash), `cronFrequency` (9 presets + fallbacks), `buildDeskSlots` (count/ids/layout), `checkDeskCollision` (overlap/far/empty), `asRecord` (object/null/primitives), `readStringValue` (string/number/null/non-finite), `readBoolishValue` (bool/string/non-bool), `readSkillsValue` (array/empty/non-array), `parseOptimisticUpdates` edge cases (bold/exec/mcp/failed exec/long lines/dedup/flush at EOF) added in v50; `statusTone` additional statuses (RECEIVED/RETRY/SCHEDULED/RESERVED/SENT/ERROR), `cronFrequency` fallback patterns (hourly/minute-interval/empty), `loadTaskHistory` edge cases (invalid JSON/non-array/empty taskId/limit enforcement), `upsertTaskHistory` (empty/whitespace taskId, default values) added in v52 |
| `frontend/src/App.tsx` (component) | Excellent (60 tests, 82.3% stmts) | Maintained | Smoke render test, default form values (repo path, prompt), empty task list, service health bar, dashboard view toggles, navigation buttons, submitted tasks header added in v50; interaction tests (Hand world/Classic view toggle, schedule navigation, New submission return, Advanced settings expand, repo path/prompt/backend input changes, Clear button disabled state) added in v52; form submission tests (POST payload validation, error handling via network/server errors, model/tools/skills inclusion, checkbox toggles, max iterations), monitor view tests (output tab switching Updates/Raw/Payload, task ID badge, status blinker, task inputs section), schedule view tests (form rendering, field changes, cron preset dropdown, schedule creation API, Cancel button, Refresh, error handling) added in v54; schedule CRUD tests (edit/delete/trigger/toggle), task discovery polling, notification banner/toast UI, monitor resize/scroll, poll error handling added in v55 |

## Remaining coverage gaps

All remaining uncovered lines are inherently untestable entry point guards or
defensive fallbacks.  See `docs/exec-plans/tech-debt-tracker.md` for details.

Dead code in `codex.py`, `goose.py`, `e2e.py`, and `iterative.py` was removed
in v104 (2026-03-10).

| Module | Line(s) | Reason |
|---|---|---|
| `cli/main.py` | 367 | `if __name__` guard |
| `cli/base.py` | 552->559 | Heartbeat-without-timeout timing |
| `web.py` | 66 | Defensive fallback (`pragma: no cover`; latin-1 accepts all bytes) |
| `mcp_server.py` | 393 | `if __name__` guard |

## Areas for improvement

- [x] Add type checking to CI (ty, when stable for CI runners) — added in v109
- [ ] Add mutation testing for critical path safety (filesystem tools)
- [ ] Add load testing for app mode concurrent task handling
- [x] Add tests for `_build_tree_snapshot` / `_build_bootstrap_context` (iterative.py) — added in v9
- [x] Add tests for `_read_bootstrap_doc` / `_apply_inline_edits` (iterative.py, needs tmp_path) — added in v9
- [x] Increase coverage for CLI hand CI fix loop and run/stream wrappers — added in v24
