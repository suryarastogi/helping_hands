# Quality Score

Metrics and standards for code quality in helping_hands.

## Current quality gates

### CI pipeline (GitHub Actions)

| Check | Tool | Scope | Status |
|---|---|---|---|
| Lint | `ruff check` | All Python | Enforced |
| Format | `ruff format --check` | All Python | Enforced |
| Type check | `ty check` | `src/` | Enforced |
| Tests | `pytest -v` | `tests/` | Enforced |
| Coverage | `pytest-cov` + Codecov | Python 3.12 job | Reporting |
| Frontend lint | `eslint` | `frontend/src/` | Enforced |
| Frontend types | `tsc --noEmit` | `frontend/src/` | Enforced |
| Frontend tests | Vitest | `frontend/src/` | Enforced |
| Frontend e2e | Playwright | `frontend/e2e/` | Enforced |

### Local quality checks

```bash
uv run ruff check .               # lint
uv run ruff format --check .      # format
uv run pytest -v                  # tests + coverage
uv run pre-commit run --all-files # all hooks
npm --prefix frontend run lint    # frontend lint
npm --prefix frontend run typecheck  # frontend types
npm --prefix frontend run test    # frontend tests
npm --prefix frontend run test:e2e # playwright e2e tests
```

## Coverage targets

- **Backend:** Track via Codecov; aim for increasing coverage each PR
- **Frontend:** Track via Codecov (separate flag); aim for component coverage

## Testing conventions

- Tests live in `tests/` (flat structure, `test_*.py` naming)
- Use `pytest` fixtures (`tmp_path`, `monkeypatch`) over manual setup/teardown
- Mock external services (GitHub API, AI providers) in unit tests
- Integration tests are opt-in (`HELPING_HANDS_RUN_E2E_INTEGRATION=1`)

## Ruff configuration

Rules enabled: `E, W, F, I, N, UP, B, SIM, RUF`
Line length: 88
Target Python: 3.12+

## Per-module coverage targets

| Module | Current state | Target | Notes |
|---|---|---|---|
| Top-level packages | Excellent (100%) | Maintained | `__all__: list[str] = []` added to 4 namespace `__init__.py` files (`lib/`, `lib/hands/`, `server/`, `cli/`) completing project-wide explicit `__all__` coverage (8 tests: has-all + no-private for each) added in v178; `helping_hands`, `lib`, `cli`, `server`, `hands` package import/docstring tests added in v67 |
| `lib/github_url.py` | Excellent (100%) | Maintained | New shared module consolidating GitHub URL helpers (`build_clone_url`, `validate_repo_spec`, `redact_credentials`, `noninteractive_env`) and constants (`GITHUB_TOKEN_USER`, `GITHUB_HOSTNAME`). Replaces duplicated implementations in `cli/main.py`, `server/celery_app.py`, `lib/github.py`, `lib/hands/v1/hand/base.py`. `__all__` with 6 exports. 20 tests (constants, validation, URL building, redaction, env, consumer import consistency) added in v179 |
| `server/constants.py` | Excellent (100%) | Maintained | Field validation bound constants (`MAX_ITERATIONS_UPPER_BOUND`, `MIN_CI_WAIT_MINUTES`, `MAX_CI_WAIT_MINUTES`, `MAX_REPO_PATH_LENGTH`, `MAX_PROMPT_LENGTH`, `MAX_MODEL_LENGTH`, `MAX_GITHUB_TOKEN_LENGTH`) extracted from duplicated `Field()` definitions in `BuildRequest`/`ScheduleRequest` (10 value/type tests, 1 __all__ test, 14 field sync tests) added in v197; Build/schedule defaults (`DEFAULT_BACKEND`, `DEFAULT_MAX_ITERATIONS`, `DEFAULT_CI_WAIT_MINUTES`, `MAX_REFERENCE_REPOS`, `USAGE_CACHE_TTL_S`) added in v196; New shared server constants module consolidating Anthropic usage API (`ANTHROPIC_USAGE_URL`, `ANTHROPIC_BETA_HEADER`, `USAGE_USER_AGENT`) and Keychain (`KEYCHAIN_SERVICE_NAME`, `KEYCHAIN_OAUTH_KEY`, `KEYCHAIN_ACCESS_TOKEN_KEY`, `JWT_TOKEN_PREFIX`) constants. Replaces duplicated definitions in `server/app.py` and `server/celery_app.py`. `__all__` with 19 exports. 8 tests (constant values, `__all__` completeness) added in v179 |
| `lib/config.py` | Excellent (48 tests, all paths) | Maintained | Config dataclass `Attributes:` section documenting all 11 fields (docstring presence + per-field mention, 12 tests) added in v171; `__all__` export (contains Config, no private names, 2 tests), `from_env()` whitespace stripping for `repo`/`model`/`github_token` (padded→stripped, already-clean passthrough, 6 tests) added in v156; Normalization edge cases added in v5; `_load_env_files` no-dotenv early return, bool tool/skill override normalization added in v30; refactored to monkeypatch, frozen immutability, repo dotenv loading, verbose truthy/falsy/override tests added in v66; `_load_env_files` non-dir repo path, config_path field, string/tuple tool/skill overrides, from_env precedence tests added in v101; `_load_env_files` tilde expansion (expanduser before is_dir, 1 test) added in v134; `github_token` field (default, override, env var, override beats env, none preserves env, no env no override, frozen immutability, 7 tests) added in v147; `reference_repos` non-str/non-list/tuple type fallback to `()` (int/dict/set/bool/None, 5 tests) added in v172 |
| `lib/repo.py` | Excellent (19 tests, all paths) | Maintained | `__all__` export (contains RepoIndex, no private names, importable, 4 tests) added in v158; `from_path()` PermissionError handling during `rglob()` with warning log and graceful empty-files return (3 tests) added in v142 |
| `lib/meta/tools/filesystem.py` | Excellent (53+ tests, security) | Maintained | `_BYTES_PER_MB = 1024 * 1024` conversion constant extracted, `_MAX_FILE_SIZE_BYTES` now uses `_BYTES_PER_MB` (4 tests) added in v197; `normalize_relative_path` empty/whitespace `ValueError` guard (empty/whitespace/tab rejection, valid path acceptance, single-char acceptance, 5 tests) added in v158; `__all__` export (6 public functions, no private names, importable, 8 tests) added in v157; `read_text_file()` `max_file_size` positive validation — rejects zero/negative with `ValueError` before file access (zero/negative/large-negative/error-message/positive/small-positive/validation-before-access, 7 tests) added in v155; `normalize_relative_path` non-string `TypeError` guard (None/int/list/dict rejection, valid string acceptance, source verification, 6 tests) added in v151; Edge cases added in v5; differentiated error messages (empty/absolute vs traversal) and 6 new `TestResolveRepoTargetErrorMessages` tests added in v112; `max_file_size` parameter with `stat()` pre-check (reject/accept/boundary/error-message/default-constant, 6 tests) added in v118; `repo_root` is-directory validation in `resolve_repo_target()` (3 tests) added in v121; `write_text_file` OSError wrapping in `RuntimeError` with file path context (permission denied/disk full, 2 tests) added in v133 |
| `lib/meta/tools/command.py` | Good (execution paths) | 90%+ | `_DEFAULT_SCRIPT_TIMEOUT_S` module-level constant extracted from 3× hardcoded `timeout_s: int = 60` defaults in `run_python_code`/`run_python_script`/`run_bash_script` (6 tests: value/type/positive/3 signature defaults) added in v194; Google-style docstrings added to 4 private helpers (`_normalize_args`, `_resolve_cwd`, `_resolve_python_command`, `_run_command`) with docstring presence and section tests (8 tests) added in v175; `__all__` export (CommandResult + 3 public functions, no private names, importable, 6 tests) added in v157; `CommandResult`, `_normalize_args`, `_resolve_cwd` added in v5; `FileNotFoundError` (exit 127) and `OSError` (exit 126) handling in `_run_command`, 5 new `TestRunCommandExecErrors` tests added in v112; `assert`→`RuntimeError` guards for `script_path`/`inline_script` type narrowing in `run_bash_script` (2 tests) added in v122 |
| `lib/hands/v1/hand/model_provider.py` | Excellent (100%) | Maintained | `_DEFAULT_OLLAMA_BASE_URL` and `_DEFAULT_OLLAMA_API_KEY` module-level constants extracted from hardcoded fallback values in `build_langchain_chat_model()` (5 tests: value/type/api-key/ollama-provider-sync) added in v178; `__all__` export (HandModel, build_atomic_client, build_langchain_chat_model, resolve_hand_model; no private names, importable, 7 tests) added in v159; `_infer_provider_name`, `HandModel` added in v5; unrecognized provider/model slash fallthrough added in v60 |
| `lib/ai_providers/` | Good (init, normalize, complete, _build_inner) | 85%+ | Google-style docstrings added to `_build_inner` and `_complete_impl` in all 5 providers (AnthropicProvider, OpenAIProvider, GoogleProvider, LiteLLMProvider, OllamaProvider) with docstring presence/non-trivial/section tests (30 tests) added in v176; `__all__` exports added to all 5 provider modules (openai, anthropic, google, litellm, ollama — 2 symbols each: ProviderClass + PROVIDER_INSTANCE; 5×5=25 tests) added in v159; Google `_complete_impl()` KeyError defense — `m["content"]` → `m.get("content")` to handle messages missing "content" key (missing-key/mixed-valid/none-content/empty-string, 4 tests) added in v155; `_build_inner` ImportError + env var paths added in v11; dedicated `test_litellm_provider.py` added in v98; dedicated `test_anthropic_provider.py` and enhanced `test_google_provider.py` (class attrs, `_build_inner` with/without API key, `_complete_impl` max_tokens default/custom, singleton identity) added in v99; `normalize_messages` non-Mapping rejection (4 tests), `AIProvider.complete()` empty-model guard (3 tests) added in v127 |
| `lib/hands/v1/hand/base.py` | Excellent (99% coverage) | Maintained | PR status sentinel constants (`_PR_STATUS_CREATED/UPDATED/NO_CHANGES/DISABLED/NOT_ATTEMPTED`, `_PR_STATUSES_WITH_URL`, `_PR_STATUSES_SKIPPED`) extracted from ~17 scattered string literals across base.py/iterative.py/cli/base.py (12 value/type/unique tests, 4 frozenset tests, 6 cross-module identity tests) added in v194; `_PRECOMMIT_UV_MISSING_MSG` constant extracted from 2 duplicate pre-commit error messages (5 tests: type/nonempty/uv/pre-commit/finalization + 2 usage tests: first-pass/second-pass FileNotFoundError), `_DEFAULT_GIT_ERROR_MSG` constant extracted from inline "unknown git error" fallback (3 tests: type/nonempty/value + 1 usage test: whitespace stderr fallback) added in v182; `_GITHUB_TOKEN_USER` and `_GITHUB_HOSTNAME` module-level constants extracted from hardcoded strings in push URL, hostname check, and SCP regex (6 tests: value/type/no-scheme/https-origin/scp-origin/non-github) added in v178; Google-style docstrings added to 7 methods (`__init__`, `_build_system_prompt`, `_build_reference_repos_prompt_section`, `interrupt`, `reset_interrupt`, `_use_native_git_auth_for_push`, `_push_noninteractive`) with docstring presence/non-trivial/section tests (19 tests) added in v176; `__all__` export (Hand, HandResponse; no private names, importable, 5 tests) added in v161; `_build_generic_pr_body` empty/whitespace backend/prompt validation (4 tests: empty backend, whitespace backend, empty prompt, whitespace prompt), `_is_git_hook_failure` edge case tests (3 tests: empty string, whitespace-only, plain text) added in v156; `_PRECOMMIT_TIMEOUT_S` constant (3 tests: value/type/positive), `_configure_authenticated_push_remote()` input validation (4 tests: empty/whitespace repo, empty/whitespace token), `_configure_authenticated_push_remote()` timeout handling (3 tests: raises RuntimeError/logs warning/passes timeout param), `_run_precommit_checks_and_fixes()` timeout (3 tests: param passed/first-pass propagates/second-pass propagates) added in v153; `_GIT_READ_TIMEOUT_S` constant (3 tests: value/type/positive), `_run_git_read()` timeout handling (3 tests: returns empty/logs warning/passes timeout param) added in v149; `config.github_token` passthrough to `GitHubClient(token=...)` in `_finalize_repo_pr` (4 tests: field exists, hand base reads, empty falls back, default empty) added in v147; Module-level constants (`_DEFAULT_BASE_BRANCH`, `_DEFAULT_GIT_USER_NAME`, `_DEFAULT_GIT_USER_EMAIL`, `_DEFAULT_CI_WAIT_MINUTES`, `_DEFAULT_CI_MAX_RETRIES`, `_BRANCH_PREFIX`, `_UUID_HEX_LENGTH`, `_MAX_OUTPUT_DISPLAY_LENGTH`, `_FILE_LIST_PREVIEW_LIMIT`, `_LOG_TRUNCATION_LENGTH`) extracted from hardcoded values (20 tests) added in v138; `_default_base_branch`, `_build_generic_pr_body` added in v8; `_run_git_read` success/failure, `_finalize_repo_pr` early returns (no_repo/not_git_repo/no_changes/disabled/no_github_origin) added in v29; `_push_to_existing_pr` whoami exception, `_finalize_repo_pr` precommit-no-changes and get_repo default_branch exception added in v36; `_finalize_repo_pr` native git auth skip and rich PR description title/body paths added in v44; `_run_precommit_checks_and_fixes` stdout-only (no stderr) branch, `_finalize_repo_pr` pr_number delegation and empty default_branch fallback added in v53; `_update_pr_description` exception logging (replaced silent suppress) added in v115; `assert`→`ValueError` guards for `pr_number` in `_push_to_existing_pr`/`_update_pr_description`/`_create_pr_for_diverged_branch` (4 tests) added in v119; debug logging for whoami/get_repo exception handlers (2 tests) added in v121; `_build_reference_repos_prompt_section` PermissionError handling (shows "(permission denied)" fallback, 1 test) and normal file listing (1 test) added in v172 |
| `lib/hands/v1/hand/cli/` | Good (dedicated test files per backend) | 85%+ | DRY `_AUTH_ERROR_TOKENS` shared constant extracted to `cli/base.py` (9 value/type tests, 4 cross-module identity tests, 7 auth detection tests), `ClaudeCodeHand._EXTRA_AUTH_TOKENS` for backend-specific tokens, refactored codex/gemini/opencode to import from base added in v193; Google-style docstring added to `_wrap_container_if_enabled` completing cli/base.py docstring coverage (6 tests: presence/Args/Returns/Raises/docker/RuntimeError) added in v181; Google-style docstrings added to 22 methods in `cli/base.py` (`__init__`, `_truncate_summary`, `_normalize_base_command`, `_base_command`, `_resolve_cli_model`, `_apply_backend_defaults`, `_render_command`, `_container_enabled`, `_container_image`, `_container_env_names`, `_use_native_cli_auth`, `_native_cli_auth_env_names`, `_effective_container_env_names`, `_execution_mode`, `_float_env`, `_io_poll_seconds`, `_heartbeat_seconds`, `_idle_timeout_seconds`, `_build_subprocess_env`, `_build_failure_message`, `_build_init_prompt`, `_build_task_prompt`, `_repo_has_changes`, `_looks_like_edit_request`, `_should_retry_without_changes`, `_build_apply_changes_prompt`, `_interrupted_pr_metadata`, `_finalize_after_run`, `_format_pr_status_message`, `_format_ci_fix_message`) with docstring presence/Args/Returns/Raises tests (83 tests) added in v168; `_FAILURE_OUTPUT_TAIL_LENGTH` consolidated to `cli/base.py` (single source of truth, 7 identity tests), `_CLI_TRUTHY_VALUES` constant harmonizing `_is_truthy` with config `_TRUTHY_VALUES` (9 tests) added in v166; `__all__` exports added to all 6 CLI hand modules (claude, codex, gemini, goose, opencode, docker_sandbox_claude — 1 public class each; 4×6=24 tests) in v161; Claude/Codex/Gemini/OpenCode/Goose helpers well-tested; subprocess paths harder; Google-style docstrings added to 4 `DockerSandboxClaudeCodeHand` methods (`__init__`, `_should_cleanup`, `_execution_mode`, `_fallback_command_when_not_found`) with docstring presence and section tests (8 tests) added in v175 |
| `lib/github.py` | Excellent (28 test classes) | Maintained | `_GITHUB_TOKEN_USER` module-level constant extracted from hardcoded `"x-access-token"` string in clone URL and redaction regex (4 tests: value/type/sync/redaction) added in v178; Google-style docstrings expanded on 4 public methods (`whoami`, `get_pr`, `default_branch`, `update_pr_body`) with Args/Returns/Raises sections; docstring presence/non-trivial/section tests (12 tests) added in v176; Google-style docstrings added to 3 dunder methods (`__post_init__`, `__enter__`, `__exit__`) with docstring presence and Raises section tests (4 tests) added in v175; `__all__` export (contains GitHubClient/PRResult, no private names, importable, 4 tests) added in v157; `clone()` full_name validation via `_validate_full_name()` (3 tests: empty/whitespace/no-slash rejection) added in v153; `create_pr` title/head/base validation (empty/whitespace rejection, 6 tests), `list_prs` `_VALID_PR_STATES` state enum validation (accepts open/closed/all, rejects invalid/empty, constant value/type tests, 7 tests), `get_check_runs` ref validation (empty/whitespace/tab rejection, 3 tests), `upsert_pr_comment` number/body validation (zero/negative number, empty/whitespace body, 4 tests) added in v150; `_validate_branch_name` (empty/whitespace/tab/valid, 5 tests), branch method input validation (`create_branch`/`switch_branch`/`fetch_branch` empty/whitespace rejection, 6 tests), commit/identity input validation (`add_and_commit` empty message, `set_local_identity` empty name/email, 6 tests) added in v148; `get_pr`/`update_pr_body` PR number validation (`number <= 0` raises ValueError, 4 tests), `list_prs` limit validation (`limit <= 0` raises ValueError, 2 tests) added in v140; `clone()` depth validation (`depth <= 0` raises ValueError, 2 tests) added in v131; `fetch_branch`, `pull` with branch, `set_local_identity`, mixed conclusion, `upsert_pr_comment` body-already-has-marker and None-body-comment added in v36; `_git_timeout` env var parsing (default/override/non-numeric/zero/negative/empty/whitespace, 7 tests), `_run_git` timeout (TimeoutExpired→RuntimeError/redacts token/passes timeout, 3 tests), `_validate_full_name` (valid/empty/whitespace/no-slash/multi-slash/trailing-slash/leading-slash/space/tab, 10 tests), `get_repo` validation (rejects invalid/accepts valid, 2 tests) added in v124; `_MAX_GIT_TIMEOUT` upper bound cap (3600s) with warning log, 6 new `TestGitTimeoutUpperBound` tests (constant/over-max/at-max/above-max/below-max/very-large) added in v125 |
| `server/app.py` | Good (27 test classes) | 85%+ | `_validate_path_param()` shared helper extracted for URL path parameter validation (8 tests: empty/whitespace/tab/newline/valid-stripped/no-padding/name-in-error/non-empty-match), Google-style docstrings for `_build_task_status()` (4 tests) and `_schedule_to_response()` (4 tests), endpoint validation for `monitor`/`get_task`/6 schedule endpoints (10 tests: 2 endpoint + 8 source verification), `_cancel_task()` refactored to use shared helper (2 tests), all skipped without fastapi; added in v187; Module-level assertions added verifying `_TERMINAL_TASK_STATES` / `_CURRENT_TASK_STATES` disjointness and `_TASK_STATE_PRIORITY` keys ⊆ `_CURRENT_TASK_STATES` (8 tests: disjoint/subset/type/contains/dict/int-values, skipped without fastapi) added in v182; Google-style docstrings added to 3 health-check helpers (`_check_redis_health`, `_check_db_health`, `_check_workers_health`) completing app.py docstring coverage (10 tests: 3 presence, 3 Returns, 4 content, skipped without server extras) added in v181; Google-style docstrings added to 4 `_ToolSkillValidatorMixin` validator methods (`_coerce_tools`, `_validate_tools`, `_coerce_skills`, `_validate_skills`) with docstring presence/Args/Returns/Raises tests (14 tests) added in v168; `__all__` export (app + 16 Pydantic models; no private names, importable, 8 tests) added in v161; Task cancellation: `TaskCancelResponse` model, `POST /tasks/{task_id}/cancel` endpoint, `_cancel_task()` helper with task_id validation, terminal state check, `celery_app.control.revoke(terminate=True, signal="SIGTERM")`, cancel button in inline HTML monitor with JS `cancelTask()`, 15 new tests (2 model, 5 helper, 5 endpoint, 3 HTML monitor) added in v160; Keychain constants (`_KEYCHAIN_SERVICE_NAME`, `_KEYCHAIN_OAUTH_KEY`, `_KEYCHAIN_ACCESS_TOKEN_KEY`) extracted from hardcoded strings in `_get_claude_oauth_token()` (5 tests), utilization numeric `isinstance` type guard (6 tests), `decode(errors="replace")` safety (2 tests), cross-module keychain sync tests (3 tests) added in v145; `_JWT_TOKEN_PREFIX` module-level constant extracted from hardcoded `"ey"` JWT heuristic in `_get_claude_oauth_token()` (4 tests: value, type, nonempty, source verification) added in v144; Preview truncation constants (`_HTTP_ERROR_BODY_PREVIEW_LENGTH`, `_USAGE_DATA_PREVIEW_LENGTH`) extracted from hardcoded slice limits (8 tests) added in v142; `_collect_celery_current_tasks` direct tests (inspector None/active/reserved/non-hh-filter/missing-id/dedup/status-fallback, 7 tests) added in v134; `_resolve_worker_capacity` env var fallback paths (valid/non-numeric/zero/negative/whitespace/priority, celery stats valid/non-dict-worker/non-int-concurrency/zero-concurrency/non-dict-pool, default fallback, 13 tests) added in v132; Health check and config helper edge cases added in v35; `/health`, `/health/services` (all-healthy/redis-error/db-na/workers-error/all-degraded), `_check_redis_health` (ok/error/import-fail), `_check_db_health` (no-url/empty/ok/error), `_check_workers_health` (ok/no-workers/empty/inspect-raises), `/health/claude-usage` (no-creds/cached/force-bypass) added in v107; direct `_get_claude_oauth_token` (JSON token/missing key/plain JWT/non-JWT/nonzero exit/empty stdout/timeout/exception/malformed JSON), `_fetch_claude_usage` (cache hit/expired/no token/HTTP error/generic exception/session+weekly/no data/force bypass/session only/cache update), `_schedule_to_response` (enabled/disabled/all fields/exception suppressed/missing attrs/run count) added in v108; `_build_form_redirect_query` (9 tests: required fields, all optional, false booleans, defaults, none values, empty/whitespace tools/skills), `_fetch_claude_usage` error body logging, `_schedule_to_response` next-run logging added in v115; `_check_redis_health`/`_check_db_health`/`_check_workers_health`/`_resolve_worker_capacity`/`_get_claude_oauth_token` debug logging on exceptions added in v116; `_ToolSkillValidatorMixin` extraction with `max_length` constraints on tools/skills (8+5 tests), mixin inheritance verification added in v119; `_get_schedule_manager` return type annotation (`ScheduleManager`) with `TYPE_CHECKING` import, `_schedule_manager` typed as `ScheduleManager | None` (3 tests) added in v125; `min_length=1` on `BuildRequest`/`ScheduleRequest` `repo_path` and `prompt` fields (4+3 tests) added in v126; `_MAX_TASK_KWARGS_LEN` size guard on `_parse_task_kwargs_str()` (3 tests: oversized/at-limit/constant) added in v127; health-check timeout constants (`_KEYCHAIN_TIMEOUT_S`, `_USAGE_API_TIMEOUT_S`, `_REDIS_HEALTH_TIMEOUT_S`, `_DB_HEALTH_TIMEOUT_S`, `_CELERY_HEALTH_TIMEOUT_S`, `_CELERY_INSPECT_TIMEOUT_S`) and Anthropic API constants (`_ANTHROPIC_USAGE_URL`, `_ANTHROPIC_BETA_HEADER`, `_USAGE_USER_AGENT`) extracted to module-level (12 tests) added in v137 |
| `lib/hands/v1/hand/iterative.py` | Excellent (97% -> 99%+) | Maintained | Google-style docstrings added to 4 remaining public methods (`BasicLangGraphHand.run`/`stream`, `BasicAtomicHand.run`/`stream`) with Args/Returns/Yields sections; docstring presence/section tests (15 tests) completing project-wide public method docstring coverage added in v193; Atomic `stream()` empty-delta branch partials covered: AssertionError sync fallback with empty `_extract_message` (2 tests) and awaitable non-iterable result with empty `_extract_message` (2 tests) added in v173; Google-style docstrings added to 21 methods across `_BasicIterativeHand` (18), `BasicLangGraphHand` (1), `BasicAtomicHand` (2) with Args/Returns/Raises sections; docstring presence/section tests (60 tests) added in v169; Bootstrap doc constants (`_README_CANDIDATES`, `_AGENT_DOC_CANDIDATES`) extracted from inline tuples (10 tests), `_BACKEND_NAME` class-level constants for `BasicLangGraphHand`/`BasicAtomicHand` replacing 6 hardcoded strings (6 tests), Google-style docstrings added to 8 key methods (6 tests) added in v162; `__all__` export (BasicAtomicHand, BasicLangGraphHand; no private names, importable, 5 tests) added in v161; `_build_tree_snapshot`, `_read_bootstrap_doc`, `_build_bootstrap_context`, `_apply_inline_edits` added in v9; `_build_tree_snapshot` empty-normalized and slash-only edge cases added in v42; `_build_tree_snapshot` slash-only and multi-slash empty-parts tests added in v51; `BasicLangGraphHand._build_agent` and `BasicAtomicHand._build_agent` (mocked imports, agent construction verification) added in v56; `_apply_inline_edits` debug logging on ValueError (1 test) added in v122; `_MAX_ITERATIONS` upper bound (1000) with warning log (5 tests), `_apply_inline_edits` OSError catch with warning log (3 tests) added in v128; `_parse_str_list` empty/whitespace rejection and whitespace stripping (3 tests) added in v134 |
| `lib/ai_providers/ollama.py` | Excellent (env vars, ImportError, kwargs, singleton) | Maintained | `_build_inner`, `_complete_impl` added in v10; `api_key_env_var`/`base_url_env_var`/`default_base_url` class attrs, `_complete_impl` delegation without kwargs, singleton identity tests added in v102 |
| `lib/hands/v1/hand/e2e.py` | Excellent (99% coverage) | Maintained | `final_pr_number is None` defensive guard after `create_pr` (returns `number=None`) and resumed PR happy path (2 tests) added in v173; `__all__` export (E2EHand; no private names, importable, 4 tests) added in v161; `_UUID_HEX_LENGTH` imported from `base.py` to replace hardcoded `[:8]` in branch naming (3 tests: import, identity, source verification) added in v144; `_E2E_MARKER_FILE` module-level constant extracted from hardcoded filename (4 tests) added in v141; `_safe_repo_dir`, `_work_base`, `_configured_base_branch`, `_build_e2e_pr_comment`, `_build_e2e_pr_body` added in v10; `run()` (dry-run/fresh-PR/resumed-PR/empty-repo/configured-base-branch/default-branch-fallback/auto-uuid), `stream()` yield added in v34; `current_branch` falsy detection path (clone_branch=None, detected="") added in v59; `final_pr_number is None` dead code documented in tech-debt-tracker in v59; `_draft_pr_enabled` (default/env true/false/yes/no/0), `create_pr` draft=True/False integration added in v105; `assert`→`RuntimeError` for `final_pr_number`, debug logging for default_branch exception (2 tests) added in v122 |
| `server/celery_app.py` | Excellent (99% coverage) | Maintained | `_ProgressEmitter` class (emit/sticky workspace/sticky model/overrides/init fields, 5 tests), `_maybe_persist_pr_to_schedule` (no schedule/input PR/non-digit/valid/exception, 5 tests), `_try_create_issue` (success/failure, 2 tests), `_sync_issue_started` (success/failure, 2 tests), `_sync_issue_status` (noop/running/completed/failed/exception, 5 tests), `_sync_issue_completed` (success/failure, 2 tests), `_sync_issue_failed` (success/failure, 2 tests), `_try_add_to_project` (no project/no issue/success/failure, 4 tests), `_get_db_url_writer` (valid/strip/missing/empty, 4 tests) added in v336; Google-style docstrings added to 4 undocumented functions (`_trim_updates`, `_append_update`, `_update_progress`, `_setup_periodic_tasks`) and 3 undocumented `_UpdateCollector` methods (`__init__`, `feed`, `flush`) with Args/Returns sections; docstring presence and section tests (14 tests, skipped without celery) added in v180; `_GITHUB_TOKEN_USER` module-level constant extracted from hardcoded clone URL string (2 tests: value/cross-module-sync, skipped without celery) added in v178; `__all__` export (build_feature, celery_app; no private names, importable, 5 tests) added in v161; `_validate_repo_spec` input validation (5 tests: empty/whitespace/no-slash/valid/trailing-slash), `_github_clone_url` validation rejection (2 tests) added in v149; `_get_db_url_writer` hardcoded credentials removed — now raises `RuntimeError` if `DATABASE_URL` not set/empty (5 tests: env set, no env raises, empty raises, whitespace raises, strips whitespace) added in v148; Keychain constants (`_KEYCHAIN_SERVICE_NAME`, `_KEYCHAIN_OAUTH_KEY`, `_KEYCHAIN_ACCESS_TOKEN_KEY`) extracted from hardcoded strings in `log_claude_usage()` (4 tests), cross-module keychain sync tests (3 tests) added in v145; `_JWT_TOKEN_PREFIX` module-level constant extracted from hardcoded `"ey"` JWT heuristic in `log_claude_usage()` (4 tests, including cross-module sync) added in v144; `_KEYCHAIN_TIMEOUT_S` and `_DB_CONNECT_TIMEOUT_S` module-level constants extracted from hardcoded timeout values (7 tests, including cross-module sync) added in v141; `_USAGE_LOG_INTERVAL_S` module-level constant extracted from hardcoded `run_every=3600.0` in `ensure_usage_schedule()` (3 tests, skipped without celery) added in v135; `_github_clone_url`, `_redact_sensitive`, `_repo_tmp_dir`, `_trim_updates`, `_append_update`, `_UpdateCollector` added in v10; `_resolve_celery_urls` all-defaults, `_resolve_repo_path` (local dir/invalid format/clone failure/PR number), `_normalize_backend` (whitespace/case/extra backends), `_has_gemini_auth` (empty/whitespace), `_update_progress` (callable/non-callable/workspace), `_collect_stream` added in v28; `_has_codex_auth` (env var set/auth file exists/neither/empty key) added in v35; `_get_db_url_writer` (env override/default), `ensure_usage_schedule` (already-exists/create/import error), `log_claude_usage` (keychain failure/no token/API HTTP error/API generic error/DB write failure/success/raw JWT/non-JWT garbage) added in v43; `_format_runtime` extraction, `_UpdateCollector` edge cases (multi-newline/split-line/buffer-boundary/whitespace/truncation), `_collect_stream` edge cases (empty/workspace/periodic), `_has_gemini_auth` in helpers added in v106; `ensure_usage_schedule` debug logging on entry-not-found and outer failure added in v116; Anthropic API constants (`_ANTHROPIC_USAGE_URL`, `_ANTHROPIC_BETA_HEADER`, `_USAGE_USER_AGENT`, `_USAGE_API_TIMEOUT_S`) extracted to module-level (5 tests, including cross-module sync) added in v137 |
| `server/schedules.py` | Excellent (98% coverage) | Maintained | `_check_optional_dep` (available true/module/false/none/install hint, 5 tests), `get_schedule_manager` (returns ScheduleManager, 1 test) added in v336; `__all__` export (CRON_PRESETS, ScheduleManager, ScheduledTask, generate_schedule_id, get_schedule_manager, next_run_time, validate_cron_expression; no private names, importable, 10 tests) added in v159; `_save_meta` Redis error handling (RuntimeError on write failure, warning log, 3 tests), `_delete_meta` Redis error handling (swallowed with warning log, 2 tests), `_list_meta_keys` Redis error handling (returns empty list with warning log, 3 tests), `_SCHEDULE_ID_HEX_LENGTH` module-level constant (4 tests) added in v143; `_check_redbeat`, `_check_croniter`, ScheduleManager with mocked Redis added in v11; `create_schedule` enabled/disabled branches, `update_schedule` enabled/disabled branches, `_create_redbeat_entry` invalid cron validation, `_delete_redbeat_entry` KeyError handling, `list_schedules` None filtering, `trigger_now` (happy path/missing/param forwarding), `get_schedule_manager`, `ScheduledTask` fix_ci/ci_check_wait_minutes roundtrip added in v33; `_delete_redbeat_entry` debug logging on KeyError, `ScheduledTask.from_dict` required-field validation (5+1 tests) added in v122; `assert`→`RuntimeError` guards for croniter (2 tests) and RedBeatSchedulerEntry (2 tests) added in v123; `_load_meta` corrupted data handling (invalid JSON/missing fields/non-dict, 4 tests) added in v128 |
| `lib/meta/tools/registry.py` (runners) | Excellent (100%) | Maintained | Google-style docstrings added to 8 private helpers (`_parse_str_list`, `_parse_positive_int`, `_parse_optional_str`, `_run_python_code`, `_run_python_script`, `_run_bash_script`, `_run_web_search`, `_run_web_browse`) with docstring presence and section tests (16 tests) added in v171; `normalize_tool_selection` input type validation — rejects dict/set/int with `TypeError` (dict/set/int rejection, None/str/list/tuple acceptance, source verification, 8 tests) added in v151; `_run_python_code`, `_run_python_script`, `_run_bash_script`, `_run_web_search`, `_run_web_browse` added in v12; `normalize_tool_selection` non-string ValueError, `format_tool_instructions_for_cli` tool-without-guidance skip added in v53; `normalize_tool_selection` empty inner comma-split token skip added in v60; `_run_bash_script` mutual exclusivity validation (neither/both/each-alone, 4 tests) added in v126; `_run_bash_script` both-None/both-provided/explicit-None validation (3 tests) added in v172; `_parse_str_list` empty/whitespace rejection and whitespace stripping (3 tests) added in v134 |
| `server/mcp_server.py` | Excellent (18 test classes, 99%) | Maintained | `__all__` export (main, mcp; no private names, importable, 5 tests) added in v161; `_INDEX_FILES_LIMIT` module-level constant extracted from hardcoded `[:200]` in `index_repo()` (4 tests: value, type, sign, source verification) added in v144; `_repo_root`, `_command_result_to_dict`, error paths (IsADirectory, Unicode, path traversal) added in v12; fixed UnicodeError handler ordering; `main()` stdio/http transport selection added in v49; non-empty input validation for `build_feature` (repo_path/prompt), `get_task_status` (task_id), `web_search` (query), `web_browse` (url) with 8 tests added in v122; `_DEFAULT_EXEC_TIMEOUT_S` and `_DEFAULT_BROWSE_MAX_CHARS` module-level constants extracted from hardcoded defaults, constant value tests and function signature default verification (7 tests) added in v135 |
| `server/app.py` (helpers) | Good (25+ test classes, 78%) | 85%+ | `_extract_nested_str_field` (first match/skip non-string/skip empty/recurse request/no match/request non-dict/strip whitespace, 7 tests), `_merge_source_tags` (add/dedup/empty existing/empty new/sort/both empty, 6 tests), `_is_recently_terminal` (non-terminal/success within window/outside window/failure/timestamp fallback/no ts/non-numeric/revoked, 8 tests), `_iter_worker_task_entries` (flatten/non-dict/non-list/non-dict entries/non-string keys, 5 tests), `_safe_inspect_call` (success/missing/non-callable, 3 tests), `_first_validation_error_msg` (extract/empty errors/custom fallback, 3 tests), `_is_running_in_docker` (dockerenv/env var/neither, 3 tests) added in v336; `_parse_backend`, `_task_state_priority`, `_normalize_task_status`, `_extract_task_id/name/kwargs`, `_coerce_optional_str`, `_parse_task_kwargs_str`, `_is_helping_hands_task`, `_upsert_current_task`, `_flower_timeout_seconds`, `_flower_api_base_url` added in v13; `_check_redis_health` (ok/error), `_check_db_health` (na/ok/error), `_check_workers_health` (ok/none/empty/exception) added in v35 |
| `lib/hands/v1/hand/cli/base.py` (CI/PR) | Good (6 test classes) | 80%+ | Hook/display constants (`_HOOK_ERROR_TRUNCATION_LIMIT`, `_GIT_REF_DISPLAY_LENGTH`) extracted from hardcoded values (8 tests) added in v142; `_build_ci_fix_prompt`, `_format_ci_fix_message`, `_format_pr_status_message`, `_looks_like_edit_request` added in v13; `stream()` CI fix message yield, pr_status_message=None skip, CI fix exhausted message added in v44; `_try_fix_git_hook_errors` fallback-also-FileNotFoundError and fallback-TimeoutExpired paths (2 tests) added in v120; defensive `.get()` defaults in `_poll_ci_checks`/`_ci_fix_loop` for malformed GitHub API responses, `_build_ci_fix_prompt` timed_out conclusion coverage (7 tests) added in v130 |
| `cli/main.py` (helpers) | Excellent (99% coverage) | Maintained | `_GITHUB_TOKEN_USER` module-level constant extracted from hardcoded clone URL string (2 tests: value/cross-module-sync) added in v178; Google-style docstrings added to 4 private methods (`_stream_hand`, `_github_clone_url`, `_git_noninteractive_env`, `_resolve_repo_path`) with docstring presence/Args/Returns/Raises tests (8 tests) added in v174; `__all__` export (build_parser, main; no private names, importable, 5 tests) added in v161; `_validate_repo_spec` input validation (7 tests: empty/whitespace/no-slash/valid/trailing/leading/multi-slash), `_github_clone_url` validation rejection (2 tests) added in v149; `--github-token` CLI arg parsing, default None, wired to Config overrides for both E2E and backend paths (4 tests) added in v147; `--pr-number` positive integer validation with early exit (4 tests) added in v141; Module-level constants (`_DEFAULT_CLONE_DEPTH`, `_TEMP_CLONE_PREFIX`) extracted from hardcoded values (5 tests) added in v138; `_github_clone_url`, `_git_noninteractive_env`, `_redact_sensitive`, `_repo_tmp_dir`, `opencodecli` backend, `model_not_found` error, invalid `--tools` added in v14; `docker-sandbox-claude` backend branch, `_stream_hand` (chunk printing/trailing newline), Python <3.12 atomic error, generic exception re-raise for non-CLI backends, `__version__` accessibility added in v41; `_clone_reference_repos` coverage: invalid repo spec skip with warning (2 tests), timeout skip with warning (2 tests), successful clone appends to repo_index (2 tests), clone failure skip (1 test), multiple successful clones (1 test), mixed invalid+valid flow (1 test), multiple timeout recovery (1 test) added in v172; `--max-iterations` positive integer validation with early exit (4 tests: zero exit, negative exit, error message, source verification) added in v189 |
| `lib/hands/v1/hand/model_provider.py` (builders) | Excellent (99% coverage) | Maintained | `build_langchain_chat_model` (all 5 providers + ImportError paths + env vars), `build_atomic_client` (OpenAI, LiteLLM + missing attr, unsupported) added in v15 |
| `lib/ai_providers/openai.py` | Good (dedicated tests) | 85%+ | `_build_inner` (ImportError, with/without API key), `_complete_impl` (delegation, kwargs) added in v16 |
| `lib/ai_providers/google.py` | Good (build_inner + complete_impl) | 85%+ | `_complete_impl` (delegation, empty content filtering, kwargs) added in v16; `_build_inner` covered in v11; empty messages guard (all-empty/single-empty content raises ValueError, 2 tests) added in v131 |
| `lib/hands/v1/hand/cli/claude.py` | Excellent (100% coverage) | Maintained | `_StreamJsonEmitter._process_line` JSON primitive passthrough, non-dict block skip in assistant/user events (4 tests) added in v335; Module-level constants (`_TEXT_PREVIEW_MAX_LENGTH`, `_TOOL_RESULT_PREVIEW_MAX_LENGTH`, `_COMMAND_PREVIEW_MAX_LENGTH`, `_FAILURE_OUTPUT_TAIL_LENGTH`) extracted from hardcoded values (10 tests) added in v139; `_command_not_found_message`, `_native_cli_auth_env_names`, `_pr_description_cmd` added in v16; _StreamJsonEmitter edge cases (empty text/result/tool_result, non-dict items, unknown events, multi-line chunks, empty flush), `_invoke_claude`/`_invoke_backend` async tests added in v31; `_skip_permissions_enabled` geteuid exception path, `_build_failure_message` instance delegation, `_StreamJsonEmitter` empty-line-between-events added in v48; `_StreamJsonEmitter` unknown content block type skip, whitespace-only text preview skip, `_skip_permissions_enabled` geteuid-not-callable path added in v58; `_StreamJsonEmitter` non-dict message defense (assistant/user string/null/missing/list), `_summarize_tool` direct tests (all 7 tools + unknown + missing fields), `_invoke_claude` flush-on-error (`try/finally`), `_inject_output_format` empty cmd added in v110; `_summarize_tool` expanded with Agent/WebFetch/WebSearch/NotebookEdit/TodoWrite/MultiTool (11 new tests), result event token usage tracking (6 new tests) added in v113; `_skip_permissions_enabled` geteuid exception debug logging (1 test) added in v122; `_summarize_tool` expanded with Skill/CronCreate/CronDelete/CronList/EnterWorktree/ExitWorktree (12 tests) added in v123 |
| `lib/hands/v1/hand/cli/base.py` (helpers) | Good (74% coverage) | 80%+ | `_APPLY_CHANGES_TRUNCATION_LIMIT` and `_STREAM_READ_BUFFER_SIZE` module-level constants extracted (7 tests) added in v140; `_FILE_LIST_PREVIEW_LIMIT` imported from `base.py` to replace hardcoded `[:200]` in `_build_init_prompt` (2 tests) added in v139; Module-level constants (`_PROCESS_TERMINATE_TIMEOUT_S`, `_CI_POLL_INTERVAL_S`, `_PR_DESCRIPTION_TIMEOUT_S`) extracted from hardcoded values (7 tests) added in v138; `_resolve_cli_model` None guard (`"None"` → default fallback, 1 test) added in v132; `_resolve_cli_model`, `_inject_prompt_argument`, `_normalize_base_command`, `_build_failure_message`, `_describe_auth`, `_effective_container_env_names`, `_build_subprocess_env`, `_interrupted_pr_metadata` added in v17; `_effective_container_env_names` empty-blocked early return added in v42; `_apply_verbose_flags` empty-cmd guard, `_build_failure_message` `_SUMMARY_CHAR_LIMIT` consistency (3+4 tests) added in v117 |
| `lib/ai_providers/anthropic.py` | Good (complete_impl kwargs) | 85%+ | Extra kwargs forwarding test added in v17; `_DEFAULT_MAX_TOKENS` module-level constant extracted from hardcoded `max_tokens=1024` default (3 tests) added in v140 |
| `lib/ai_providers/litellm.py` | Good (complete_impl kwargs) | 85%+ | Extra kwargs forwarding test added in v17 |
| `lib/hands/v1/hand/base.py` (statics) | Excellent (83% -> 95%) | Maintained | `_github_repo_from_origin` edge cases, `_run_precommit_checks_and_fixes`, `_push_noninteractive`, `_push_to_existing_pr`, `_should_run_precommit_before_pr`, `_finalize_repo_pr` error paths added in v18; `_update_pr_description` (rich/fallback/exception-suppressed), `_create_pr_for_diverged_branch` (rich/fallback/follow-up link) added in v26; `_run_git_read` success/failure, `_finalize_repo_pr` early returns (no_repo/not_git_repo/no_changes/disabled/no_github_origin) added in v29 |
| `lib/meta/tools/command.py` | Good (73% -> 85%+) | 85%+ | `_resolve_python_command` (uv/direct/neither/empty), `_run_command` timeout (with/without output, zero/negative), `run_python_code` empty validation, `run_python_script` (missing/directory), `run_bash_script` (missing/directory/empty-both/inline-args) added in v18 |
| `lib/hands/v1/hand/cli/base.py` (prompts/container) | Good (78% -> 92%+) | 90%+ | `_execution_mode`, `_container_enabled` (env var truthy/falsy/missing/empty/no-config), `_container_image` (set/missing/empty/no-config/whitespace), `_apply_verbose_flags` (on/off/duplicates/no-flags), `_build_init_prompt` (repo root, file list, cap at 200, empty list), `_build_task_prompt` (prompt, summary, truncation, placeholder), `_build_apply_changes_prompt` (prompt, output, truncation, placeholder) added in v19; `_stage_skill_catalog` (stages to temp dir/no-op), `_cleanup_skill_catalog` (removes dir/no-op), `_wrap_container_if_enabled` (disabled/docker-not-found/env-forwarding/mount), `_build_task_prompt` tool/skill sections (include/omit branches) added in v29 |
| `lib/hands/v1/hand/cli/base.py` (render/finalize) | Good (80%+) | 85%+ | `_truncate_summary` positive limit validation (`limit < 1` raises `ValueError`, 0/-1/-100 rejection, limit=1/exact acceptance, source verification, 6 tests) added in v151; `_render_command` base-level tests (placeholder/model/prompt/verbose/repo/multi-placeholder), `_finalize_after_run` direct tests (interrupted/normal/truncated/auto-pr), `_collect_run_output` (collects/empty/newlines), `_truncate_summary` (stripped/exact/over/empty/whitespace) added in v114 |
| `lib/hands/v1/hand/cli/base.py` (command/timing) | Good (75% -> 76%+) | 80%+ | `_repo_has_changes` timeout handling (`TimeoutExpired` returns False, logs warning, 2 tests), `_base_command` error message redaction (raw env var value no longer exposed, 1 test) added in v149; `_base_command` (default/env override/empty raises), `_io_poll_seconds`/`_heartbeat_seconds`/`_idle_timeout_seconds` (defaults/env overrides/verbose heartbeat), `_repo_has_changes` (with changes/clean/non-git) added in v20; `_base_command` shlex.split error wrapping (unclosed quote/valid passthrough) added in v115 (moved to `test_cli_hand_base_utils.py` in v120 to fix fastapi importorskip gate); `_float_env` warning logging (non-numeric/zero/negative/valid/unset, 5 tests) added in v118; `_repo_has_changes` debug logging on git status failure (1 test) added in v123 |
| `lib/hands/v1/hand/cli/base.py` (retry/interrupt) | Good (76% -> 77%+) | 80%+ | `_should_retry_without_changes` (all 4 branches: feature off, interrupted, not edit, has changes), `_no_change_error_after_retries` (base returns None), `_build_apply_changes_prompt` (prompt+output, empty output, truncation), `_terminate_active_process` (None/exited/terminate/kill-on-timeout), `interrupt()` (active/None/exited) added in v22 |
| `lib/hands/v1/hand/iterative.py` (iteration helpers) | Excellent (79% -> 97%+) | Maintained | `_build_iteration_prompt`, `_execution_tools_enabled`/`_web_tools_enabled`, `_tool_instructions`, `BasicLangGraphHand._result_content`, `BasicAtomicHand._extract_message` added in v20; `_execute_read_requests` error paths, `_run_tool_request` dispatch, `_execute_tool_requests` error pass-through added in v21; `BasicLangGraphHand.stream()` (satisfied/max-iter/interrupt/file-changes/PR metadata) and `BasicAtomicHand.stream()` (satisfied/max-iter/interrupt/assertion-fallback/awaitable/PR metadata) added in v26; `BasicAtomicHand.run()` interrupted/max_iterations status paths added in v39; `BasicAtomicHand.stream()` delta-without-prefix (assertion-fallback/async-iter/awaitable paths), file-change yield, tool-result yield added in v40; `BasicLangGraphHand.run()` max_iterations status path, LangGraph/Atomic `stream()` pr_url yield at max iterations added in v42; LangGraph/Atomic `stream()` pr_status elif branches (entered/skipped for both satisfied and max-iterations paths), LangGraph interrupted inner loop, non-chat-model event skip, empty text skip, Atomic async-iter duplicate delta skip, awaitable empty delta skip added in v45; Atomic `stream()` `run_async` non-AssertionError exception re-raise (except Exception path) added in v46; Atomic `stream()` non-AssertionError debug logging before re-raise (1 test) added in v129 |

| `lib/hands/v1/hand/atomic.py` | Excellent (78% -> 98%+) | Maintained | `_build_agent` (mocked atomic_agents imports), `run()` (HandResponse structure, PR metadata), `stream()` (AssertionError sync fallback, async iterator path, awaitable path, awaitable AssertionError fallback, PR URL yield), `_make_input` added in v23; `stream()` chat_message falsy branches (assertion fallback/async iter/awaitable paths) added in v45; `stream()` non-AssertionError exception propagation (`except Exception: raise` path) added in v57; `stream()` non-AssertionError debug logging before re-raise (1 test) added in v129 |
| `lib/hands/v1/hand/langgraph.py` | Good (83% -> 95%+) | 90%+ | Google-style docstrings added to `__init__`, `_build_agent`, `run` (7 docstring tests: presence, Args, Returns) added in v168; `_build_agent` (mocked create_react_agent), `run()` (HandResponse, str fallback, PR metadata), `stream()` (chunk extraction, non-chat event filtering, empty content skip, PR URL yield) added in v23; `run()` defensive message access (empty/missing/None messages, 5 tests) added in v117; `_build_agent` streaming=True assertion (1 test), `run()` empty/missing/None messages (3 tests), `stream()` chunk without content attribute (1 test) added in v132 |
| `lib/hands/v1/hand/cli/goose.py` | Excellent (91% -> 99%) | Maintained | `_OLLAMA_DEFAULT_HOST` module-level constant extracted from hardcoded fallback in `_resolve_ollama_host()` (5 tests: value, type, URL validity, resolve function usage, build_subprocess_env usage) added in v143; `_describe_auth` (all providers, key set/not set), `_normalize_base_command` (bare goose/goose run/goose run --instructions/passthrough), `_pr_description_cmd` (anthropic with/without claude, non-anthropic), `_has_goose_builtin_flag`, `_apply_backend_defaults`, `_resolve_ollama_host` added in v23; `_resolve_goose_provider_model_from_config` (bare model/provider-slash/default/empty/gemini-normalized/gpt-inferred), `_invoke_backend` delegation added in v48; refactored to shared `make_cli_hand` fixture in v64; `_build_subprocess_env` (GH_TOKEN/GITHUB_TOKEN propagation, missing token RuntimeError, provider/model from config, env overrides, OLLAMA_HOST injection, default model, 10 tests) added in v129 |
| `lib/hands/v1/hand/cli/base.py` (CI fix loop) | Good (78% -> 80%+) | 80%+ | `_ci_fix_loop` (all early-return paths, success/no_checks/pending/failure conclusions, fix-with-changes-then-success, no-changes-exhausted, interrupt before/after poll, exception error status), `_poll_ci_checks` (immediate return, poll-until-deadline), `run()` (collect+finalize, CI fix triggered, CI fix skipped when no PR, CI fix noop emit callable added in v49), `stream()` (chunk yielding, PR status message, CI fix integration) added in v24; `stream()` consumer-break producer_task cancellation path added in v51 |
| `lib/hands/v1/hand/cli/base.py` (subprocess invoke) | Good (92% -> 95%+) | 95%+ | `_invoke_cli_with_cmd` FileNotFoundError (no fallback/with fallback/npx fallback/same-as-original raises), stdout-None, non-zero exit (no retry/with retry/same-as-original raises), idle timeout termination, verbose mode (cmd+cwd+finished emitted) added in v39; `_invoke_backend` delegation, `_run_two_phase` skill catalog staging/cleanup (including cleanup on exception) added in v40; `_run_two_phase_inner` verbose mode (model/heartbeat/phase timings), auth display (empty/non-empty), default model fallback added in v44; `_invoke_cli` delegation to `_invoke_cli_with_cmd`, `stream()` producer error re-raise added in v46; IO loop interrupt break and process-exited-during-timeout break added in v49 |
| `lib/meta/tools/web.py` | Excellent (99% coverage) | Maintained | `_DEFAULT_WEB_TIMEOUT_S` module-level constant extracted from 2× hardcoded `timeout_s: int = 20` defaults in `search_web`/`browse_url` (5 tests: value/type/positive/2 signature defaults) added in v194; Google-style docstrings added to 4 private helpers (`_require_http_url`, `_strip_html`, `_as_string_keyed_dict`, `_extract_related_topics`) with docstring presence and section tests (8 tests) added in v171; `__all__` export (5 public symbols, no private names, importable, 8 tests), `_DUCKDUCKGO_API_URL` module-level constant extracted from hardcoded URL (4 tests: value, type, https, source verification) added in v158; `search_web`/`browse_url` URLError/HTTPError → RuntimeError wrapping with debug logging (6 tests: HTTP 503/429/404/500, URLError connection refused/DNS failure) added in v131 | `_extract_related_topics` (text+url extraction, recursive Topics, non-dict/missing/empty skips), `_require_http_url` (whitespace-only, no-scheme), `_strip_html` (noscript, blank line collapsing), `search_web` (invalid params, unexpected format, dedup, empty URLs, max_results cap), `browse_url` (invalid params, non-HTML content, HTML-by-body-detection) added in v24; `search_web` RelatedTopics non-list skip added in v53; dataclass construction tests (WebSearchItem/WebSearchResult/WebBrowseResult frozen immutability, equality, hashability), `_decode_bytes` edge cases (empty bytes, UTF-8 BOM, multibyte, UTF-16 BOM, latin-1 high bytes) added in v65; `_as_string_keyed_dict` direct tests (valid/empty/non-dict/non-string-keys/mixed, 5 tests), `_require_http_url` host validation (no-host/with-port/ftp/whitespace, 6 tests) added in v126 |
| `lib/hands/v1/hand/cli/gemini.py` | Excellent (98% -> 99%) | Maintained | Google-style docstrings added to 14 methods (14 docstring presence tests) in v166; `_describe_auth` (key set/not set/empty), `_pr_description_cmd` (gemini found/not found), `_command_not_found_message` added in v25; `_build_failure_message` instance delegation, `_invoke_gemini`/`_invoke_backend` async delegation added in v48 |
| `lib/hands/v1/hand/cli/codex.py` | Excellent (92% -> 98%) | Maintained | Google-style docstrings added to 12 methods (12 docstring presence tests) in v166; `_command_not_found_message`, `_native_cli_auth_env_names`, `_apply_codex_exec_sandbox_defaults` (empty/whitespace env override fallback) added in v25; `_build_failure_message` delegation, `_invoke_codex`/`_invoke_backend` async delegation added in v46 |
| `lib/hands/v1/hand/cli/opencode.py` | Excellent (91% -> 100%) | Maintained | `_resolve_cli_model` None guard (`"None"` → default fallback, 1 test) added in v132; `_build_failure_message` delegation, `_build_opencode_failure_message` additional auth token variations (case-insensitive, non-auth distinction, exit code) added in v26; `_invoke_opencode`/`_invoke_backend` async delegation added in v46 |
| `lib/hands/v1/hand/cli/docker_sandbox_claude.py` | Excellent (100%) | Maintained | Google-style docstrings added to 4 methods (`_invoke_claude`, `_run_two_phase`, `_build_failure_message`, `_command_not_found_message`) with Args/Returns sections (8 docstring tests), `_AUTH_FAILURE_SUBSTRINGS` tuple constant extracted from hardcoded strings (7 tests: type/nonempty/all-strings/all-lowercase/values/source) added in v174; Sandbox naming constants (`_SANDBOX_NAME_MAX_LENGTH`, `_SANDBOX_UUID_HEX_LENGTH`) extracted from hardcoded values (7 tests) added in v142; Class attrs, `_resolve_sandbox_name` (env override/auto-gen/cache/sanitize), `_should_cleanup` (truthy/falsy), `_wrap_sandbox_exec` (wrapping/env forwarding), `_execution_mode`, `_build_failure_message` (auth/generic/sandbox note), `_command_not_found_message`, `_fallback_command_when_not_found`, `_docker_sandbox_available` (success/fail/FileNotFoundError), `_ensure_sandbox` (skip/docker-not-found/plugin-unavailable/success/failure/template), `_remove_sandbox` (skip/stop+rm) added in v27; `_invoke_claude` (sandbox-wrapped cmd/result/raw fallback), `_run_two_phase` (ensure+cleanup/skip-cleanup/cleanup-on-exception) added in v40; `_ensure_sandbox` verbose=False branch, `_build_failure_message` sandbox-already-in-base skip added in v60; `_ensure_sandbox` stdout-None RuntimeError (assert→explicit guard, 1 test) added in v121 |
| `lib/meta/skills/__init__.py` | Excellent (100%) | Maintained | `normalize_skill_selection` input type validation — rejects dict/set/int with `TypeError` (dict/set/int rejection, None/str/list/tuple acceptance, source verification, 8 tests) added in v151; `normalize_skill_selection` non-string ValueError, `stage_skill_catalog` missing .md skip added in v30; `_discover_catalog` returns empty when catalog dir missing added in v46; `normalize_skill_selection` whitespace-only token skip added in v58; `_discover_catalog` no-heading title fallback (branches 42->47, 44->42) added in v60 |
| `lib/hands/v1/hand/pr_description.py` | Excellent (150+ tests, all paths) | Maintained | Parser marker constants (`_PR_TITLE_MARKER`, `_PR_BODY_MARKER`, `_COMMIT_MSG_MARKER`) extracted from hardcoded strings, `_COMMIT_TYPE_PREFIX_RE` regex constant DRYing duplicate inline patterns in `_is_trivial_message` and `_commit_message_from_prompt` (22 tests: value/type/colon/usage/regex match/source verification) added in v174; `_commit_message_from_prompt` boilerplate-then-content extraction tests (4 tests, branch 581→583 confirmed unreachable — added to tech debt tracker) added in v173; `__all__` export (PRDescription, generate_commit_message, generate_pr_description; no private names, importable, 6 tests) added in v159; Dead `if cmd else "cli"` removal in `generate_pr_description()`/`generate_commit_message()` (2 tests), `_COMMIT_MSG_DIFF_LIMIT`/`_COMMIT_MSG_TIMEOUT` constant docstrings and value tests (6 tests) added in v147; `_truncate_text()` helper for prompt/summary truncation with `...[truncated]` indicator (8 tests), `_infer_commit_type()` with `_COMMIT_TYPE_KEYWORDS` dict and word-boundary matching replacing hardcoded `"feat:"` prefix (24 tests), prompt builder truncation indicator tests (7 tests), `_commit_message_from_prompt` type inference tests (7 tests) added in v146; `_truncate_diff` limit validation (`limit <= 0` raises ValueError, 4 tests) added in v140; Module-level constants (`_PR_SUMMARY_TRUNCATION_LENGTH`, `_COMMIT_SUMMARY_TRUNCATION_LENGTH`, `_PROMPT_CONTEXT_LENGTH`, `_PR_ERROR_TAIL_LENGTH`, `_COMMIT_ERROR_TAIL_LENGTH`, `_COMMIT_MSG_MAX_LENGTH`) extracted from hardcoded values (13 tests) added in v139; `_is_trivial_message()` guard rejecting trivially short/meaningless commit messages (constant, prefix stripping, punctuation-only, short body, 15 tests), `_parse_commit_message` trivial rejection (5 tests), `_commit_message_from_prompt` trivial rejection (4 tests) added in v136; Warning logging for invalid/non-positive `_timeout_seconds`/`_diff_char_limit` env var values (4 tests), `_get_diff`/`_get_uncommitted_diff` `FileNotFoundError` graceful handling when git not installed (2 tests) added in v133 | `_diff_char_limit` negative, `_get_diff` empty stdout on success, `_build_prompt`/`_build_commit_message_prompt` summary truncation, `_parse_output` whitespace-only body, `_commit_message_from_prompt` whitespace edge cases added in v32; `_commit_message_from_prompt` punctuation sentence boundaries (exclamation/question), prefix stripping (fix/refactor/docs/chore/test/ci/case-insensitive/parenthetical scope), single word, trailing period removal; `_parse_output` inline body marker, title whitespace, body whitespace/internal blanks, late title; `_build_prompt`/`_build_commit_message_prompt` whitespace-only summary, conventional commit/72-char/single-line guidance validation added in v100; direct `_is_boilerplate_line` tests (bracket banners, numbered lists, bullets, all 18 prefixes, case insensitivity, non-boilerplate, empty string, 26 tests) added in v125; `generate_pr_description()` `base_branch` empty/whitespace validation (3 tests: empty/whitespace/tab), `backend` empty/whitespace validation (2 tests: empty/whitespace), cmd=None skips validation (1 test), valid params pass (1 test); `generate_commit_message()` `backend` empty/whitespace/tab validation (3 tests), valid backend passes (1 test) added in v189 |
| `lib/hands/v1/hand/placeholders.py` | Excellent (100%) | Maintained | Backward-compat shim re-export verification (class symbols, module aliases, `__all__`, identity checks) added in v34 |
| `lib/hands/v1/hand/__init__.py` | Excellent (100%) | Maintained | Package re-export tests (`__all__` completeness, symbol identity, subprocess alias) added in v37 |
| `lib/ai_providers/__init__.py` | Excellent (100%) | Maintained | PROVIDERS dict completeness, singleton identity, `__all__` verification added in v37 |
| `lib/default_prompts.py` | Excellent (100%) | Maintained | `__all__` export (contains DEFAULT_SMOKE_TEST_PROMPT, no private names, importable, 4 tests) added in v158; Constant content assertions (directives, guards, references) covered in test_default_prompts.py; structural validation (numbered steps, directive uniqueness, guard ordering, safety note, script references, python version, reasonable length) added in v100 |
| `lib/hands/v1/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness, symbol identity with hand package, all 10 re-exports verified added in v38 |
| `lib/hands/v1/hand/cli/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness (7 symbols), identity checks against source modules added in v38 |
| `lib/meta/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness (`skills`, `tools`), submodule identity added in v38 |
| `lib/meta/tools/__init__.py` | Excellent (100%) | Maintained | `__all__` completeness (21 symbols), identity checks for all 4 submodules (filesystem, command, registry, web) added in v38 |
| `lib/ai_providers/types.py` | Excellent (100%) | Maintained | `__all__` export (contains AIProvider/PromptInput/normalize_messages, no private names, importable, 5 tests), `normalize_messages` Google-style docstring (has Args/Returns/Raises sections, 4 tests) added in v157; `normalize_messages` (string/sequence/empty/missing keys), `AIProvider` base (lazy init, complete, acomplete, model override) covered in test_ai_providers.py |
| `server/multiplayer_yjs.py` | Excellent (99% coverage) | Maintained | `get_player_activity_summary` awareness=None skip, unparseable state skip; `get_decoration_state` ydoc=None skip, ydoc.get exception skip, deco_map=None skip (5 tests) added in v335; `validate_awareness_state`, `get_multiplayer_stats`, `get_connected_players`, `get_player_activity_summary`, `get_decoration_state`, `_clamp_float`, `_strip_control_chars`, `_parse_awareness_state`, `_extract_player_state` added in v308/v313/v320/v322 |
| `server/task_result.py` | Excellent (100%) | Maintained | `__all__` export (contains normalize_task_result, no private names, importable, 4 tests) added in v158; All branches of `normalize_task_result` (None/dict/exception/other/bool/list/custom) covered in test_task_result.py |
| `frontend/src/App.tsx` (utils) | Excellent (100 tests) | 80%+ | `shortTaskId`, `statusTone`, `parseBool`, `extractUpdates`, `parseOptimisticUpdates`, `parseError` (detail/no-detail/non-JSON/empty body), `isTerminalTaskStatus` (terminal/non-terminal/whitespace), `apiUrl`, `loadTaskHistory`, `upsertTaskHistory` added in v47; `backendDisplayName`, `providerFromBackend` (all 10 backends), `formatProviderName` (openai/opencode/e2e/generic), `repoName` (slash/trailing-slash/no-slash), `cronFrequency` (9 presets + fallbacks), `buildDeskSlots` (count/ids/layout), `checkDeskCollision` (overlap/far/empty), `asRecord` (object/null/primitives), `readStringValue` (string/number/null/non-finite), `readBoolishValue` (bool/string/non-bool), `readSkillsValue` (array/empty/non-array), `parseOptimisticUpdates` edge cases (bold/exec/mcp/failed exec/long lines/dedup/flush at EOF) added in v50; `statusTone` additional statuses (RECEIVED/RETRY/SCHEDULED/RESERVED/SENT/ERROR), `cronFrequency` fallback patterns (hourly/minute-interval/empty), `loadTaskHistory` edge cases (invalid JSON/non-array/empty taskId/limit enforcement), `upsertTaskHistory` (empty/whitespace taskId, default values) added in v52; `statusBlinkerColor` (ok/fail/run/idle tones, empty string, 7 tests) added in v130 |
| `frontend/src/App.tsx` (component) | Excellent (62 tests, 82.3% stmts) | Maintained | Smoke render test, default form values (repo path, prompt), empty task list, service health bar, dashboard view toggles, navigation buttons, submitted tasks header added in v50; interaction tests (Hand world/Classic view toggle, schedule navigation, New submission return, Advanced settings expand, repo path/prompt/backend input changes, Clear button disabled state) added in v52; form submission tests (POST payload validation, error handling via network/server errors, model/tools/skills inclusion, checkbox toggles, max iterations), monitor view tests (output tab switching Updates/Raw/Payload, task ID badge, status blinker, task inputs section), schedule view tests (form rendering, field changes, cron preset dropdown, schedule creation API, Cancel button, Refresh, error handling) added in v54; schedule CRUD tests (edit/delete/trigger/toggle), task discovery polling, notification banner/toast UI, monitor resize/scroll, poll error handling added in v55; form validation tests (empty repo_path/prompt rejection before API call, 2 tests) added in v130 |

## Type checking

`ty check` runs in CI with `--ignore unresolved-import --ignore invalid-method-override`.
All stale `ty: ignore` suppression comments were removed in v152 (2026-03-13) —
the underlying issues were resolved in newer `ty` versions. A codebase-wide
regression guard test (`test_v152_ty_ignore_cleanup.py::TestNoTyIgnoreInSource`)
prevents reintroduction.

## Remaining coverage gaps

All remaining uncovered lines are inherently untestable entry point guards or
defensive fallbacks.  See `docs/exec-plans/tech-debt-tracker.md` for details.

Dead code in `codex.py`, `goose.py`, `e2e.py`, and `iterative.py` was removed
in v104 (2026-03-10).

| Module | Line(s) | Reason |
|---|---|---|
| `cli/main.py` | 367 | `if __name__` guard |
| `cli/base.py` | 552->559 | Heartbeat-without-timeout timing |
| `web.py` | 66 | Defensive fallback (`pragma: no cover`; latin-1 accepts all bytes) |
| `mcp_server.py` | 393 | `if __name__` guard |

## Areas for improvement

- [x] Add type checking to CI (ty, when stable for CI runners) — added in v109
- [ ] Add mutation testing for critical path safety (filesystem tools)
- [ ] Add load testing for app mode concurrent task handling
- [x] Add tests for `_build_tree_snapshot` / `_build_bootstrap_context` (iterative.py) — added in v9
- [x] Add tests for `_read_bootstrap_doc` / `_apply_inline_edits` (iterative.py, needs tmp_path) — added in v9
- [x] Increase coverage for CLI hand CI fix loop and run/stream wrappers — added in v24
