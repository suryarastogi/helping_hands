# Project todos

## Open items

  - [ ] commit message created by claudecodecli still mediocore
'''
ex: 
feat: -
feat: ...[truncated]
'''

- [ ] per task github token override -- ability to add a github token for a specific task, so that it can be used for that task instead of the default token. This would be useful for tasks that require different permissions than the default token.

Also useful if no default token is set, but you want to use a token for a specific task.

- [ ] kill signal from UI for a task -- ability to send a kill signal from the UI to stop a task that is currently running. This would be useful for tasks that are taking too long or are no longer needed.

- [ ] checkout additional repos as read-only for reference (read-only dir) -- ability to checkout additional repos as read-only for reference during task execution. This would be useful for tasks that require access to multiple repos, but only need read-only access to some of them.
